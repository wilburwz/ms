import type { KnowledgePoint } from '../types'

export const vueData: KnowledgePoint[] = [
  {
    id: "vue-01",
    title: "Vue 3 响应式原理：Proxy vs Object.defineProperty",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 使用 Proxy 替代 Object.defineProperty，带来了哪些改进？",
    answer: 'Vue 3 换 Proxy 替代 defineProperty，这个决定当时争议挺大，但现在回头看是绝对正确的。\n\nVue 2 的 Object.defineProperty 有两个硬伤：新加属性检测不到（得用 Vue.set），数组索引赋值和 length 变化检测不到。这两个坑在实际开发里真的烦——动态表单新加字段、数组批量更新数据，每次都得记着用 Vue.set。我们项目里光是一个动态配置表单，因为忘记用 Vue.set 导致的 bug 就有好几个。\n\nProxy 完全解决了这些问题。它能代理整个对象，拦截 13 种操作——get、set、deleteProperty、has、ownKeys 等等。你新增一个属性，Proxy 的 set 会被触发；你删一个属性，deleteProperty 被触发；数组按索引赋值，也能被拦到。用了 Proxy 后你再也不用考虑"这个属性是不是响应式的"——只要是通过 reactive 或 ref 创建的，全量响应式。\n\n底层原理上，Vue 3 把依赖追踪和触发分成三个角色：reactive/ref 数据源被 Proxy 包裹，effect 是副作用函数（组件渲染、computed、watch），track/trigger 负责在读取时收集依赖、在写入时触发更新。整个过程自动完成，不用手动声明依赖数组。\n\n和 React 比最大的优势：不用 useMemo、useCallback 这些 hooks 来手动声明依赖关系。Vue 的响应式系统会自动追踪，不会出现 stale closure 的问题。当然了，这也意味着 Vue 的运行时比 React 稍重（有整个响应式系统），而 React 的设计哲学是"重新渲染一切，由 diff 来优化"。两种思路，没有绝对好坏。',
    codeExample: "// Vue 3 简化版 reactive\nfunction reactive<T extends object>(obj: T): T {\n  return new Proxy(obj, {\n    get(target, key, receiver) {\n      track(target, key)  // 依赖收集\n      const result = Reflect.get(target, key, receiver)\n      if (typeof result === 'object' && result !== null) {\n        return reactive(result)  // 惰性深层响应\n      }\n      return result\n    },\n    set(target, key, value, receiver) {\n      const oldValue = target[key]\n      const result = Reflect.set(target, key, value, receiver)\n      if (oldValue !== value) {\n        trigger(target, key)  // 触发更新\n      }\n      return result\n    }\n  })\n}",
    tags: ["Vue3", "响应式", "Proxy", "reactive"],
  },
  {
    id: "vue-02",
    title: "Vue 3 Composition API 设计动机",
    category: "vue",
    difficulty: 2,
    question: "为什么 Vue 3 引入 Composition API？与 Options API 对比有什么优势？",
    answer: 'Composition API 出来的时候很多人不解——Vue 2 的 Options API 不是挺好用的吗，为啥要加这一套？用了两年下来我理解了这个决策。\n\nOptions API 最大的问题不是功能不足，而是逻辑分散。一个复杂组件，响应式数据在 data 里，计算属性在 computed 里，方法在 methods 里，生命周期在 mounted 里。小项目没问题，但组件稍微大一点——你看一个功能的代码要跨 data → computed → methods → mounted 四个位置去翻，理解成本高。\n\nComposition API 把同一逻辑相关的代码聚在一起。比如一个数据加载逻辑：const data = ref()、const loading = ref(false)、const error = ref(null)、onMounted(() => fetchData())——这些全写在一段 setup 函数里，一目了然。\n\n第二个好处是逻辑复用。Options API 时代逻辑复用靠 mixin（命名冲突、来源不透明）或者 scoped slots（太绕）。Composition API 靠 composable 函数——把逻辑抽成一个 useXXX 函数，在组件里引用就行。来源明确、命名可控、TypeScript 友好。\n\n第三个好处是 TypeScript 支持。Options API 里 this 的类型推断一直有问题（this 上的属性时有时无）。Composition API 用普通函数和变量，TS 推断自然就对了。\n\n但 Composition API 也有学习成本——ref 的 .value 用起来刚开始不习惯、setup 里 return 稍显啰嗦。不过 3.2 加了 script setup 语法糖后这些问题基本解决了，现在写 Vue 3 首选 script setup + Composition API。',
    codeExample: "// Options API - 逻辑分散\nexport default {\n  data() { return { count: 0, keyword: '' } },\n  computed: { double() { return this.count * 2 } },\n  methods: { increment() { this.count++ } },\n  watch: { keyword(v) { this.search(v) } }\n}\n\n// Composition API - 逻辑聚合\nimport { ref, computed, watch } from 'vue'\n\nfunction useCounter() {\n  const count = ref(0)\n  const double = computed(() => count.value * 2)\n  function increment() { count.value++ }\n  return { count, double, increment }\n}",
    tags: ["Vue3", "Composition API", "Options API"],
  },
  {
    id: "vue-03",
    title: "Vue 组件通信的多种方式",
    category: "vue",
    difficulty: 1,
    question: "Vue 中父子组件、兄弟组件、跨层级组件如何通信？",
    answer: 'Vue 组件通信方案的选择取决于层级关系，我给你梳理一下常用的。\n\n父子通信最标准：父传子用 props，子传父用 emit。props 应该单向数据流——父组件的数据通过 props 流到子组件，子组件不要直接改 props（会触发 warning），而是通过 emit 通知父组件来改。\n\n兄弟组件、跨层级通信可以用 provide/inject。provide 在祖先组件提供数据，inject 在任意后代组件获取。适合做"透传"——比如主题色、locale 这些全局配置，不需要经过每一层组件手动传 props。\n\n全局状态管理用 Pinia（Vue 3 官方推荐的替代 Vuex）。为什么不用 Vuex？Vuex 写起来太啰嗦了——state → mutations → actions → getters，一个简单的布尔值切换要写三处代码。Pinia 简洁得多，而且天然支持 Composition API、TypeScript、devtools，没有 mutations 的概念。\n\n还有一些特殊场景的通信方式。EventBus 在 Vue 3 里官方不再推荐——因为发布订阅没有类型保障，调试困难，容易造成"事件满天飞"。Vue 3 推荐用 mitt 库或者直接靠 Pinia 的状态驱动。v-model 本质也是 props + emit 的语法糖，适合表单类的双向绑定。$parent / $refs 适合直接访问子组件实例，但不推荐过度使用——破坏了组件的封装性。',
    codeExample: "// defineModel (Vue 3.4+)\n// 子组件\nconst model = defineModel()\n// <input v-model=\"model\" />\n\n// provide / inject\n// 祖先\nprovide('theme', ref('dark'))\n// 后代\nconst theme = inject('theme')\n\n// Pinia\nconst store = useCounterStore()\nstore.increment()",
    tags: ["Vue", "组件通信", "props", "emit", "provide", "Pinia"],
  },
  {
    id: "vue-04",
    title: "Vue Router 导航守卫",
    category: "vue",
    difficulty: 2,
    question: "Vue Router 的导航守卫有哪些？它们的执行顺序是怎样的？",
    answer: 'Vue Router 的导航守卫是做权限控制、数据预加载、页面离开确认的必备工具。\n\n守卫分三种。全局守卫：beforeEach（进入之前）、beforeResolve（解析完成之后、确认之前）、afterEach（进入之后）。最常用的是 beforeEach——一般用来做登录状态检查：如果用户没登录要去 /admin，直接重定向到 /login。\n\n路由级守卫：beforeEnter，在路由配置里定义，只对特定路由生效。适合某个路由页面的特殊权限要求。\n\n组件内守卫：beforeRouteEnter（组件创建前）、beforeRouteUpdate（路由参数变化但组件复用时）、beforeRouteLeave（离开前）。beforeRouteLeave 有个特别实用的场景：用户填了表单要离开，弹一个"你有未保存的内容，确定离开吗？"的确认框。\n\n守卫执行的完整流程：触发导航 → 失活组件的 beforeRouteLeave → 全局 beforeEach → 复用组件的 beforeRouteUpdate → 路由配置的 beforeEnter → 激活组件的 beforeRouteEnter → 全�� beforeResolve → 导航确认 → afterEach → DOM 更新 → beforeRouteEnter 的 next 回调执行。\n\n有个容易踩的坑：next() 函数的使用。Vue Router 4 里 next 不再强制要求——你可以不调 next，直接 return true/false 或返回一个路径来重定向。如果调了 next 但没传参数，导航会挂住。',
    codeExample: "// 全局守卫 - 权限检查\nrouter.beforeEach((to, from, next) => {\n  if (to.meta.requiresAuth && !isLoggedIn()) {\n    next('/login')\n  } else {\n    next()\n  }\n})\n\n// 组件内守卫\nbeforeRouteLeave((to, from, next) => {\n  if (hasChanges) {\n    const answer = confirm('有未保存的更改，确定离开？')\n    next(answer)\n  } else next()\n})",
    tags: ["Vue Router", "导航守卫", "路由"],
  },
  {
    id: "vue-05",
    title: "Vue 虚拟 DOM 与 Diff 算法",
    category: "vue",
    difficulty: 3,
    question: "请解释 Vue 的虚拟 DOM (Virtual DOM) 原理和 Diff 算法。",
    answer: '虚拟 DOM 和 Diff 算法的核心价值是：用 JS 对象的比较代价换取真实 DOM 操作的最小化。\n\n虚拟 DOM 就是把真实 DOM 树的每个节点用 JS 对象表示——{ tag, props, children }。模板变了先生成新的虚拟 DOM 树，然后和旧的树做 diff 比较，找出最小的变化集合，最后批量更新真实 DOM。\n\nVue 的 Diff 有几个关键优化。同层比较：只比较同一层级的节点，不跨层级比较。如果节点类型变了（div 变 span），直接销毁重建，不递归比较子节点。双端比较：新旧子节点数组各维护两个指针（头尾），从两端往中间比。用 key 来识别节点是否可以复用——不依赖 key 的话只能按索引位置匹配。\n\nVue 3 在此基础上加了更多编译时优化：静态标记（patch flag）标注哪些部分是动态的，diff 时只比动态节点；Block Tree 把动态节点单独收集，diff 只比这个数组；静态提升把不变的部分提到 render 函数外面，每次渲染复用。\n\n面试官追问：Vue 和 React 的 diff 有什么区别？React 是单向链表 diff（从头到尾逐个比），Vue 用的是双端比较（头尾同时推进）。双端在某些场景更快——比如列表只是头部插了一个元素，Vue 能很快发现尾部都匹配，React 需要移动所有节点。但 React Fiber 通过时间切片和优先级队列在调度上更灵活。',
    tags: ["Vue", "Virtual DOM", "Diff", "PatchFlags"],
  },
  {
    id: "vue-06",
    title: "Pinia 与 Vuex 对比",
    category: "vue",
    difficulty: 2,
    question: "Pinia 相比 Vuex 有哪些优势？Pinia 的核心概念是什么？",
    answer: 'Pinia 和 Vuex 都是 Vue 的状态管理库，但 Pinia 是 Vue 3 时代的官方推荐，我觉得它确实比 Vuex 好用太多了。\n\n先说 Pinia 的优势。第一，书写简洁。Vuex 要定义 state、mutations、actions、getters 四个概念，一个简单的计数器要写一堆模板代码。Pinia 只需要 state + actions（mutation 和 action 合并成 action），代码量少一半。第二，TypeScript 支持——Vuex 的 TypeScript 类型推断一直是个痛点，Pinia 从一开始就为 TS 设计。第三，去掉了 mutations 的概念——可以直接在 actions 里改 state（同步），也可以写异步操作，概念更简单。第四，模块化做得好——通过导出的 defineStore 自然地分割，不需要 Vuex 的 modules 嵌套写法。\n\n在 Vue 2 迁移到 Vue 3 的项目里，Pinia 可以替代 Vuex 同时和 Options API 兼容——你不用 Composition API 也能用 Pinia。\n\n什么时候用 Pinia，什么时候用 Provide/Inject？我的经验：全局的、跨多个页面共享的数据用 Pinia（用户信息、权限、主题、购物车）。局部的、只在某个模块内共享的数据用 Provide/Inject（表单上下文、列表筛选条件）。不要把什么状态都往 Pinia 里塞——它应该是全局状态的集中管理，不是所有状态的仓库。',
    codeExample: "// Setup Store 风格（推荐）\nexport const useUserStore = defineStore('user', () => {\n  const user = ref<User | null>(null)\n  const isLoggedIn = computed(() => !!user.value)\n\n  async function login(credentials: Credentials) {\n    const res = await api.login(credentials)\n    user.value = res.data\n  }\n\n  function logout() { user.value = null }\n\n  return { user, isLoggedIn, login, logout }\n})",
    tags: ["Pinia", "Vuex", "状态管理"],
  },
  {
    id: "vue-07",
    title: "Vue 生命周期详解",
    category: "vue",
    difficulty: 1,
    question: "Vue 3 的生命周期钩子有哪些？与 Vue 2 有什么区别？",
    answer: 'Vue 的生命周期我分三个阶段来说：创建、挂载、更新、销毁。\n\n创建阶段（只执行一次）：setup() 或 created 里可以访问响应式数据和函数，但还没有 DOM。这个阶段适合做数据初始化——比如调 API 获取数据填充到 ref 里。\n\n挂载阶段：beforeMount 的时候模板已经编译成 render 函数了，但还没有真实 DOM。onMounted 时组件已经挂载到页面上了，可以访问 DOM 节点、操作 Canvas、初始化第三方库。大部分异步数据请求放在这里。\n\n更新阶段：数据变化导致组件重新渲染。beforeUpdate 时数据已经变了但 DOM 还没更新。onUpdated 时 DOM 已经更新完毕。但注意不要在 updated 里改数据——会导致死循环。如果需要在数据变化后做操作，用 watch 或 computed 更合适。\n\n销毁阶段：onBeforeUnmount 里做清理——清除定时器、取消请求、解绑事件、关闭 WebSocket。这一步很多人忘记，导致内存泄漏。onUnmounted 时组件已经完全销毁了，可以做一些善后日志记录。\n\nVue 3 里生命周期钩子的命名在 Composition API 下变成了 onXxx 格式（onMounted、onUpdated 等），并且没⋯有了 beforeCreate 和 created——setup 本身就替代了它们。\n\n实际项目里最容易被忽略的是 onActivated 和 onDeactivated——这两个是配合 keep-alive 使用的。缓存的组件再次激活时走 onActivated，离开缓存时走 onDeactivated，不走 mount/unmount。',
    codeExample: "// Composition API\nimport { onMounted, onUnmounted } from 'vue'\n\nonMounted(() => {\n  console.log('组件已挂载，可访问 DOM')\n  window.addEventListener('resize', handleResize)\n})\n\nonUnmounted(() => {\n  console.log('组件已卸载，清理资源')\n  window.removeEventListener('resize', handleResize)\n})",
    tags: ["Vue", "生命周期", "mounted", "unmounted"],
  },
  {
    id: "vue-08",
    title: "v-if 与 v-show 的区别",
    category: "vue",
    difficulty: 1,
    question: "v-if 和 v-show 的区别是什么？各自适合什么场景？",
    answer: 'v-if 和 v-show 的选择看起来简单，但选错了浪费的性能不小。\n\nv-if 是"真条件渲染"。它为 false 的时候，元素不会被渲染到 DOM 里，组件也不会被创建，事件不会被绑定——整个都不存在。切换成本高，因为它涉及销毁和重建。适合运行中很少切换状态的场景——比如用户是否登录、权限判断、界面模式切换。\n\nv-show 只是改 CSS 的 display 属性。不管状态是什么，元素始终在 DOM 里存在，只是 display:none 的时候看不见。初始渲染成本高（无论如何都要渲染），但切换只是改一个 CSS 属性，几乎零成本。适合频繁切换的场景——比如 Tab 切换、展开折叠、加载中/加载完成。\n\n组合使用的技巧：对于首屏不显示但可能频繁切换的元素，先 v-if 让它不渲染（省掉初始渲染成本），等第一次需要显示的时候换成 v-show（享受后续零切换成本）。这个技巧在大型列表和复杂组件里效果很明显。\n\n性能选择口诀："一次切换 v-if，频繁切换 v-show"。Tab 页面切换——只要不是十几个 Tab，用 v-show 就行；侧边栏展开折叠——v-show。权限判断——比如"管理员才看到的按钮"，这种整个生命周期基本不变的，用 v-if。',
    tags: ["Vue", "v-if", "v-show", "条件渲染"],
  },
  {
    id: "vue-09",
    title: "Vue 中 key 的作用与原理",
    category: "vue",
    difficulty: 2,
    question: "Vue 中 key 的作用是什么？为什么不能用 index 作为 key？",
    answer: 'key 在 Vue 里不只是 for 循环里的那个 :key 了，它是最重要的 diff 优化手段。\n\nkey 的核心作用是给每个虚拟 DOM 节点一个唯一标识。Diff 算法在对比新旧节点数组的时候，先按 key 找可复用的节点——key 相同的节点如果能复用就直接移动位置而不是销毁重建。没有 key 就只能按索引位置匹配——列表里插入一个元素，后面的所有元素索引全变了，Vue 可能把它们都重建一遍。\n\nkey 不设或者设 index 是同一个效果——列表变化时索引不可靠，diff 效率低还可能出 bug。比如一个待办列表，每个待办项是一个可编辑的组件——删除第一个待办，如果没有用唯一 ID 做 key，Vue 会把第一个位置的旧组件"改成"第二个位置的内容，而不是销毁第一个、移动后面的。\n\nkey 还能用在条件切换上。两个不同 key 的同类型元素，Vue 会视为不同元素——这会强制触发完整的组件销毁和重建。有时反而是你需要的——比如一个表单切换内容模板，换 key 可以保证表单状态被重置。\n\n最佳实践是：总是给 v-for 提供基于数据唯一 ID（数据库的主键 ID）的 key。如果没有唯一 ID，用数据自身的组合（比如 name + timestamp）。绝对不要用 index——这是 Vue 官方文档明确不推荐的。',
    codeExample: "// ❌ 用 index 做 key\n<li v-for=\"(item, index) in list\" :key=\"index\">\n  {{ item.name }}\n</li>\n\n// ✅ 用唯一 id 做 key\n<li v-for=\"item in list\" :key=\"item.id\">\n  {{ item.name }}\n</li>\n\n// 强制替换组件\n<Comp :key=\"forceUpdateKey\" />",
    tags: ["Vue", "key", "Diff", "列表渲染"],
  },
  {
    id: "vue-10",
    title: "ref vs reactive 的区别",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 中 ref 和 reactive 有什么区别？各自的使用场景？",
    answer: 'ref 和 reactive 都是创建响应式数据的方法，但使用场景和原理有区别。我刚学 Vue 3 的时候也被这两个搞混过。\n\nref 可以包装任何类型的值，包括基本类型和对象。内部原理是：ref 创建的是一个带有 .value 属性的对象，通过 getter/setter 实现响应式。当 ref 包装对象时，内部其实是用 reactive 来包那个对象。所以在模板里，ref 会被自动解包——不需要写 .value；但在 JS 代码里，你必须 ref.value。\n\nreactive 只能包装对象类型（对象、数组、Map、Set 等），不能包装基本类型。内部用 Proxy 实现。reactive 不能整体替换——如果你把一个 reactive 对象整个赋值成一个新对象，它就丢了响应式。这也是我几乎只用 ref 的原因——ref 可以 .value = newObj 直接替换。\n\nref 的 .value 刚开始写确实不习惯，但 script setup 配合 Volar 的自动解引用让模板里不需要 .value，代码里用的也不算多。而且 ref 做函数参数传递的时候更安全——它是引用一个包裹对象，而 reactive 直接暴露对象本身，解构就丢响应式。\n\n实际使用建议：个人偏好是几乎全用 ref（模板自动解包、可以整体替换、函数传递安全），reactive 只在处理表单对象这种天生就是对象结构的场景用。如果遇到解构 reactive 丢响应式的问题，可以用 toRefs 解决。',
    codeExample: "import { ref, reactive, toRefs } from 'vue'\n\n// ref\nconst count = ref(0)\ncount.value++\n\n// reactive\nconst state = reactive({ name: 'Alice', age: 25 })\nstate.age++  // 直接访问\n\n// reactive 解构问题\nconst { name, age } = toRefs(state)  // 保持响应\n\n// reactive 重新赋值问题\nlet obj = reactive({ x: 1 })\nobj = reactive({ x: 2 })  // ❌ 丢失响应\n// ✅ 用 ref 代替\nconst obj = ref({ x: 1 })\nobj.value = { x: 2 }  // 仍响应",
    tags: ["Vue3", "ref", "reactive", "响应式"],
  },
  {
    id: "vue-11",
    title: "computed 与 watch / watchEffect 区别",
    category: "vue",
    difficulty: 2,
    question: "computed、watch 和 watchEffect 的区别是什么？各自的适用场景？",
    answer: 'computed、watch、watchEffect 三者都是"依赖变化后做某事"，但触发机制和用途不同。\n\ncomputed 是声明式的派生值——"这个值是从其他值推算出来的"。有缓存，只有当它依赖的值变了才会重新计算。适合模板里需要的派生数据——比如 fullName = firstName + \' \' + lastName，或者把数组过滤排序后展示。\n\nwatch 是命令式的副作用——"当某个值变了，执行这个函数"。需要显式声明监听哪些数据源。watch 给你新值和旧值，适合需要对比前后值的场景——比如数据变了打 log、触发保存、同步到 localStorage。\n\nwatchEffect 也是副作用，但它自动追踪——不需要声明依赖，回调里用到了什么就追什么。适合"数据变了就重跑这个逻辑"的场景——比如根据筛选条件自动请求数据。它立即执行（computed 延迟求值），没有旧值。\n\n我的使用经验：模板里派生的用 computed（性能最好，有缓存），需要新/旧值对比或用条件判断是否触发的用 watch，单纯"数据变了就做某件事"且不想费心声明依赖的用 watchEffect。\n\n面试官追问：computed 内部是怎么实现缓存和脏检查的？它内部维护了一个 dirty 标记——依赖变了 mark dirty，下次读取时如果 dirty 就重新计算并清 dirty，不脏就直接返回缓存值。这也是它比 method 高效的原因。',
    codeExample: "// computed\nconst fullName = computed(() => `${first.value} ${last.value}`)\n\n// watch\nwatch(userId, async (newId, oldId) => {\n  const data = await fetchUser(newId)\n  userData.value = data\n}, { immediate: true })\n\n// watchEffect\nconst stop = watchEffect(() => {\n  console.log(route.params.id)  // 自动追踪 route.params.id\n  fetchDetail(route.params.id)\n})\nstop()  // 停止监听",
    tags: ["Vue3", "computed", "watch", "watchEffect"],
  },
  {
    id: "vue-12",
    title: "keep-alive 组件详解",
    category: "vue",
    difficulty: 2,
    question: "keep-alive 的作用和原理是什么？如何控制缓存的组件？",
    answer: 'keep-alive 是一个内置组件，用于缓存不活动的组件实例而不是销毁它们。在切换组件时保持它们的状态，避免重新渲染。\n\n最常见的场景：Tab 切换——用户在一个 Tab 里滚到第 50 条数据，切换到另一个 Tab 再切回来，如果没 keep-alive，组件被重建了，滚动位置和数据状态全丢。keep-alive 让组件实例被缓存，切回来的时候跟离开时一模一样。\n\nkeep-alive 提供了 include/exclude 属性来精确控制哪些组件缓存哪些不缓存（用组件 name 匹配），还有 max 属性限制最大缓存数量（LRU 淘汰——最近最少使用的组件先被销毁）。\n\n配合 keep-alive 有两个特殊的生命周期：activated（组件从缓存中激活时调用）和 deactivated（组件被缓存时调用）。这俩在数据刷新场景很常用——比如 Tab 切回来的时候需要拉一下最新数据，就在 activated 里发请求。\n\n性能注意事项：缓存的组件会一直占用内存——如果缓存的组件内部有大列表、大图片或者 WebSocket 连接，这些都活着。所以别什么都 keep-alive，只有确实需要保持状态的组件才缓存。内存敏感的场景（移动端 H5）要特别注意。',
    codeExample: "<!-- 缓存路由组件 -->\n<router-view v-slot=\"{ Component }\">\n  <keep-alive :include=\"['UserList', 'Settings']\" :max=\"10\">\n    <component :is=\"Component\" />\n  </keep-alive>\n</router-view>\n\n<!-- 组件内监听激活 -->\nonActivated(() => {\n  refreshData()  // 每次激活时刷新\n})",
    tags: ["Vue", "keep-alive", "缓存", "activated"],
  },
  {
    id: "vue-13",
    title: "Vue 3 Teleport 与 Suspense",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 中 Teleport 和 Suspense 的作用和使用场景？",
    answer: 'Teleport 和 Suspense 是 Vue 3 新增的两个组件，解决的问题都很实际。\n\nTeleport 让你把组件的内容"传送"到 DOM 的别处去渲染——比如模态框、通知、下拉菜单需要渲染到 body 下面（避免被父组件的 overflow: hidden 或 z-index 限制）。以前的做法是自己用原生 JS 操作 DOM 插入——能跑但和 Vue 的响应式系统割裂。Teleport 在 Vue 的虚拟 DOM 内管理这些"不在原位渲染"的元素，逻辑和样式都正常工作。\n\n我用 Teleport 最多的场景是全局弹窗系统——弹窗内容写在触发它的组件里，渲染到 body 末尾。省去了之前创建 portal 组件的繁琐工作。\n\nSuspense 是异步组件加载的中间状态处理。异步组件还没加载完的时候，Suspense 的 fallback 插槽内容会显示（比如骨架屏、loading）。加载完自动切换到真正的内容。\n\n在实际项目中 Suspense 目前用得不多——主要是 Vue 3 的异步组件配合 defineAsyncComponent 已经够用了，而且 Next.js 里 Suspense 的场景在 Vue 里天然不太存在（Vue SSR 本身就是同步等待异步数据的）。但随着流式渲染的发展，Suspense 的作用会越来越大。',
    codeExample: "<!-- Teleport -->\n<Teleport to=\"body\">\n  <div class=\"modal\">\n    <h2>弹窗标题</h2>\n    <slot />\n  </div>\n</Teleport>\n\n<!-- Suspense -->\n<Suspense>\n  <template #default>\n    <AsyncComponent />\n  </template>\n  <template #fallback>\n    <LoadingSpinner />\n  </template>\n</Suspense>\n\n// 异步组件\nconst AsyncComp = defineAsyncComponent(() =>\n  import('./HeavyComponent.vue')\n)",
    tags: ["Vue3", "Teleport", "Suspense", "异步组件"],
  },
  {
    id: "vue-14",
    title: "Vue 编译优化：静态提升与标记",
    category: "vue",
    difficulty: 3,
    question: "Vue 3 编译器做了哪些优化来提升运行时性能？",
    answer: 'Vue 3 的编译器进行的优化远比大多数人意识到的要多。这些编译时优化是 Vue 3 性能大幅超越 Vue 2 的关键。\n\n静态提升（Static Hoisting）：编译器在编译 template 时，如果发现一个元素完全没有动态绑定（没有 {{}}、没有 v-if、没有 v-for），就把它提升到 render 函数外面成为常量。这样每次组件重渲染的时候，这个静态节点的虚拟 DOM 不会被重新创建，直接复用。\n\nPatch Flag（补丁标记）：对于有动态绑定的元素，编译器打上标记——"这个元素只有 text 是动态的"（TEXT=1）、"只有 class 是动态的"（CLASS=2）、"只有 props 是动态的"（PROPS=8）。运行时 diff 的时候，Vue 跳过所有没有 patch flag 的节点，只对有 flag 的节点做检查。不知道哪些会变 = React（全量 diff 再优化），知道哪些会变 = Vue（编译时精准标注）。\n\nBlock Tree：把动态节点单独收集到一个数组里，diff 的时候只比这个数组里的节点，跳过了所有静态节点。大模板里效果尤其显著。\n\n这些优化的本质思想是：能在编译时知道的事情就不要放到运行时去做。React 选择了灵活的运行时，Vue 选择了精确的编译时——两种取舍，没有对错。但在性能 benchmark 上，Vue 3 确实领先不少。',
    codeExample: "// 编译前\n<template>\n  <div>\n    <h1>静态标题</h1>  <!-- 静态提升 -->\n    <p>{{ msg }}</p>     <!-- PatchFlag: TEXT -->\n    <span :class=\"cls\">{{ text }}</span>  <!-- PatchFlag: CLASS | TEXT -->\n  </div>\n</template>\n\n// 编译后（简化）\nconst hoisted = _createElementVNode('h1', null, '静态标题', -1)\n\nfunction render() {\n  return _createElementBlock('div', null, [\n    hoisted,  // 复用静态节点\n    _createVNode('p', null, _toDisplayString(msg), 1 /* TEXT */),\n    _createVNode('span', { class: cls }, _toDisplayString(text), 3 /* TEXT | CLASS */)\n  ])\n}",
    tags: ["Vue3", "编译优化", "PatchFlags", "静态提升"],
  },
  {
    id: "vue-15",
    title: "Vue 3 自定义指令",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 如何自定义指令？与 Vue 2 的钩子函数有什么区别？",
    answer: '自定义指令是 Vue 提供的操作底层 DOM 的 API，当组件无法满足需求时使用。最常见的是 v-focus（自动聚焦）、v-click-outside（点击外部区域关闭）、v-lazy（图片懒加载）。\n\n自定义指令的生命周期钩子和组件类似：created（元素创建但尚未插入 DOM）、beforeMount、mounted（可以访问 DOM）、beforeUpdate、updated、beforeUnmount、unmounted。在 Vue 3 里，指令钩子名称和组件生命周期统一了（Vue 2 时叫 bind、inserted 等，很混乱）。\n\n实际使用场景：v-focus 在对话框和表单里自动聚焦输入框——v-focus（自定义）和 autofocus（原生）的区别是前者可以条件触发。v-click-outside 用于下拉菜单、弹出框——监听 document 的点击事件，如果点击发生在绑定元素之外就触发回调。v-lazy 做图片懒加载——利用 IntersectionObserver 在图片进入视口时替换 src。\n\n开发时需要注意：指令里的 DOM 操作要避免和 Vue 的响应式更新冲突——不要在指令里直接修改 Vue 管理的数据。还有就是在 unmounted 钩子里一定要清理事件监听和 observer，不然内存泄漏。',
    codeExample: "// 自定义 v-focus 指令\nconst vFocus = {\n  mounted(el: HTMLInputElement) {\n    el.focus()\n  }\n}\n\n// 自定义 v-permission\nconst vPermission = {\n  mounted(el, binding) {\n    const role = getUserRole()\n    if (!binding.value.includes(role)) {\n      el.parentNode?.removeChild(el)\n    }\n  }\n}\n\n// 使用\n<input v-focus />\n<button v-permission=\"['admin']\">删除</button>",
    tags: ["Vue3", "自定义指令", "directive"],
  },
  {
    id: "vue-16",
    title: "Vue SSR 与 Nuxt.js",
    category: "vue",
    difficulty: 3,
    question: "什么是 SSR？Vue SSR 的原理和优缺点？Nuxt.js 带来了什么？",
    answer: 'Vue SSR（服务端渲染）的核心价值就两个：SEO 和首屏速度。\n\n传统 SPA 的问题是：服务器返回的是空 HTML + JS bundle，浏览器要等 JS 加载完、Vue 应用启动、数据请求完成才开始渲染。这段时间用户看到的是白屏。Googlebot 有时候能爬 SPA，但效果不稳定，百度等其他搜索引擎基本不行。\n\nSSR 的流程是：服务端把 Vue 组件渲染成完整的 HTML 字符串，和 JS bundle 一起发给浏览器。浏览器先显示服务端渲染好的 HTML（用户立刻看到内容），然后 JS 加载完后再"激活"（hydration）——让静态 HTML 变成可交互的 Vue 应用。\n\nNuxt.js 是 Vue 生态里的 SSR 框架（就像 Next.js 之于 React）。它提供了文件路由系统、SSR/SSG/CSR 灵活切换、模块系统、自动化优化等能力。\n\n我用 Nuxt 做过我们游戏官网——新闻和文章页用 SSR（SEO 友好），用户个人设置页用 CSR（纯客户端交互）。Nuxt 的 hybrid rendering 支持路由级别的渲染模式选择。\n\nSSR 的代价也不小：服务端多了渲染的 CPU 开销、开发时要注意"服务端和客户端不一致"的问题（比如服务端没有 window 对象、localStorage 不可用）、部署复杂度增加。如果不是强 SEO 需求或首屏性能要求极高的页面，CSR 完全够用。',
    tags: ["Vue", "SSR", "Nuxt", "服务端渲染"],
  },
  {
    id: "vue-17",
    title: "nextTick 原理与应用",
    category: "vue",
    difficulty: 2,
    question: "Vue 中 nextTick 的作用是什么？原理是什么？",
    answer: 'nextTick 是 Vue 里处理"等 DOM 更新完再做事"的标准方案。\n\nVue 的响应式更新不是同步的——你在同一个事件循环里改了 10 个 ref，Vue 不会触发 10 次组件更新。它会把这些更新收集到队列里，在当前同步代码执行完之后，在微任务里统一执行一次 DOM 更新。这个机制叫"异步更新队列"。\n\nnextTick 就是让你在 DOM 更新完成后执行回调。实际场景我常用在：动态生成的内容需要滚动到底部（聊天消息列表）、更新数据后需要获取更新后的 DOM 尺寸（计算元素高度做动画）、更新 ref 后需要立即让子组件执行某个方法。\n\n内部实现上，Vue 优先用 Promise.then（微任务），降级方案依次是 MutationObserver、setImmediate（Node）、setTimeout。这也是为什么 nextTick 回调在 DOM 更新之后但在下一次宏任务之前执行。\n\n一个小技巧：nextTick 返回 Promise，你可以 await nextTick() 来实现同步风格的"等 DOM 更新"。\n\n面试官追问：不用 nextTick 会咋样？如果你在数据更新后立即读 DOM 属性，读到的是旧值——因为同步代码里的 DOM 还没更新。常见翻车场景是改了一个列表数据，然后立刻读列表容器的 scrollHeight——读到的是更新前的值，动画或者滚动定位就跑偏了。',
    codeExample: "// 数据更新后获取 DOM\nconst count = ref(0)\nfunction increment() {\n  count.value++\n  await nextTick()\n  console.log(document.querySelector('.count').textContent)\n}\n\n// Vue 3 nextTick 返回 Promise\nimport { nextTick } from 'vue'\nawait nextTick()\n\n// 内部简化实现\nconst queue = []\nlet pending = false\nfunction nextTick(cb) {\n  return Promise.resolve().then(cb)\n}",
    tags: ["Vue", "nextTick", "异步更新", "DOM"],
  },
  {
    id: "vue-18",
    title: "Vue mixin 的应用与局限",
    category: "vue",
    difficulty: 2,
    question: "Vue 中 mixin 有什么用途？有什么问题和替代方案？",
    answer: 'Mixin 是 Vue 2 时代逻辑复用的主要手段，但用多了问题就来了。\n\nMixin 的问题：命名冲突——多个 Mixin 和组件自身的属性如果有重名，你想搞清楚"这个变量到底是哪里来的"得翻好几个文件。来源不透明——组件里用了一个 data 属性，你不知道它是组件自身的还是 Mixin 注入的。Mixin 之间还可能互相依赖——Mixin A 里用了 Mixin B 提供的属性，形成了隐蔽的耦合。\n\n我在一个老项目里吃过亏：一个组件 mixin 了 5 个 Mixin，里面有个 computed 属性的名字不小心和另一个 Mixin 里的冲突了，花了一个下午 debug——因为控制台没有任何报错，只有数据表现异常。\n\nVue 3 的 Composition API 解决了这些问题。Composable 函数是显式 import + 显式解构——变量从哪里来的，代码里一目了然。命名冲突？TypeScript 会告警（重复声明）。而且 Composable 之间通过函数调用组合，不是隐式注入，耦合关系更清晰。\n\n迁移建议：新项目直接全部 Composable，老项目逐步抽——先把公用的 Mixin 转成 Composable，业务组件不需要一次性全改。',
    codeExample: "// mixin 方式\nconst searchMixin = {\n  data() { return { keyword: '' } },\n  methods: { search() { /* ... */ } }\n}\n\n// 替代 - Composition API\nfunction useSearch() {\n  const keyword = ref('')\n  const results = ref([])\n  watch(keyword, async (kw) => {\n    results.value = await fetchSearch(kw)\n  })\n  return { keyword, results }\n}",
    tags: ["Vue", "mixin", "逻辑复用", "Composition API"],
  },
  {
    id: "vue-19",
    title: "插槽 (Slot) 详解：默认/具名/作用域",
    category: "vue",
    difficulty: 2,
    question: "Vue 中插槽有哪几种类型？各自的使用场景？",
    answer: '插槽（Slot）是 Vue 里实现组件内容分发的机制，有三个层级。\n\n默认插槽最简单：父组件在子组件标签里写的内容，渲染在子组件的 <slot /> 位置。比如一个 Card 组件，标题写在 prop 里，正文写在 slot 里。\n\n具名插槽让你给多个插槽命名，父组件用 v-slot:name（或 #name 缩写）指定内容给哪个插槽。一个典型的布局组件：header、sidebar、main、footer 四个具名插槽，父组件可以灵活组合。\n\n作用域插槽是最强大的——子组件把自己的数据通过 slot props 暴露给父组件。父组件在插槽内容里用 v-slot="slotProps"（或解构）获取数据。使用场景：一个 List 组件负责数据获取和渲染逻辑（加载、空状态、错误处理），但每个列表项的 UI 由父组件控制。子组件把每个 item 通过 slot props 传给父组件，父组件决定怎么展示。\n\n实际项目里，作用域插槽在组件库设计和表格渲染里特别有用。比如我们 UI 组件库的 Table 组件，默认单元格直接显示文本，但如果调用方想自定义某个列的渲染（比如显示图片、加链接），通过列的作用域插槽就行。\n\n和 React 的 render props 类似，但写法更自然——直接在 template 里用 v-slot 而不是传函数。',
    codeExample: "<!-- 子组件 List -->\n<template>\n  <div v-for=\"(item, index) in items\" :key=\"item.id\">\n    <slot name=\"item\" :item=\"item\" :index=\"index\">\n      {{ item.name }}  <!-- 默认 -->\n    </slot>\n  </div>\n</template>\n\n<!-- 父组件使用 -->\n<List :items=\"users\">\n  <template #item=\"{ item, index }\">\n    <div class=\"user-card\">\n      <span>{{ index + 1 }}. {{ item.name }}</span>\n    </div>\n  </template>\n</List>",
    tags: ["Vue", "slot", "插槽", "作用域插槽"],
  },
  {
    id: "vue-20",
    title: "Vue 修饰符 (Modifiers) 大全",
    category: "vue",
    difficulty: 1,
    question: "Vue 中有哪些常用修饰符？各自的作用？",
    answer: 'Vue 修饰符是一类特殊的后缀，给指令添加额外行为。分几类说。\n\n表单修饰符（v-model）：.lazy 延迟到 change 事件才更新（而非 input 事件）——适合输入完再校验而非打一个字校验一次。.number 自动转数字——用户输入 "42" 拿到 42 而不是 "42"。.trim 自动去首尾空格——用户名和邮箱输入几乎必加。\n\n事件修饰符：.stop 阻止冒泡（e.stopPropagation()）、.prevent 阻止默认行为（e.preventDefault()）、.once 只触发一次、.capture 使用捕获模式、.self 只在事件源是自身时触发（防止子元素事件冒泡上来）。\n\n键盘修饰符：@keyup.enter 只在按下回车时触发、.tab、.delete、.esc 等。系统修饰符 .ctrl、.alt、.shift、.meta，可以组合：@keydown.ctrl.enter = Ctrl+Enter。\n\n鼠标修饰符：.left、.right、.middle。.exact 配合系统键使用——@click.ctrl.exact 只在"只按了 Ctrl"的时候触发（Ctrl+其他键不触发）。\n\n实际使用上，修饰符比在 handler 里手写 if 判断干净多了。而且修饰符可以链式组合——@click.stop.prevent 既阻止冒泡又阻止默认行为，读起来也直观：点击、停下来、阻止默认。',
    codeExample: "<!-- 事件修饰符 -->\n<button @click.stop.prevent=\"submit\">提交</button>\n<form @submit.prevent=\"onSubmit\">\n<div @click.self=\"close\">\n\n<!-- 按键修饰符 -->\n<input @keyup.enter=\"search\" />\n<input @keyup.ctrl.s=\"save\" />\n\n<!-- v-model 修饰符 -->\n<input v-model.lazy=\"msg\" />\n<input v-model.number=\"age\" />\n<input v-model.trim=\"name\" />",
    tags: ["Vue", "修饰符", "事件", "v-model"],
  },
  {
    id: "vue-21",
    title: "Vue 权限管理方案",
    category: "vue",
    difficulty: 2,
    question: "Vue 项目中如何实现权限管理？包括路由级别和按钮级别。",
    answer: 'Vue 前端权限管理这块，我在几个后台管理系统里实践过，总结了一套成熟的方案。\n\n权限分三层：路由权限、按钮权限、数据权限。\n\n路由权限是最基础的。在路由配置里给每个路由加 meta: { roles: [\'admin\', \'editor\'] }，然后在全局导航守卫 beforeEach 里检查用户角色和路由所需角色的交集。没权限就跳 403 页面。动态路由的方案更灵活——根据后端返回的权限列表，用 router.addRoute 动态注册路由。\n\n按钮权限让你控制页面上特定操作按钮的显隐。一个自定义指令 v-permission="[\'admin\']" 就能搞定——在指令的 mounted 钩子里检查权限，没权限就直接 remove 掉这个元素。也可以用封装成 auth 函数在模板里用 v-if 判断。\n\n数据权限是控制用户能看到哪些数据。比如"普通用户只能看自己团队的订单，经理能看自己部门的，管理员全看"。这种前端做不了主体控制，靠后端返回有权限的数据。前端负责的是在请求里带上正确的参数，以及不同权限级别的 UI 差异展示。\n\n还有一个容易被忽略的点：权限不等于安全。前端权限控制只是用户体验优化——让用户看不到没权限的东西。真正的安全控制在后端——即使前端把按钮隐藏了，有心之人可以直接调 API。前端权限是"门帘"，后端权限是"门锁"。',
    codeExample: "// 路由守卫\nrouter.beforeEach((to, from, next) => {\n  const user = useUserStore()\n  if (to.meta.roles && !to.meta.roles.includes(user.role)) {\n    next('/403')\n  } else next()\n})\n\n// 自定义指令\nconst vPermission = {\n  mounted(el, binding) {\n    const perms = useUserStore().permissions\n    if (!perms.includes(binding.value)) {\n      el.parentNode?.removeChild(el)\n    }\n  }\n}\n<button v-permission=\"'user:edit'\">编辑</button>",
    tags: ["Vue", "权限", "路由守卫", "指令"],
  },
  {
    id: "vue-22",
    title: "Vue 项目中 Axios 封装",
    category: "vue",
    difficulty: 1,
    question: "在 Vue 项目中如何封装 Axios？核心功能有哪些？",
    answer: 'Axios 封装是我每个 Vue 项目几乎都做的事情。核心就几块。\n\n请求拦截器：统一加 token、加公共请求头（如 Content-Type）、加请求 ID 用于追踪。一般从 localStorage 或 Pinia store 里读 token，如果有就自动挂 Authorization 头。\n\n响应拦截器：统一错误处理。401 → 跳登录页（token 过期）、403 → 提示"无权限"、500 → 提示"服务器错误"。还要做数据提取——后端可能返回 { code: 200, data: {...}, message: \'ok\' }，在拦截器里直接把 data 层提取出来，业务代码里就不用每次都 .then(res => res.data.data)。\n\n请求取消：用 Axios 的 AbortController 或旧的 CancelToken 实现。Tab 切换时取消上一个页面的未完成请求、重复请求自动取消上一次。这个在搜索输入和快速切换页面时很有用。\n\nLoading 管理：全局请求 loading 一般在拦截器里维护一个计数器——请求 +1，响应或失败 -1，计数器 > 0 就显示 loading bar（比如 NProgress）。\n\nBase URL 根据环境变量切换：开发环境指向本地代理、预发布指向 staging API、生产指向正式 API。\n\n面试官追问：为什么用 Axios 不用 fetch？fetch 不支持请求/响应拦截器（要用中间件模式模拟）、没有请求取消的原生机制（AbortController 可以但 API 不友好）、错误处理不如 Axios 直观（fetch 只有网络错误才 reject，404/500 仍算成功）。',
    codeExample: "// request.ts\nimport axios from 'axios'\n\nconst request = axios.create({\n  baseURL: import.meta.env.VITE_API_BASE,\n  timeout: 10000,\n})\n\nrequest.interceptors.request.use(config => {\n  const token = getToken()\n  if (token) config.headers.Authorization = `Bearer ${token}`\n  return config\n})\n\nrequest.interceptors.response.use(\n  res => res.data,\n  error => {\n    if (error.response?.status === 401) router.push('/login')\n    return Promise.reject(error)\n  }\n)\n\nexport default request",
    tags: ["Vue", "Axios", "拦截器", "封装"],
  }
]
