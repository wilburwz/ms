import type { KnowledgePoint } from '../types'

export const vueData: KnowledgePoint[] = [
  {
    id: "vue-01",
    title: "Vue 3 响应式原理：Proxy vs Object.defineProperty",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 使用 Proxy 替代 Object.defineProperty，带来了哪些改进？",
    answer: "Object.defineProperty 的局限（Vue 2）：\n- 无法检测对象属性的添加/删除（需用 Vue.set/delete）\n- 无法直接监听数组索引和 length 变化\n- 需要递归遍历所有属性，性能开销大\n- 初始化时就要劫持所有属性\n\nProxy 的优势（Vue 3）：\n- 可拦截 13 种操作（get, set, deleteProperty, has, ownKeys 等）\n- 天然支持数组操作（push, splice 等）\n- 支持 Map, Set, WeakMap, WeakSet\n- 惰性响应：只有访问到的属性才被追踪\n- 无需 Vue.set / Vue.delete\n\nReflect 配合：Proxy 的 handler 中使用 Reflect 保证 this 指向正确。",
    codeExample: "// Vue 3 简化版 reactive\nfunction reactive<T extends object>(obj: T): T {\n  return new Proxy(obj, {\n    get(target, key, receiver) {\n      track(target, key)  // 依赖收集\n      const result = Reflect.get(target, key, receiver)\n      if (typeof result === 'object' && result !== null) {\n        return reactive(result)  // 惰性深层响应\n      }\n      return result\n    },\n    set(target, key, value, receiver) {\n      const oldValue = target[key]\n      const result = Reflect.set(target, key, value, receiver)\n      if (oldValue !== value) {\n        trigger(target, key)  // 触发更新\n      }\n      return result\n    }\n  })\n}",
    tags: ["Vue3", "响应式", "Proxy", "reactive"],
  },
  {
    id: "vue-02",
    title: "Vue 3 Composition API 设计动机",
    category: "vue",
    difficulty: 2,
    question: "为什么 Vue 3 引入 Composition API？与 Options API 对比有什么优势？",
    answer: "Options API 的问题（Vue 2）：\n- 逻辑分散：同一功能代码分散在 data, methods, computed, watch 中\n- 逻辑复用困难：mixin 有命名冲突、来源不清晰问题\n- TypeScript 支持弱：this 的类型推导困难\n\nComposition API 的优势：\n- 逻辑聚合：相关代码组织在一起\n- 逻辑复用：通过可组合函数 (composables) 提取复用\n- 更好的类型推导：普通函数天然支持 TS\n- 更灵活的代码组织：按功能而非选项组织\n- Tree-shaking：未使用的 API 会被移除\n\n选择建议：\n- 小型组件用 Options API 也没问题\n- 复杂组件、需逻辑复用时用 Composition API\n- 两者可混用",
    codeExample: "// Options API - 逻辑分散\nexport default {\n  data() { return { count: 0, keyword: '' } },\n  computed: { double() { return this.count * 2 } },\n  methods: { increment() { this.count++ } },\n  watch: { keyword(v) { this.search(v) } }\n}\n\n// Composition API - 逻辑聚合\nimport { ref, computed, watch } from 'vue'\n\nfunction useCounter() {\n  const count = ref(0)\n  const double = computed(() => count.value * 2)\n  function increment() { count.value++ }\n  return { count, double, increment }\n}",
    tags: ["Vue3", "Composition API", "Options API"],
  },
  {
    id: "vue-03",
    title: "Vue 组件通信的多种方式",
    category: "vue",
    difficulty: 1,
    question: "Vue 中父子组件、兄弟组件、跨层级组件如何通信？",
    answer: "父子组件：\n- props（父→子）+ emit（子→父）\n- v-model 语法糖 / defineModel (Vue 3.4+)\n- ref + defineExpose：父组件通过 ref 调用子组件方法\n\n兄弟组件：\n- 通过共同父组件作为中转\n- EventBus（Vue 2）/ mitt 事件库（Vue 3 不再内置）\n\n跨层级：\n- provide / inject：祖先向后代注入依赖\n- Pinia / Vuex：全局状态管理\n- 路由参数（query / params）\n\n任意组件：\n- Pinia Store\n- 全局事件总线\n- 浏览器存储（localStorage）支持跨标签页通信",
    codeExample: "// defineModel (Vue 3.4+)\n// 子组件\nconst model = defineModel()\n// <input v-model=\"model\" />\n\n// provide / inject\n// 祖先\nprovide('theme', ref('dark'))\n// 后代\nconst theme = inject('theme')\n\n// Pinia\nconst store = useCounterStore()\nstore.increment()",
    tags: ["Vue", "组件通信", "props", "emit", "provide", "Pinia"],
  },
  {
    id: "vue-04",
    title: "Vue Router 导航守卫",
    category: "vue",
    difficulty: 2,
    question: "Vue Router 的导航守卫有哪些？它们的执行顺序是怎样的？",
    answer: "全局守卫：\n- beforeEach：进入前\n- beforeResolve：解析后（组件内守卫解析后）\n- afterEach：进入后（无 next）\n\n路由守卫：\n- beforeEnter：路由配置内，只对当前路由生效\n\n组件内守卫：\n- beforeRouteEnter：进入组件前（无法访问 this）\n- beforeRouteUpdate：路由参数变化时\n- beforeRouteLeave：离开前（可阻止）\n\n执行顺序：\n1. beforeEach（全局）\n2. beforeEnter（路由）\n3. beforeRouteEnter（组件）\n4. beforeResolve（全局）\n5. afterEach（全局）\n\n应用：权限校验、登录检查、页面标题更新、数据预取",
    codeExample: "// 全局守卫 - 权限检查\nrouter.beforeEach((to, from, next) => {\n  if (to.meta.requiresAuth && !isLoggedIn()) {\n    next('/login')\n  } else {\n    next()\n  }\n})\n\n// 组件内守卫\nbeforeRouteLeave((to, from, next) => {\n  if (hasChanges) {\n    const answer = confirm('有未保存的更改，确定离开？')\n    next(answer)\n  } else next()\n})",
    tags: ["Vue Router", "导航守卫", "路由"],
  },
  {
    id: "vue-05",
    title: "Vue 虚拟 DOM 与 Diff 算法",
    category: "vue",
    difficulty: 3,
    question: "请解释 Vue 的虚拟 DOM (Virtual DOM) 原理和 Diff 算法。",
    answer: "Virtual DOM：用 JS 对象描述真实 DOM 结构的轻量表示。\n\n为什么需要 VDOM：\n- 直接操作真实 DOM 开销大\n- VDOM 提供中间层，批量计算最小变更\n- 跨平台能力（可渲染到不同目标）\n\nVue 3 Diff 优化：\n- 静态标记 (PatchFlags)：编译时标记动态节点，跳过静态内容\n- Block Tree：将动态节点收集到 block 中，只 diff 动态部分\n- 最长递增子序列：优化相同顺序节点的移动，减少 DOM 操作\n- 靶向更新：精确知道哪些绑定需要更新\n\nDiff 核心流程：\n1. 头头、尾尾、头尾、尾头快速对比\n2. 中间乱序部分用 key 匹配\n3. 使用最长递增子序列最小化 DOM 移动",
    tags: ["Vue", "Virtual DOM", "Diff", "PatchFlags"],
  },
  {
    id: "vue-06",
    title: "Pinia 与 Vuex 对比",
    category: "vue",
    difficulty: 2,
    question: "Pinia 相比 Vuex 有哪些优势？Pinia 的核心概念是什么？",
    answer: "Pinia 优势：\n- 无 mutations（直接修改状态，内部转为 action）\n- TypeScript 完美支持（无需额外类型声明）\n- 无模块嵌套（扁平化设计）\n- 自动代码分割\n- DevTools 支持更好\n- 更轻量（~1KB）\n\n核心概念：\n- State：ref/reactive 定义的状态\n- Getters：computed 推导状态\n- Actions：async/await 异步 + 同步操作\n- Store：defineStore 定义\n\n用法模式：\n- Option Store：类似 Vuex（state, getters, actions）\n- Setup Store：类似 Composition API（ref, computed, 函数）",
    codeExample: "// Setup Store 风格（推荐）\nexport const useUserStore = defineStore('user', () => {\n  const user = ref<User | null>(null)\n  const isLoggedIn = computed(() => !!user.value)\n\n  async function login(credentials: Credentials) {\n    const res = await api.login(credentials)\n    user.value = res.data\n  }\n\n  function logout() { user.value = null }\n\n  return { user, isLoggedIn, login, logout }\n})",
    tags: ["Pinia", "Vuex", "状态管理"],
  },
  {
    id: "vue-07",
    title: "Vue 生命周期详解",
    category: "vue",
    difficulty: 1,
    question: "Vue 3 的生命周期钩子有哪些？与 Vue 2 有什么区别？",
    answer: "Vue 3 生命周期（Composition API）：\n- onBeforeMount：挂载前（DOM 未渲染）\n- onMounted：挂载后（可访问 DOM）\n- onBeforeUpdate：更新前\n- onUpdated：更新后\n- onBeforeUnmount：卸载前（清理资源）\n- onUnmounted：卸载后\n- onActivated / onDeactivated：keep-alive 激活/停用\n- onErrorCaptured：捕获后代组件错误\n\n与 Vue 2 区别：\n- destroyed → unmounted（语义更准确）\n- beforeDestroy → beforeUnmount\n- 新增 onRenderTracked / onRenderTriggered（调试用）\n\n执行时机：\n- setup() 相当于 beforeCreate + created\n- 父子挂载：子先 mounted，父后 mounted\n- 父子更新：父 beforeUpdate → 子 beforeUpdate → 子 updated → 父 updated",
    codeExample: "// Composition API\nimport { onMounted, onUnmounted } from 'vue'\n\nonMounted(() => {\n  console.log('组件已挂载，可访问 DOM')\n  window.addEventListener('resize', handleResize)\n})\n\nonUnmounted(() => {\n  console.log('组件已卸载，清理资源')\n  window.removeEventListener('resize', handleResize)\n})",
    tags: ["Vue", "生命周期", "mounted", "unmounted"],
  },
  {
    id: "vue-08",
    title: "v-if 与 v-show 的区别",
    category: "vue",
    difficulty: 1,
    question: "v-if 和 v-show 的区别是什么？各自适合什么场景？",
    answer: "v-if：\n- 条件为 false 时，DOM 元素完全不存在（真正的条件渲染）\n- 切换时会销毁/重建组件及其子组件\n- 有更高的切换开销\n- 适合条件不常变化的场景\n- 可配合 v-else / v-else-if\n\nv-show：\n- DOM 始终存在，通过 CSS display:none 控制显隐\n- 组件始终会被创建和保留\n- 有更高的初始渲染开销\n- 适合频繁切换的场景\n\n性能对比：\n- 初次渲染：v-if（lazy）更优，条件不满足时不会渲染\n- 频繁切换：v-show 更优，只切换 CSS 属性\n\n注意事项：\n- v-if 和 v-for 不要一起用（v-for 优先级更高）\n- v-show 不支持 template 元素",
    tags: ["Vue", "v-if", "v-show", "条件渲染"],
  },
  {
    id: "vue-09",
    title: "Vue 中 key 的作用与原理",
    category: "vue",
    difficulty: 2,
    question: "Vue 中 key 的作用是什么？为什么不能用 index 作为 key？",
    answer: "key 的作用：\n- 给 VNode 一个唯一标识，帮助 Diff 算法高效地识别节点\n- 复用 vs 重建：相同 key 的节点会被复用，不同 key 会销毁重建\n\n为什么不能用 index：\n1. 列表增删时，index 会变化导致节点错误复用\n2. 可能导致渲染 Bug（表单数据错位等）\n3. 无法正确触发过渡动画\n4. 性能更差（本应复用的节点被重建）\n\n正确做法：\n- 使用唯一且稳定的标识（如 id）\n- 静态列表可省略 key（使用 index 无妨）\n- 强制替换组件时可用 :key=\"Date.now()\"",
    codeExample: "// ❌ 用 index 做 key\n<li v-for=\"(item, index) in list\" :key=\"index\">\n  {{ item.name }}\n</li>\n\n// ✅ 用唯一 id 做 key\n<li v-for=\"item in list\" :key=\"item.id\">\n  {{ item.name }}\n</li>\n\n// 强制替换组件\n<Comp :key=\"forceUpdateKey\" />",
    tags: ["Vue", "key", "Diff", "列表渲染"],
  },
  {
    id: "vue-10",
    title: "ref vs reactive 的区别",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 中 ref 和 reactive 有什么区别？各自的使用场景？",
    answer: "ref：\n- 可包装任意类型的值（基本类型 + 引用类型）\n- 通过 .value 访问/修改\n- 在模板中自动解包（不需要 .value）\n- 重新赋值不会丢失响应性\n\nreactive：\n- 只能包装对象类型（Object, Array, Map, Set）\n- 直接访问属性，不需要 .value\n- 重新赋值会丢失响应性（整体替换时需注意）\n- 解构会丢失响应性（需用 toRefs）\n\n选择建议：\n- 基本类型：必须用 ref\n- 对象类型：两者皆可\n  - 需要重新赋值整个对象 → ref\n  - 只修改属性 → reactive\n- 推荐统一用 ref，避免混用增加心智负担",
    codeExample: "import { ref, reactive, toRefs } from 'vue'\n\n// ref\nconst count = ref(0)\ncount.value++\n\n// reactive\nconst state = reactive({ name: 'Alice', age: 25 })\nstate.age++  // 直接访问\n\n// reactive 解构问题\nconst { name, age } = toRefs(state)  // 保持响应\n\n// reactive 重新赋值问题\nlet obj = reactive({ x: 1 })\nobj = reactive({ x: 2 })  // ❌ 丢失响应\n// ✅ 用 ref 代替\nconst obj = ref({ x: 1 })\nobj.value = { x: 2 }  // 仍响应",
    tags: ["Vue3", "ref", "reactive", "响应式"],
  },
  {
    id: "vue-11",
    title: "computed 与 watch / watchEffect 区别",
    category: "vue",
    difficulty: 2,
    question: "computed、watch 和 watchEffect 的区别是什么？各自的适用场景？",
    answer: "computed：\n- 有返回值，必须有 return\n- 有缓存（依赖不变不重新计算）\n- 默认只读，可写（get + set）\n- 场景：根据已有数据派生新数据\n\nwatch：\n- 无返回值，执行副作用\n- 需要明确指定监听源\n- 支持 deep / immediate 选项\n- 可获取新旧值\n- 场景：数据变化时执行异步操作\n\nwatchEffect：\n- 自动追踪依赖（无需指定）\n- 立即执行一次（相当于 immediate: true）\n- 无法获取旧值\n- 返回 stop 函数可停止监听\n- 场景：依赖关系复杂时自动追踪\n\n优先级：能用 computed 就不用 watch",
    codeExample: "// computed\nconst fullName = computed(() => `${first.value} ${last.value}`)\n\n// watch\nwatch(userId, async (newId, oldId) => {\n  const data = await fetchUser(newId)\n  userData.value = data\n}, { immediate: true })\n\n// watchEffect\nconst stop = watchEffect(() => {\n  console.log(route.params.id)  // 自动追踪 route.params.id\n  fetchDetail(route.params.id)\n})\nstop()  // 停止监听",
    tags: ["Vue3", "computed", "watch", "watchEffect"],
  },
  {
    id: "vue-12",
    title: "keep-alive 组件详解",
    category: "vue",
    difficulty: 2,
    question: "keep-alive 的作用和原理是什么？如何控制缓存的组件？",
    answer: "作用：缓存组件实例，切换时不销毁，保留状态。\n\n原理：\n- 将组件 VNode 缓存在内存中\n- 切换时调用 deactivate 而非 unmount\n- 激活时调用 activate 而非 mount\n\n属性：\n- include：匹配的组件名会被缓存\n- exclude：匹配的组件名不会被缓存\n- max：最大缓存实例数（LRU 淘汰）\n\n生命周期：\n- onActivated：组件被激活时\n- onDeactivated：组件被停用时\n\n常见用法：\n- 配合 router-view 缓存页面\n- 配合动态组件缓存 Tab 内容\n\n注意：\n- 被缓存组件不会触发 onMounted/onUnmounted\n- 需要在 onActivated 中刷新数据",
    codeExample: "<!-- 缓存路由组件 -->\n<router-view v-slot=\"{ Component }\">\n  <keep-alive :include=\"['UserList', 'Settings']\" :max=\"10\">\n    <component :is=\"Component\" />\n  </keep-alive>\n</router-view>\n\n<!-- 组件内监听激活 -->\nonActivated(() => {\n  refreshData()  // 每次激活时刷新\n})",
    tags: ["Vue", "keep-alive", "缓存", "activated"],
  },
  {
    id: "vue-13",
    title: "Vue 3 Teleport 与 Suspense",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 中 Teleport 和 Suspense 的作用和使用场景？",
    answer: "Teleport（传送门）：\n- 将组件内容渲染到 DOM 中的其他位置\n- 解决弹窗/下拉菜单被父元素 overflow:hidden 遮挡的问题\n- 逻辑上仍属于当前组件（事件/prop 正常工作）\n\n场景：Modal、Dialog、Tooltip、全局通知、全屏 Loading\n\nSuspense（悬念）：\n- 等待异步组件/异步 setup 完成前显示 fallback\n- 支持 async setup 组件\n- 支持 defineAsyncComponent 异步加载\n\n场景：异步组件加载、数据预取、代码分割",
    codeExample: "<!-- Teleport -->\n<Teleport to=\"body\">\n  <div class=\"modal\">\n    <h2>弹窗标题</h2>\n    <slot />\n  </div>\n</Teleport>\n\n<!-- Suspense -->\n<Suspense>\n  <template #default>\n    <AsyncComponent />\n  </template>\n  <template #fallback>\n    <LoadingSpinner />\n  </template>\n</Suspense>\n\n// 异步组件\nconst AsyncComp = defineAsyncComponent(() =>\n  import('./HeavyComponent.vue')\n)",
    tags: ["Vue3", "Teleport", "Suspense", "异步组件"],
  },
  {
    id: "vue-14",
    title: "Vue 编译优化：静态提升与标记",
    category: "vue",
    difficulty: 3,
    question: "Vue 3 编译器做了哪些优化来提升运行时性能？",
    answer: "1. 静态提升 (Static Hoisting)：\n- 纯静态节点只创建一次，后续复用\n- 减少重复的 VNode 创建开销\n\n2. 静态标记 (PatchFlags)：\n- 编译时为动态节点标记类型\n- 如 TEXT, CLASS, PROPS 等\n- diff 时只检查标记的动态部分\n\n3. Block Tree：\n- 将模板分成 block\n- 每个 block 只收集动态后代节点\n- diff 时跳过所有静态内容\n\n4. 缓存事件处理函数：\n- 内联函数 @click=\"handler\" 自动缓存\n- 避免每次渲染创建新函数\n\n5. Tree-shaking：\n- 按需引入运行时 API\n- 未使用的功能不会打包",
    codeExample: "// 编译前\n<template>\n  <div>\n    <h1>静态标题</h1>  <!-- 静态提升 -->\n    <p>{{ msg }}</p>     <!-- PatchFlag: TEXT -->\n    <span :class=\"cls\">{{ text }}</span>  <!-- PatchFlag: CLASS | TEXT -->\n  </div>\n</template>\n\n// 编译后（简化）\nconst hoisted = _createElementVNode('h1', null, '静态标题', -1)\n\nfunction render() {\n  return _createElementBlock('div', null, [\n    hoisted,  // 复用静态节点\n    _createVNode('p', null, _toDisplayString(msg), 1 /* TEXT */),\n    _createVNode('span', { class: cls }, _toDisplayString(text), 3 /* TEXT | CLASS */)\n  ])\n}",
    tags: ["Vue3", "编译优化", "PatchFlags", "静态提升"],
  },
  {
    id: "vue-15",
    title: "Vue 3 自定义指令",
    category: "vue",
    difficulty: 2,
    question: "Vue 3 如何自定义指令？与 Vue 2 的钩子函数有什么区别？",
    answer: "Vue 3 自定义指令钩子（与组件生命周期对齐）：\n- created：元素创建后，属性/事件之前\n- beforeMount：挂载前\n- mounted：挂载后\n- beforeUpdate：更新前\n- updated：更新后\n- beforeUnmount：卸载前\n- unmounted：卸载后\n\n与 Vue 2 区别：\n- bind → beforeMount\n- inserted → mounted\n- update → removed（与 updated 合并）\n- componentUpdated → updated\n- unbind → unmounted\n\n简写形式：\n- 传入函数时，在 mounted 和 updated 时都会调用\n\n常见应用：\n- v-focus 自动聚焦\n- v-permission 权限控制\n- v-loading 加载状态\n- v-lazy 懒加载\n- v-debounce 防抖点击",
    codeExample: "// 自定义 v-focus 指令\nconst vFocus = {\n  mounted(el: HTMLInputElement) {\n    el.focus()\n  }\n}\n\n// 自定义 v-permission\nconst vPermission = {\n  mounted(el, binding) {\n    const role = getUserRole()\n    if (!binding.value.includes(role)) {\n      el.parentNode?.removeChild(el)\n    }\n  }\n}\n\n// 使用\n<input v-focus />\n<button v-permission=\"['admin']\">删除</button>",
    tags: ["Vue3", "自定义指令", "directive"],
  },
  {
    id: "vue-16",
    title: "Vue SSR 与 Nuxt.js",
    category: "vue",
    difficulty: 3,
    question: "什么是 SSR？Vue SSR 的原理和优缺点？Nuxt.js 带来了什么？",
    answer: "SSR (Server-Side Rendering)：服务端渲染 HTML 后发送到客户端。\n\n原理：\n- 服务端执行 Vue 组件渲染为 HTML 字符串\n- 客户端接收 HTML 并「激活」(hydration) 使其可交互\n- hydration 过程复用服务端渲染的 DOM，绑定事件\n\n优点：\n- 首屏加载快（无需等待 JS 下载执行）\n- SEO 友好（搜索引擎可抓取完整内容）\n- 社交媒体分享预览\n\n缺点：\n- 服务端压力大\n- 开发受限（不能直接用 window/document）\n- 生命周期只有 beforeCreate/created 在服务端执行\n- hydration 不匹配时需要重建 DOM\n\nNuxt.js：\n- 基于 Vue 的全栈框架\n- 自动路由、自动导入、SSR/SSG 切换\n- 内置 SEO 管理、数据获取策略\n- 支持 Universal / SPA / SSG 模式",
    tags: ["Vue", "SSR", "Nuxt", "服务端渲染"],
  },
  {
    id: "vue-17",
    title: "nextTick 原理与应用",
    category: "vue",
    difficulty: 2,
    question: "Vue 中 nextTick 的作用是什么？原理是什么？",
    answer: "作用：在下一次 DOM 更新循环结束之后执行回调。\n\n使用场景：\n- 数据更新后需要操作更新后的 DOM\n- 在 created 中需要访问 DOM 时\n\n原理：\n- 内部维护一个回调队列\n- 数据变化触发 Watcher 更新入队\n- 在同一 tick 内多次数据变化只触发一次更新\n- 用 Promise.then / MutationObserver / setImmediate / setTimeout 实现异步\n- Vue 3 中直接用 Promise 实现\n\n注意：\n- Vue 3 中 nextTick 返回 Promise\n- 同一 tick 中多个 nextTick 按注册顺序执行\n- 本质是微任务",
    codeExample: "// 数据更新后获取 DOM\nconst count = ref(0)\nfunction increment() {\n  count.value++\n  await nextTick()\n  console.log(document.querySelector('.count').textContent)\n}\n\n// Vue 3 nextTick 返回 Promise\nimport { nextTick } from 'vue'\nawait nextTick()\n\n// 内部简化实现\nconst queue = []\nlet pending = false\nfunction nextTick(cb) {\n  return Promise.resolve().then(cb)\n}",
    tags: ["Vue", "nextTick", "异步更新", "DOM"],
  },
  {
    id: "vue-18",
    title: "Vue mixin 的应用与局限",
    category: "vue",
    difficulty: 2,
    question: "Vue 中 mixin 有什么用途？有什么问题和替代方案？",
    answer: "mixin：把公共逻辑混入多个组件中。\n\n优点：\n- 代码复用\n- 逻辑共享\n\n问题：\n1. 命名冲突：多个 mixin 或组件间属性/方法名冲突\n2. 来源不清晰：不知道某个方法来自哪个 mixin\n3. 隐式依赖：mixin 依赖组件的某些属性\n4. 维护困难：修改 mixin 可能影响所有使用组件\n\n合并策略：\n- data 和 methods：组件优先\n- 生命周期钩子：先 mixin 后组件（依次执行）\n- watch/computed/props：组件覆盖 mixin\n\n替代方案：\n- Composition API（显著改善以上问题）\n- 自定义 Hooks / Composables\n- 高阶组件 (HOC)\n- 插槽 (Slots)",
    codeExample: "// mixin 方式\nconst searchMixin = {\n  data() { return { keyword: '' } },\n  methods: { search() { /* ... */ } }\n}\n\n// 替代 - Composition API\nfunction useSearch() {\n  const keyword = ref('')\n  const results = ref([])\n  watch(keyword, async (kw) => {\n    results.value = await fetchSearch(kw)\n  })\n  return { keyword, results }\n}",
    tags: ["Vue", "mixin", "逻辑复用", "Composition API"],
  },
  {
    id: "vue-19",
    title: "插槽 (Slot) 详解：默认/具名/作用域",
    category: "vue",
    difficulty: 2,
    question: "Vue 中插槽有哪几种类型？各自的使用场景？",
    answer: "默认插槽：<slot /> 默认内容\n\n具名插槽：<slot name=\"header\" /> + <template #header>\n- 多个插槽共存时区分\n- 配合 v-slot 缩写 #\n\n作用域插槽：子组件传数据给父组件的插槽内容\n- <slot :item=\"item\" :index=\"index\" />\n- <template #default=\"{ item, index }\">\n\n动态插槽名：#['dynamicName']\n\n常见应用：\n- 布局组件（header/sidebar/main/footer）\n- 列表组件自定义渲染项\n- Dialog/Modal 的标题和内容\n- Table 列自定义渲染\n- 表单组件自定义表单项",
    codeExample: "<!-- 子组件 List -->\n<template>\n  <div v-for=\"(item, index) in items\" :key=\"item.id\">\n    <slot name=\"item\" :item=\"item\" :index=\"index\">\n      {{ item.name }}  <!-- 默认 -->\n    </slot>\n  </div>\n</template>\n\n<!-- 父组件使用 -->\n<List :items=\"users\">\n  <template #item=\"{ item, index }\">\n    <div class=\"user-card\">\n      <span>{{ index + 1 }}. {{ item.name }}</span>\n    </div>\n  </template>\n</List>",
    tags: ["Vue", "slot", "插槽", "作用域插槽"],
  },
  {
    id: "vue-20",
    title: "Vue 修饰符 (Modifiers) 大全",
    category: "vue",
    difficulty: 1,
    question: "Vue 中有哪些常用修饰符？各自的作用？",
    answer: "事件修饰符：\n- .stop：阻止冒泡\n- .prevent：阻止默认\n- .self：只当 event.target 是自身触发\n- .once：只触发一次\n- .capture：捕获阶段触发\n- .passive：不阻止默认行为（提升滚动性能）\n\n按键修饰符：\n- .enter, .tab, .delete, .esc, .space\n- .up, .down, .left, .right\n- 系统键：.ctrl, .alt, .shift, .meta\n- .exact：精确匹配系统键\n\nv-model 修饰符：\n- .lazy：change 事件而非 input\n- .number：自动转数字\n- .trim：去除首尾空格\n\n鼠标修饰符：\n- .left, .right, .middle",
    codeExample: "<!-- 事件修饰符 -->\n<button @click.stop.prevent=\"submit\">提交</button>\n<form @submit.prevent=\"onSubmit\">\n<div @click.self=\"close\">\n\n<!-- 按键修饰符 -->\n<input @keyup.enter=\"search\" />\n<input @keyup.ctrl.s=\"save\" />\n\n<!-- v-model 修饰符 -->\n<input v-model.lazy=\"msg\" />\n<input v-model.number=\"age\" />\n<input v-model.trim=\"name\" />",
    tags: ["Vue", "修饰符", "事件", "v-model"],
  },
  {
    id: "vue-21",
    title: "Vue 权限管理方案",
    category: "vue",
    difficulty: 2,
    question: "Vue 项目中如何实现权限管理？包括路由级别和按钮级别。",
    answer: "权限管理层次：\n\n1. 路由权限（页面级）：\n   - router.beforeEach 中检查权限\n   - 动态路由：从后端获取菜单，addRoute 动态添加\n   - meta.roles 配置角色\n\n2. 按钮权限（操作级）：\n   - 自定义指令 v-permission\n   - 或条件渲染 v-if=\"hasPermission('edit')\"\n\n3. 数据权限（接口级）：\n   - 请求拦截器添加 token\n   - 后端根据角色过滤数据\n\n流程：\n1. 登录后获取用户角色和权限列表\n2. 存储到 store 中\n3. 路由守卫根据角色动态生成路由\n4. 组件内通过权限码控制按钮显隐\n5. 接口层面后端再次校验",
    codeExample: "// 路由守卫\nrouter.beforeEach((to, from, next) => {\n  const user = useUserStore()\n  if (to.meta.roles && !to.meta.roles.includes(user.role)) {\n    next('/403')\n  } else next()\n})\n\n// 自定义指令\nconst vPermission = {\n  mounted(el, binding) {\n    const perms = useUserStore().permissions\n    if (!perms.includes(binding.value)) {\n      el.parentNode?.removeChild(el)\n    }\n  }\n}\n<button v-permission=\"'user:edit'\">编辑</button>",
    tags: ["Vue", "权限", "路由守卫", "指令"],
  },
  {
    id: "vue-22",
    title: "Vue 项目中 Axios 封装",
    category: "vue",
    difficulty: 1,
    question: "在 Vue 项目中如何封装 Axios？核心功能有哪些？",
    answer: "封装核心功能：\n\n1. 实例创建：baseURL, timeout, headers\n2. 请求拦截器：\n   - 添加 token\n   - 添加公共参数\n   - Loading 展示\n   - 请求加密\n3. 响应拦截器：\n   - 统一错误处理\n   - 401 跳转登录 / 刷新 token\n   - 数据格式化\n   - 错误提示\n4. 取消重复请求：AbortController\n5. 请求缓存\n6. 超时重试：axios-retry\n\n最佳实践：\n- 结构化响应格式：{ code, data, message }\n- 错误码统一管理\n- 类型安全（TypeScript 泛型）",
    codeExample: "// request.ts\nimport axios from 'axios'\n\nconst request = axios.create({\n  baseURL: import.meta.env.VITE_API_BASE,\n  timeout: 10000,\n})\n\nrequest.interceptors.request.use(config => {\n  const token = getToken()\n  if (token) config.headers.Authorization = `Bearer ${token}`\n  return config\n})\n\nrequest.interceptors.response.use(\n  res => res.data,\n  error => {\n    if (error.response?.status === 401) router.push('/login')\n    return Promise.reject(error)\n  }\n)\n\nexport default request",
    tags: ["Vue", "Axios", "拦截器", "封装"],
  }
]
