import type { KnowledgePoint } from '../types'

export const javascriptData: KnowledgePoint[] = [
  {
    id: "js-01",
    title: "闭包的概念与应用",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请解释 JavaScript 闭包（Closure）的原理，并举出实际应用场景。",
    answer: '闭包这个概念我刚开始学 JS 的时候也是稀里糊涂的，后来在一个实际 bug 里才真正搞懂。\n\n说白了，闭包就是函数能记住它出生时周围的环境。一个函数在 A 函数内部定义，它就能访问 A 函数里的变量，就算 A 函数已经执行完了、返回了，这个内部函数还能通过闭包访问到那些变量。\n\n我举个实际场景。我们游戏活动页有个防重复点击的逻辑，用闭包做计数器再合适不过了：\n\n```javascript\nfunction createCounter() {\n  let count = 0\n  return {\n    increment: () => ++count,\n    getCount: () => count\n  }\n}\n```\n\ncount 这个变量在外面完全访问不到，只能通过返回的方法来操作。这就是模块模式的核心——用闭包实现私有变量。\n\n还有 React 的 Hooks 本质也是闭包。useState 返回的 state 存在哪？存在 React 内部的闭包环境里。这也是为什么 Hooks 必须在组件顶层调用——React 通过调用顺序来关联闭包里存的状态。\n\n容易踩的坑：循环里用 var 声明变量然后在 setTimeout 里引用——所有回调拿到的都是循环结束后的最终值。以前用 var 的时候用立即执行函数（IIFE）来"捕获"每次循环的值，现在用 let 就自动搞定了，因为 let 每次迭代都会创建一个新的块级作用域。\n\n面试官追问闭包会不会导致内存泄漏？会的，因为闭包引用着外部变量，GC 不敢回收。但也别过度担心——合理的闭包（比如事件回调、定时器）是正常使用。关键是记得在不用的时⋯候清理：组件卸载时清掉定时器、解绑事件、关掉 WebSocket。',
    codeExample: "// 数据私有化\nfunction createCounter() {\n  let count = 0  // 私有变量\n  return {\n    increment: () => ++count,\n    decrement: () => --count,\n    getCount: () => count\n  }\n}\nconst counter = createCounter()\ncounter.increment() // 1\n\n// 函数工厂\nfunction multiply(factor: number) {\n  return (x: number) => x * factor\n}\nconst double = multiply(2)\ndouble(5) // 10",
    tags: ["闭包", "作用域", "词法环境"],
  },
  {
    id: "js-02",
    title: "原型链与继承",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请解释 JavaScript 的原型链（Prototype Chain）机制，以及 ES6 class 继承的原理。",
    answer: '原型链是 JS 继承机制的基石。每个 JS 对象都有一个内部 [[Prototype]] 链接，通过 __proto__ 访问（或者用 Object.getPrototypeOf()）。你在对象上读一个属性，如果自身没有，JS 引擎就沿着原型链往上找，直到 null。\n\n这个过程就像你在找东西——先在自己桌上找，找不到去隔壁房间找，再找不到去楼下找，直到没有可以找的地方。\n\nJS 的继承也经历了好几个阶段。最早的直接改 prototype：Child.prototype = new Parent()，但问题是所有子实例共享父实例的属性（引用类型会互相影响）。后来用构造函数继承：Parent.call(this)，解决了引用共享但没法继承原型上的方法。组合继承把两者拼在一起，但调了两次父构造函数。\n\n寄生组合继承是目前最优方案：Object.create(Parent.prototype) 创建一个干净的中间对象，只继承原型方法不继承实例属性。ES6 的 class 语法糖背后就是寄生组合继承。\n\n在实际项目里，现在基本都用 class 了，但我还是觉得理解原型链很有必要——因为你调试的时候看到的是一个对象的 __proto__ 链条，console.log 出来全是 [[Prototype]]。而且很多库的内部实现还是用原型操作的，比如 Vue 2 的响应式就深度依赖 Object.defineProperty 来劫持原型上的属性访问。',
    codeExample: "// ES6 Class 继承\nclass Animal {\n  constructor(name: string) {\n    this.name = name\n  }\n  speak() {\n    console.log(`${this.name} makes a sound`)\n  }\n}\n\nclass Dog extends Animal {\n  constructor(name: string, breed: string) {\n    super(name)\n    this.breed = breed\n  }\n  speak() {\n    console.log(`${this.name} barks`)\n  }\n}",
    tags: ["原型链", "继承", "class", "prototype"],
  },
  {
    id: "js-03",
    title: "事件循环 (Event Loop) 与宏任务/微任务",
    category: "javascript",
    subCategory: "异步编程",
    difficulty: 3,
    question: "请详细描述 JavaScript 的事件循环机制，包括宏任务和微任务的执行顺序。",
    answer: '事件循环（Event Loop）是 JS 面试永远的必考题，因为它决定了所有异步代码的执行顺序。我记这个有句口诀："同步清空 → 微任务清空 → 一个宏任务 → 微任务清空 → 循环"。\n\n具体展开说：一段脚本开始执行，先跑完所有同步代码——调用栈从满到空。然后检查微任务队列（Microtask Queue），把里面的任务一个不剩地执行完——包括执行过程中新产生的微任务也要清空。然后从宏任务队列（Macrotask Queue）取一个任务执行。执行完这一个宏任务，又要清空微任务队列。然后循环。\n\n宏任务包括 setTimeout、setInterval、I/O、UI 渲染。微任务包括 Promise.then/catch/finally、MutationObserver、queueMicrotask()。\n\n这个执行顺序在真实项目里最常踩的坑是：你在一个 setTimeout 回调里更新了数据，然后在同一个异步流程里期望 Promise.then 能拿到——它拿不到，因为 Promise.then 在上一个宏任务之后的下一个微任务阶段就执行了，而 setTimeout 还在更后面。\n\n面试官喜欢让你口算输出顺序。比如经典的：console.log(1) → setTimeout(console.log(2), 0) → Promise.resolve().then(console.log(3)) → console.log(4)。结果是 1、4、3、2。因为同步（1、4）先跑，然后微任务（3），最后宏任务（2）。node 里 process.nextTick 比 Promise 还早——它在微任务之前执行。',
    codeExample: "console.log('1')  // 同步\n\nsetTimeout(() => console.log('2'), 0)  // 宏任务\n\nPromise.resolve().then(() => console.log('3'))  // 微任务\n\nconsole.log('4')  // 同步\n\n// 输出顺序：1 → 4 → 3 → 2",
    tags: ["事件循环", "Event Loop", "宏任务", "微任务", "Promise"],
  },
  {
    id: "js-04",
    title: "Promise 实现原理",
    category: "javascript",
    subCategory: "异步编程",
    difficulty: 3,
    question: "Promise 的三种状态是什么？能否手写一个简易 Promise？",
    answer: 'Promise 这个设计太经典了。核心要理解的就三个点：状态机、链式调用、异步执行。\n\n三种状态：pending（等待中）、fulfilled（成功）、rejected（失败）。状态一旦从 pending 变成 fulfilled 或 rejected，就不能再变了——这保证了异步结果的确定性。你调了三次 resolve，只有第一次生效。\n\n链式调用的关键是 then 返回一个新 Promise。这允许你 .then().then().catch() 一路写下去。then 的回调分两种情况：如果回调返回值不是 Promise，包装成 resolved；如果返回值是 Promise，就等这个 Promise 解决。then 的第二个参数和 catch 的区别：catch 能捕获前面所有 then 里的错误，而 then 的第二个参数只能捕获前一个 Promise 的错误。\n\n手写 Promise 是面试高频题。核心要实现：构造函数接收 executor（含 resolve/reject）、内部维护状态和回调数组、resolve/reject 要保证异步（用 setTimeout 包一下）、then 返回新 Promise、处理 onFulfilled 不传的情况实现值穿透。\n\n实际项目里我现在很少手写 Promise 了，async/await 更直观。但理解 then 的执行时机很重要——Promise.then 是微任务，这决定了它和 setTimeout 的执行顺序。',
    codeExample: "class MyPromise {\n  state = 'pending'\n  value: any = undefined\n  callbacks: Array<{ onFulfilled: Function, onRejected: Function }> = []\n\n  constructor(executor: Function) {\n    const resolve = (value: any) => {\n      if (this.state !== 'pending') return\n      this.state = 'fulfilled'\n      this.value = value\n      this.callbacks.forEach(cb => cb.onFulfilled(value))\n    }\n    const reject = (reason: any) => {\n      if (this.state !== 'pending') return\n      this.state = 'rejected'\n      this.value = reason\n      this.callbacks.forEach(cb => cb.onRejected(reason))\n    }\n    try { executor(resolve, reject) } catch(e) { reject(e) }\n  }\n\n  then(onFulfilled?: any, onRejected?: any) {\n    return new MyPromise((resolve: Function, reject: Function) => {\n      if (this.state === 'fulfilled') resolve(onFulfilled?.(this.value))\n      else if (this.state === 'rejected') reject(onRejected?.(this.value))\n      else this.callbacks.push({\n        onFulfilled: (v: any) => resolve(onFulfilled?.(v)),\n        onRejected: (r: any) => reject(onRejected?.(r))\n      })\n    })\n  }\n}",
    tags: ["Promise", "异步", "手写", "微任务"],
    relatedIds: ["js-03", "js-05"]
  },
  {
    id: "js-05",
    title: "async/await 原理与错误处理",
    category: "javascript",
    subCategory: "异步编程",
    difficulty: 2,
    question: "async/await 是如何实现的？它与 Promise 的关系是什么？",
    answer: 'async/await 本质上是 Generator + Promise 的语法糖。async 函数始终返回一个 Promise，await 会暂停函数执行，等待后面的 Promise resolve，拿到值再继续。\n\n这玩意真香但也容易用错。最大的坑是：await 会阻塞 async 函数内部的后续代码，但它不阻塞外部代码。很多人以为 await 像同步阻塞一样会影响整个程序——不会的，只有当前 async 函数内部被"暂停"了。\n\n另一个常见错误是串行执行本可以并行的异步操作。比如：\n\n```javascript\n// 不好：串行，总耗时 = 1s + 1s = 2s\nconst user = await fetchUser()\nconst posts = await fetchPosts()\n\n// 好：并行，总耗时 = max(1s, 1s) = 1s\nconst [user, posts] = await Promise.all([fetchUser(), fetchPosts()])\n```\n\n错误处理上，try/catch 包 await 是最常规的做法。但也有个坑：如果 await 后面的 Promise reject 了且你没 catch，错误会往上抛变成未处理的 rejection。我现在的习惯是写一个 wrap 工具函数，把 try/catch 封装掉返回 [error, result] 元组。\n\n面试官追问 forEach 里用 await 会怎样？forEach 的回调是同步执行的，它不会等 await。要用 for...of 或者 Promise.all + map。',
    codeExample: "// async/await 错误处理\nasync function fetchData() {\n  try {\n    const res = await fetch('/api/data')\n    const data = await res.json()\n    return data\n  } catch (err) {\n    console.error('请求失败:', err)\n    return null\n  }\n}\n\n// 并行请求\nconst [user, posts] = await Promise.all([\n  fetchUser(),\n  fetchPosts()\n])",
    tags: ["async", "await", "Generator", "Promise"],
    relatedIds: ["js-04"]
  },
  {
    id: "js-06",
    title: "this 指向的四种绑定规则",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请详细说明 JavaScript 中 this 的四种绑定规则及其优先级。",
    answer: 'JS 的 this 指向是最容易面试"翻车"的话题之一，因为它取决于调用方式而非定义位置。有个口诀是："new 显隐默"，优先级从高到低。\n\n最高优先级是 new 绑定——你 new 一个构造函数，this 指向新创建的对象。\n\n其次是显式绑定——用 call、apply、bind 强制指定 this。call 和 apply 的区别只是传参方式（call 逐个传，apply 用数组）。bind 返回一个新函数，this 永久绑定。\n\n然后隐式绑定——对象的方法调用，this 指向那个对象：obj.fn() 的 this 就是 obj。这是最常见的情况。\n\n最低优先级是默认绑定——独立函数调用，非严格模式下 this = window（浏览器）或 global（Node），严格模式下是 undefined。\n\n如果面试官追问箭头函数的 this，跟上面四规则没关系——箭头函数没有自己的 this，它继承定义时的外层作用域的 this。所以箭头函数很适合用在回调里（比如 setTimeout、事件处理），不会丢 this。\n\n实际踩坑场景：把对象方法赋值给变量再调用，this 就丢了。React 类组件里的事件处理方法，不用 bind 或箭头函数的话 this 就是 undefined。这两个坑我早期都掉进去过。',
    codeExample: "// new 绑定\nfunction Person(name) { this.name = name }\n\n// 显式绑定\nfn.call(obj, arg1, arg2)\nfn.apply(obj, [arg1, arg2])\nconst bound = fn.bind(obj)\n\n// 隐式绑定 vs 丢失\nconst obj = {\n  name: 'obj',\n  fn() { console.log(this.name) }\n}\nobj.fn()  // 'obj'\nconst f = obj.fn\nf()  // undefined (strict)",
    tags: ["this", "bind", "call", "apply", "箭头函数"],
  },
  {
    id: "js-07",
    title: "防抖 (Debounce) 与节流 (Throttle)",
    category: "javascript",
    subCategory: "性能优化",
    difficulty: 2,
    question: "请解释防抖和节流的区别，并手写实现。",
    answer: '防抖和节流这俩概念面试频率极高，而且经常被问手写实现。\n\n防抖（Debounce）的思路是"等你不动了我再动手"。比如搜索框——用户连续输入，你不想每敲一个字符就发请求，而是等用户停下来了（比如 300ms 没再输入）再发。核心就是用 setTimeout 延迟执行，每次新触发就清掉上次的 timer 重新计时。适合搜索输入、窗口 resize、按钮防重复点击、表单验证。\n\n节流（Throttle）的思路是"不管你来多少次，我固定频率处理"。比如滚动事件——用户疯狂滚，你不需要每秒触发 100 次，每 200ms 处理一次就行。核心是记录上次执行时间，这次的间隔不够就跳过。适合滚动加载、鼠标移动、视频播放进度上报。\n\n手写的话，防抖分"延迟执行版"和"立即执行版"。延迟版就是上面说的标准思路。立即执行版是第一次触发立即执行，之后要等冷却结束才能再触发——适合需要即时反馈的场景，比如按钮点击。\n\n节流分"时间戳版"和"定时器版"。时间戳版在触发瞬间判断间隔，可能会有尾巴丢掉的场景——最后一次触发在间隔内，如果之后没有新触发，这次就被跳过了。定时器版可以保证最后一次也执行。\n\n实际使用我推荐直接用 lodash 的 debounce 和 throttle——它们把 trailing、leading、maxWait 都处理好了。但面试让你手写的时候得会。',
    codeExample: "// 防抖\nfunction debounce(fn: Function, delay: number) {\n  let timer: number\n  return function(...args: any[]) {\n    clearTimeout(timer)\n    timer = setTimeout(() => fn.apply(this, args), delay)\n  }\n}\n\n// 节流（时间戳版）\nfunction throttle(fn: Function, interval: number) {\n  let last = 0\n  return function(...args: any[]) {\n    const now = Date.now()\n    if (now - last >= interval) {\n      last = now\n      fn.apply(this, args)\n    }\n  }\n}",
    tags: ["防抖", "节流", "debounce", "throttle"],
  },
  {
    id: "js-08",
    title: "深拷贝与浅拷贝",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请解释深拷贝和浅拷贝的区别，并实现一个深拷贝函数。",
    answer: '深拷贝和浅拷贝这个问题看起来简单，但要做到"完善"很难。\n\n浅拷贝只复制对象的第一层属性。如果属性是引用类型，拷贝的是引用，原对象和拷贝对象共享内层数据。展开运算符、Object.assign、数组的 slice/concat 都是浅拷贝。对于那些只有一层属性的简单对象（比如配置项、表单初始值），浅拷贝就够了。\n\n深拷贝要求递归复制所有层级，每个引用类型都创建新对象。JSON.parse(JSON.stringify(obj)) 是最粗暴但最常用的方式——但它有三个致命缺陷：忽略 undefined、函数、Symbol；Date 会变成字符串；无法处理循环引用。\n\n我在实际项目里最常用的深拷贝方案是 lodash 的 cloneDeep——它处理了 Date、RegExp、Map、Set、循环引用等所有边界情况。如果你非要手写一个精简版，核心是：类型判断 + 递归调用 + WeakMap 记录已拷贝对象防止循环引用。\n\n面试官追问：如果对象层级非常深（几百层），递归深拷贝会爆栈吗？会的。可以用迭代（自己维护一个栈）代替递归，或者用 MessageChannel + structuredClone 做浏览器原生的深拷贝——structuredClone 现在主流浏览器都支持了，处理循环引用和大多数特殊对象都没问题。',
    codeExample: "function deepClone(obj: any, map = new WeakMap()): any {\n  if (obj === null || typeof obj !== 'object') return obj\n  if (obj instanceof Date) return new Date(obj)\n  if (obj instanceof RegExp) return new RegExp(obj)\n  if (map.has(obj)) return map.get(obj)  // 循环引用\n\n  const clone: any = Array.isArray(obj) ? [] : {}\n  map.set(obj, clone)\n\n  for (const key of Object.keys(obj)) {\n    clone[key] = deepClone(obj[key], map)\n  }\n  return clone\n}\n\n// 原生方案\nconst cloned = structuredClone(original)",
    tags: ["深拷贝", "浅拷贝", "clone"],
  },
  {
    id: "js-09",
    title: "let/const/var 的区别与暂时性死区",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 1,
    question: "请详细说明 var、let、const 的区别，以及什么是暂时性死区（TDZ）？",
    answer: 'let、const、var 的区别其实可以归结为三个维度：作用域、提升、重复声明。\n\nvar 是函数级作用域，let 和 const 是块级作用域（花括号内有效）。这个区别在 for 循环里特别明显——用 var 声明的 i 循环完还能访问到，用 let 就只能在循环体内访问。\n\n第二个区别是暂时性死区（TDZ）。var 声明被提升到作用域顶部并初始化为 undefined，你在声明前访问 var 变量拿到 undefined。let 和 const 也被提升，但不会被初始化——在声明之前访问会报 ReferenceError。这段"声明之前"的时间窗口就叫 TDZ。\n\nconst 比 let 多一个限制：声明时必须赋值，且不能重新赋值。但注意 const 声明的是常量引用，不是常量值——const 对象的内容可以修改，只是不能整个替换。\n\n我的实际使用习惯：默认用 const，需要重新赋值的时候用 let，基本不用 var 了（除非在维护遗留代码）。还有一个值得注意的点：const 在 for...of 和 for...in 里每次迭代都创建一个新的绑定，所以可以安全使用——很多人以为 const 不能在循环里用。',
    codeExample: "// TDZ 示例\nconsole.log(a) // undefined (var hoisted)\nvar a = 1\n\nconsole.log(b) // ReferenceError: Cannot access 'b' before initialization\nlet b = 2\n\n// const 引用类型\nconst obj = { x: 1 }\nobj.x = 2  // OK\nobj = {}   // TypeError",
    tags: ["var", "let", "const", "TDZ", "暂时性死区", "块级作用域"],
  },
  {
    id: "js-10",
    title: "ES6 模块化 (ESM) vs CommonJS",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 2,
    question: "ES Module 和 CommonJS 有什么区别？它们的加载机制有何不同？",
    answer: 'ESM（ES Modules）和 CommonJS 是 JS 模块化历史上的两个里程碑。\n\n最明显的区别是语法：ESM 用 import/export，CommonJS 用 require/module.exports。但底层的差异大得多。\n\nCommonJS 是动态加载的——你可以在 if 条件里 require，模块的导入是运行时决定的。它是同步的，所以适合 Node.js（文件系统读取很快）。require 返回的是导出对象的拷贝。\n\nESM 是静态的——import 必须在文件顶部，不能在条件里用。这让打包工具可以做 tree-shaking（移除未使用的代码）和静态分析。import 返回的是导出值的只读引用（live binding）——如果导出模块更新了变量，导入方能看到最新的值。\n\n浏览器里用 ESM 需要在 script 标签上加 type="module"。还有一个容易被忽略的点：ESM 默认是 defer 的——module 脚本不会阻塞 HTML 解析，等 DOM 构建完毕再执行。\n\n实际项目里，现在新项目基本全用 ESM 了（Vite、Next.js 都是默认 ESM）。但在 Node.js 里混用两者还是会踩坑：ESM 里不能用 require，除非你 createRequire；CJS 里不能用 import，但可以动态 import()。',
    codeExample: "// CommonJS\nconst { foo } = require('./module')\nmodule.exports = { bar }\n\n// ES Module\nimport { foo } from './module.js'\nimport('./module.js').then(m => m.foo) // 动态导入\nexport const bar = 123\nexport default function() {}",
    tags: ["ESM", "CommonJS", "模块化", "import", "require", "Tree Shaking"],
  },
  {
    id: "js-11",
    title: "Map/Set/WeakMap/WeakSet 区别与应用",
    category: "javascript",
    subCategory: "数据结构",
    difficulty: 2,
    question: "请对比 Map、Set、WeakMap、WeakSet 的特性、区别和应用场景。",
    answer: 'Map、Set、WeakMap、WeakSet 这四个数据结构，我刚开始用的时候也经常搞混。\n\nMap 最大特点是 key 可以是任意类型（对象、函数、基本类型都行），而普通对象的 key 只能是字符串或 Symbol。Map 保持插入顺序，有 .size 属性，有 .keys()、.values()、.entries() 迭代器。我用 Map 最多的场景是：需要频繁添加/删除键值对，或者 key 不是字符串的情况（比如用 DOM 元素作为 key 存关联数据）。\n\nSet 是唯一值的集合，自动去重。一个典型场景是记录"已读消息 ID"——往 Set 里 add，重复的自动忽略。ES6 之前得用对象来模拟，麻烦。\n\nWeakMap 的 key 必须是对象，而且对 key 是弱引用——如果 key 被 GC 回收了，对应的条目也自动消失，且不可迭代。这个特性让 WeakMap 特别适合存"对象关联的元数据"。比如 Vue 3 的响应式就用 WeakMap 来存对象和它的 proxy 的映射——如果对象被销毁了，映射会自动清理，不会内存泄漏。\n\nWeakSet 只能存对象、弱引用、不可迭代。用得不多，一个场景是给对象打标记（比如标记"已经处理过"）而不影响 GC。\n\n面试官追问：WeakMap 为什么不能遍历？因为它的条目可能随时被 GC 回收——你遍历的时候发现最前面的条目被回收了，后面的索引全变了。所以设计上就禁止迭代。',
    codeExample: "// Map 应用 - 计数\nconst map = new Map<string, number>()\narr.forEach(x => map.set(x, (map.get(x) || 0) + 1))\n\n// Set 应用 - 去重\nconst unique = [...new Set(arr)]\n\n// WeakMap - 私有数据\nconst privates = new WeakMap()\nclass Foo {\n  constructor() { privates.set(this, { secret: 42 }) }\n  getSecret() { return privates.get(this).secret }\n}",
    tags: ["Map", "Set", "WeakMap", "WeakSet", "数据结构"],
  },
  {
    id: "js-12",
    title: "跨域解决方案详解",
    category: "javascript",
    subCategory: "网络请求",
    difficulty: 2,
    question: "什么是跨域？有哪些常见的跨域解决方案？各自的优缺点是什么？",
    answer: '跨域这个问题这么多年了，解决方案已经非常成熟。我用几个场景来说明。\n\n最标准最安全的是 CORS。服务端在响应头里加 Access-Control-Allow-Origin，浏览器就会放行。简单请求直接用，复杂请求（比如 POST JSON 带 Authorization）会先发 OPTIONS 预检请求。坑点：CORS 只管浏览器的限制——你在 Postman 里调同个接口没问题，浏览器里就不行。\n\nJSONP 是古老的跨域手段，利用 script 标签不受同源限制。只支持 GET、有 XSS 风险，现在基本不用了。\n\npostMessage 适用于 iframe 或者跨 tab 通信。发送方调 targetWindow.postMessage(data, origin)，接收方监听 message 事件。注意 origin 参数要设对——不要用 \'*\'，应该精确指定目标域。\n\nWebSocket 天然不受同源限制——用 ws:// 而不是 http://。适合实时通信场景。\n\nNginx 反向代理是我们项目里最常用的方案——前端开发服务器把所有 /api 请求转发到后端，对浏览器来说请求全发给自己同源的地址。Vite 里配 proxy 就行。\n\n面试官追问：跨域携带 cookie 怎么做？服务端设 Access-Control-Allow-Credentials: true，客户端设 credentials: \'include\'。注意 withCredentials 时 Access-Control-Allow-Origin 不能是 \'*\'，必须精确匹配。',
    codeExample: "// Vite 代理配置\n// vite.config.ts\nexport default {\n  server: {\n    proxy: {\n      '/api': {\n        target: 'https://backend.example.com',\n        changeOrigin: true,\n        rewrite: (path) => path.replace(/^\\/api/, '')\n      }\n    }\n  }\n}",
    tags: ["跨域", "CORS", "JSONP", "代理", "同源策略"],
  },
  {
    id: "js-13",
    title: "前端存储方案对比",
    category: "javascript",
    subCategory: "浏览器 API",
    difficulty: 1,
    question: "请对比 Cookie、localStorage、sessionStorage、IndexedDB 的特性。",
    answer: '前端存储方案的选择经常被简化成"cookie vs localStorage"，但实际场景要细致得多。\n\nCookie 最老、限制最多。4KB 上限、每次 HTTP 请求都自动带上（浪费带宽）、因安全问题逐渐被淘汰。现在的主要用途是存储 session token（HttpOnly + Secure + SameSite）。如果你存非鉴权的数据，别用 cookie。\n\nlocalStorage 容量大（5-10MB）、API 简单（getItem/setItem）、不会自动发送。适合存用户配置、主题偏好、草稿等非敏感持久化数据。但注意：localStorage 是同步 API，主线程上读写大量数据会阻塞 UI。\n\nsessionStorage 和 localStorage 几乎一样，唯一区别是关闭标签页数据就清空。这个特性在表单多步骤场景很好用——用户刷新页面草稿还在，但关闭了整个标签页就清掉防止下次混淆。\n\nIndexedDB 是浏览器的"真正的数据库"，支持事务、索引、大容量存储（几百 MB 甚至更大）。异步 API 不阻塞主线程。我用来存视频字幕数据、离线缓存文件。用原生 IndexedDB API 地狱般的回调，一般用 Dexie.js 或 idb 库包装。\n\nService Worker Cache 是专门的缓存存储，配合 SW 可以做离线应用。我在游戏官网上就用了 Workbox 做 SW 缓存。\n\n面试官追问：localStorage 真的完全同步吗？是的，读写 localStorage 是同步 IO，但在现代浏览器上已经很快了。不过极端场景（存几百 KB 的数据）还是会卡，这种情况下建议 IndexedDB。',
    codeExample: "// localStorage\nlocalStorage.setItem('theme', 'dark')\nconst theme = localStorage.getItem('theme')\n\n// IndexedDB (简化)\nconst db = await openDB('myDB', 1, {\n  upgrade(db) {\n    db.createObjectStore('items', { keyPath: 'id' })\n  }\n})\nawait db.put('items', { id: 1, name: 'test' })\nconst item = await db.get('items', 1)",
    tags: ["Cookie", "localStorage", "sessionStorage", "IndexedDB", "存储"],
  },
  {
    id: "js-14",
    title: "箭头函数 vs 普通函数",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 1,
    question: "箭头函数和普通函数有什么区别？什么场景不适合用箭头函数？",
    answer: '箭头函数和普通函数的核心区别，我觉得就三个点：this、arguments、constructor。\n\n箭头函数没有自己的 this——它的 this 是"定义时的外层 this"，而不是"调用时的 this"。所以箭头函数特别适合写回调：你在 React 组件里写 onClick={handleClick}，如果用普通函数就得 bind，用箭头函数就直接搞定。\n\n箭头函数没有 arguments 对象——你需要参数的时候用 rest 参数（...args）。这个我倒觉得是优点，因为 rest 参数是真数组，arguments 是类数组。\n\n箭头函数不能用 new 调用，因为它没有 [[Construct]] 内部方法。也没有 prototype 属性。所以它不适合做构造函数。\n\n还有一个小区别：箭头函数不能用 yield（不能做 Generator 函数），也不能用 bind/call/apply 改变 this。\n\n实际使用习惯：回调函数用箭头函数（简写简洁且 this 不丢），对象方法用普通函数（需要动态 this），构造函数用 class（不涉及箭头/普通的区别）。React 函数组件里的所有函数基本都用箭头函数。',
    codeExample: "// ❌ 对象方法不要用箭头函数\nconst obj = {\n  name: 'obj',\n  fn: () => console.log(this.name) // this 指向外层（window/undefined）\n}\n\n// ✅ 普通函数\nconst obj2 = {\n  name: 'obj',\n  fn() { console.log(this.name) } // this 指向 obj2\n}\n\n// ✅ 回调场景\n[1, 2, 3].map(x => x * 2)\nsetTimeout(() => this.doSomething(), 1000)",
    tags: ["箭头函数", "this", "ES6"],
  },
  {
    id: "js-15",
    title: "JavaScript 数据类型与检测方法",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "JavaScript 有哪些数据类型？如何准确检测数据类型？",
    answer: 'JavaScript 的数据类型分两类：基本类型和引用类型。基本类型包括 string、number、bigint、boolean、undefined、symbol、null（虽然 typeof null 返回 "object" 是历史 bug）。\n\n类型检测有四种方式，各有适用场景。typeof 最简单，但 null → "object" 是经典坑，且区分不了数组和对象。所以 typeof 主要用来检测基本类型和 undefined。\n\ninstanceof 检测原型链——obj instanceof Constructor，看 Constructor.prototype 是否在 obj 的原型链上。但跨 iframe 或跨 realm 的时候会失效（两个窗口的 Array 构造函数不同）。适合在单窗口环境里用。\n\nObject.prototype.toString.call() 是最靠谱的——能精确返回 "[object Array]"、"[object Date]"、"[object Null]" 这种。但写法太啰嗦，一般封装成工具函数。\n\nArray.isArray() 是专门检测数组的，有很好的浏览器兼容性。\n\n实际项目里，我封装了一个 getType 函数，内部用 Object.prototype.toString.call 返回类型字符串，然后 typeof 做快速路径。对于 null 和 undefined 单独处理。基本类型检测没必要把每一种方法都用上——typeof + Array.isArray 覆盖了 90% 的需求。',
    codeExample: "// 通用类型检测\nfunction getType(val: unknown): string {\n  return Object.prototype.toString.call(val).slice(8, -1)\n}\n\ngetType([])     // 'Array'\ngetType(null)   // 'Null'\ngetType(new Map()) // 'Map'\n\n// NaN 检测\nNumber.isNaN(val)  // 比 isNaN 更严格\n\n// 判断是否纯对象\nfunction isPlainObject(val: unknown): boolean {\n  return Object.prototype.toString.call(val) === '[object Object]'\n}",
    tags: ["数据类型", "typeof", "instanceof", "类型检测"],
  },
  {
    id: "js-16",
    title: "new 操作符的实现原理",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "new 操作符做了什么？如何手写实现一个 new？",
    answer: 'new 操作符背后干了四件事：\n\n1. 创建一个新的空对象\n2. 让这个对象通过原型链连接到构造函数的原型（obj.__proto__ = Constructor.prototype）\n3. 调用构造函数，把 this 绑定到新对象（执行构造函数里的初始化代码）\n4. 如果构造函数返回了一个对象，就返回那个对象；否则返回新创建的对象（第 4 步经常被遗忘）\n\n手写 new 的面试题思路就是模拟这四步。关键细节是第 4 步——构造函数返回基本类型（如 return 42）会被忽略，返回新对象；但返回引用类型（如 return { x: 1 }）会覆盖。\n\n面试官追问：new 和 Object.create 有什么区别？new 会执行构造函数（走一遍上面的四步），Object.create 只创建原型链连接，不调构造函数——适合做纯粹的原型继承，干净。',
    codeExample: "function myNew(Constructor: Function, ...args: any[]) {\n  // 1. 创建对象，绑定原型\n  const obj = Object.create(Constructor.prototype)\n  // 2. 执行构造函数\n  const result = Constructor.apply(obj, args)\n  // 3. 返回结果\n  return result instanceof Object ? result : obj\n}\n\n// 使用\nfunction Person(name: string) {\n  this.name = name\n}\nconst p = myNew(Person, 'Alice')\nconsole.log(p.name)      // 'Alice'\nconsole.log(p instanceof Person) // true",
    tags: ["new", "构造函数", "手写", "原型"],
  },
  {
    id: "js-17",
    title: "call / apply / bind 实现原理",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "call、apply、bind 的区别？如何手写实现它们？",
    answer: 'call、apply、bind 都是强制改变函数 this 的方法，但细节不一样。\n\ncall 和 apply 是立即执行的——call(obj, arg1, arg2)、apply(obj, [args])。唯一的区别就是传参方式不同。我通常：参数已知数量用 call（更直观），参数是数组用 apply。\n\nbind 不立即执行，返回一个 this 永久绑定的新函数。bind 还可以做偏函数（partial application）——预先填一部分参数：fn.bind(obj, presetArg)。\n\n手写实现的思路：call 和 apply 是在目标对象上临时挂这个函数，执行完再删掉——这样就利用了隐式绑定规则。bind 稍微复杂点，要返回一个新函数，在新函数里调 call/apply。还有一个高阶细节：bind 返回的函数如果用 new 调用，this 绑定会失效——new 的优先级高于 bind。实现时要检查一下是否通过 new 调用。',
    codeExample: "// call\nFunction.prototype.myCall = function(ctx: any, ...args: any[]) {\n  ctx = ctx || window\n  const fn = Symbol('fn')\n  ctx[fn] = this\n  const result = ctx[fn](...args)\n  delete ctx[fn]\n  return result\n}\n\n// bind\nFunction.prototype.myBind = function(ctx: any, ...args1: any[]) {\n  const fn = this\n  return function F(...args2: any[]) {\n    return fn.apply(\n      this instanceof F ? this : ctx,\n      [...args1, ...args2]\n    )\n  }\n}",
    tags: ["call", "apply", "bind", "手写", "this"],
  },
  {
    id: "js-18",
    title: "数组常用方法与链式调用",
    category: "javascript",
    subCategory: "数组操作",
    difficulty: 1,
    question: "请列举 JavaScript 数组的常用高阶方法，并说明哪些会改变原数组。",
    answer: '数组方法不用全背，但高频的那几个必须熟。\n\nmap 做映射——每个元素变一下，返回新数组。filter 做筛选——true 的保留。reduce 做聚合——累加、分组、管道操作。find 找第一个匹配的。some 和 every 做断言——有一个满足或全都满足。\n\n链式调用的核心是：所有不直接改变原数组的方法（纯函数）都可以链起来。比如 .filter().map().reduce()。但要注意中间数组的创建——每个方法都生成新数组，大数组链式调用会内存占用高。这种情况用一次 reduce 替代多次 map+filter。\n\n实际项目里，我很少写那种"用 reduce 实现 map"的炫技代码——可读性比奇技淫巧重要。但我确实经常用 reduce 做分组和转换——比如把一个用户列表按角色分组，reduce 比写循环干净得多。\n\n面试官追问 flat 和 flatMap：flat 拍平嵌套数组，flatMap 是先 map 再 flat（深度为 1）。场景很实用——比如分词后拍平："hello world".split(" ").flatMap(word => word.split(""))。',
    codeExample: "// 链式调用实现数据转换\nconst result = users\n  .filter(u => u.age >= 18)\n  .map(u => ({\n    name: u.name.toUpperCase(),\n    year: new Date().getFullYear() - u.age\n  }))\n  .sort((a, b) => b.year - a.year)\n  .slice(0, 10)\n\n// reduce 实现 flat\nfunction flat(arr: any[], depth = 1) {\n  return depth > 0\n    ? arr.reduce((acc, val) =>\n        acc.concat(Array.isArray(val) ? flat(val, depth - 1) : val), [])\n    : arr.slice()\n}",
    tags: ["数组", "map", "filter", "reduce", "高阶函数"],
  },
  {
    id: "js-19",
    title: "ES2020+ 新特性汇总",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 1,
    question: "ES2020 及之后版本有哪些重要的新特性？",
    answer: 'ES2020 以来的新特性，我用得最多的是这几个。\n\n可选链（?.）解决了深层属性访问的 null 检查噩梦。以前 a && a.b && a.b.c，现在 a?.b?.c，遇到 undefined/null 直接短路返回 undefined，不报错。在响应式数据处理和 API 响应解析里特别常用。\n\nNullish Coalescing（??）是 || 的改进版——只在 null 和 undefined 时走默认值，0、""、false 不会触发。比如 count ?? 0 在 count 为 0 时不会错误地替换掉（而 count || 0 会）。这个经常和可选链搭配使用：user?.name ?? "匿名用户"。\n\n逻辑赋值运算符（??=、||=、&&=）让代码更简洁。比如 cache[key] ??= fetchData(key)，缓存里有就用缓存的，没有就 fetch 并赋值。\n\nBigInt 处理大整数，做计算器或者加密的时候有用，平常基本用不上。\n\nglobalThis 统一了不同环境的全局对象（浏览器的 window、Node 的 global、Web Worker 的 self）。\n\nPromise.allSettled 等待所有 Promise 完成（不管成功失败），适合你要处理一批异步请求但不想一个失败全停的场景——和 Promise.all 的最大区别就在这里。',
    codeExample: "// Optional Chaining + Nullish Coalescing\nconst name = user?.profile?.name ?? 'Anonymous'\n\n// 类私有字段\nclass Counter {\n  #count = 0  // 私有字段\n  increment() { this.#count++ }\n  get value() { return this.#count }\n}\n\n// Array.at()\nconst arr = [1, 2, 3]\narr.at(-1)  // 3 (最后一个)\n\n// Top-level await (module)\nconst data = await fetch('/api').then(r => r.json())",
    tags: ["ES2020", "ES2021", "Optional Chaining", "Nullish Coalescing", "新特性"],
  },
  {
    id: "js-20",
    title: "内存泄漏常见场景与排查",
    category: "javascript",
    subCategory: "性能优化",
    difficulty: 2,
    question: "前端常见的内存泄漏场景有哪些？如何排查和避免？",
    answer: '内存泄漏在单页应用里特别值得重视，因为用户可能不刷新页面用半小时，内存一点点涨上去就卡了。\n\n最常见的原因：全局变量意外的被声明了（没有 var/let/const 修饰的变量变成 global 属性），GC 不敢清。定时器忘记清——setInterval 设了但组件卸载了没 clearInterval。事件监听器忘了解绑——addEventListener 了但没 removeEventListener。闭包里引用了大的变量但一直持有引用。\n\nReact 项目里还有一个常见的：useEffect 里创建的订阅或连接忘了在 cleanup 里销毁。比如 WebSocket 连接、EventSource 监听、DOM Mutation Observer。\n\n排查方法：Chrome DevTools 的 Memory 面板，Take Heap Snapshot 看哪些对象占内存最大。Performance Monitor 面板看 JS Heap Size 的曲线——如果一直在涨没有回落，就有泄漏嫌疑。还有一个技巧：在 Performance 面板里录制、勾选 Memory，可以看到内存曲线的 GC 痕迹。\n\n我实际碰到的一个泄漏案例：视频播放页里每秒更新一次播放进度存到 localStorage，但页面关闭时那个 setInterval 没清，导致页面虽然视觉上关了（路由变了），但定时器还在跑——"幽灵定时器"。修复就是 useEffect return 里 clearInterval。',
    codeExample: "// ❌ 容易泄漏的代码\nuseEffect(() => {\n  const timer = setInterval(() => {\n    fetch('/api/heartbeat')\n  }, 10000)\n  // 忘记清除定时器！\n}, [])\n\n// ✅ 正确做法\nuseEffect(() => {\n  const timer = setInterval(() => {\n    fetch('/api/heartbeat')\n  }, 10000)\n  return () => clearInterval(timer) // 清理函数\n}, [])",
    tags: ["内存泄漏", "GC", "调试", "Chrome DevTools"],
  },
  {
    id: "js-21",
    title: "for...of / for...in / forEach 区别",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "for...of、for...in 和 forEach 有什么区别？各自的使用场景？",
    answer: '这三个循环方式看起来做一样的事，但差异不小。\n\nfor...in 遍历对象的所有可枚举属性，包括原型链上的。数组用它会遍历到非索引属性（如果你给数组加了自定义属性），不推荐用在数组上。主要用于遍历对象 key。\n\nfor...of 遍历可迭代对象——数组、字符串、Map、Set、NodeList、生成器等。它只遍历值，不管原型链，也不会漏掉非索引属性。数组场景首选 for...of。\n\nforEach 是数组方法，对每个元素执行回调，回调里 return 只能跳过当前回调（类似 continue），不能跳出循环。如果中途想 break，只能用普通的 for 或 for...of。而且 forEach 里不能用 async/await——它不等 Promise。\n\n性能上，普通 for 循环最快，其次是 for...of，forEach 最慢——但差距在大多数场景可忽略。\n\n实际使用习惯：遍历数组用 for...of（看起来最舒服且支持 break），遍历对象用 for...in 或 Object.keys().forEach()，需要索引的遍历用 entries()：for (const [i, val] of arr.entries())。',
    codeExample: "// for...in - 对象\nfor (const key in obj) {\n  if (Object.hasOwn(obj, key)) {\n    console.log(key, obj[key])\n  }\n}\n\n// for...of - 数组/可迭代对象\nfor (const [index, value] of arr.entries()) {\n  console.log(index, value)\n}\n\n// for...of + await\nfor (const item of items) {\n  await processItem(item)  // 串行处理\n}",
    tags: ["for...of", "for...in", "forEach", "迭代"],
  },
  {
    id: "js-22",
    title: "严格模式 (Strict Mode) 详解",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "严格模式 (\"use strict\") 有哪些限制和行为变化？",
    answer: '严格模式（\'use strict\'）是 ES5 引入的，目的是让 JS 抛更多的错误，减少静默失败。\n\n最常见的几个变化：变量必须声明才能用（否则 reference error 而非静默创建全局变量）、函数内部的 this 不再默认指向 window 而是 undefined、不能给不可写属性赋值、不能删除不可删属性、函数参数不能重名、禁用了 with 语句、禁止八进制字面量。\n\nclass 和 ES Modules 里默认就是严格模式——你不需要显式写 \'use strict\'。\n\n实际项目里，新项目用 ES Modules 自动严格模式了，不用操心。老项目如果还是普通 script 标签加载的，建议在文件头部加 \'use strict\'。反正也不影响什么，只是多了一行。\n\n面试官追问：严格模式能不能局部使用？可以——在函数内部第一行写 \'use strict\'，只对那个函数生效。但通常都是文件级或者在 IIFE 里用。',
    codeExample: "'use strict'\n\n// 变量必须先声明\nx = 1  // ReferenceError: x is not defined\n\n// this 为 undefined\nfunction fn() {\n  console.log(this) // undefined (非严格模式为 window)\n}\n\n// 禁止删除变量\nlet a = 1\ndelete a // SyntaxError",
    tags: ["严格模式", "use strict", "this"],
  },
  {
    id: "js-23",
    title: "函数柯里化 (Currying) 与应用",
    category: "javascript",
    subCategory: "函数式编程",
    difficulty: 2,
    question: "什么是函数柯里化？它有什么实际应用场景？如何实现？",
    answer: '函数柯里化就是把一个多参数函数变成一串单参数函数。比如 add(a, b, c) 柯里化后变成 add(a)(b)(c)。\n\n实际项目里最实用的场景：参数复用。比如你要对很多不同的数加 10——把 add 柯里化成 const add10 = curry(add)(10)，然后对数字列表 map(add10)。每个数"加 10"这个公共操作被预填充了，调用时只需传一个参数。\n\n另一个常见应用是事件处理的参数预填充。比如多个按钮的点击事件都需要传同一个参数（按钮组 ID），用柯里化预填 ID，每个按钮的 onClick 只需一个参数。\n\n手写柯里化实现：核心是递归 + 闭包。定义一个内部函数，收集参数直到参数个数达到原函数的参数个数再执行，否则继续返回一个收参数的函数。\n\n和偏函数（partial application）的区别：柯里化是把 n 元函数变成 n 个一元函数的链条，偏函数是提前填一部分参数得到一个新函数。bind 就是偏函数的一种。实际中用 lodash 的 curry 就行，不用手写。',
    codeExample: "// 通用柯里化函数\nfunction curry(fn: Function) {\n  return function curried(...args: any[]) {\n    if (args.length >= fn.length) {\n      return fn.apply(this, args)\n    }\n    return (...args2: any[]) => curried.apply(this, [...args, ...args2])\n  }\n}\n\n// 使用示例\nconst add = (a: number, b: number, c: number) => a + b + c\nconst curriedAdd = curry(add)\ncurriedAdd(1)(2)(3)  // 6\ncurriedAdd(1, 2)(3)  // 6\n\n// 实际应用 - 日志函数\nconst log = curry((level: string, date: Date, msg: string) =>\n  `[${level}] ${date.toISOString()} - ${msg}`\n)\nconst errorLog = log('ERROR')(new Date())\nerrorLog('Something went wrong')",
    tags: ["柯里化", "curry", "函数式编程", "偏函数"],
  },
  {
    id: "js-24",
    title: "垃圾回收机制 (GC) 详解",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 3,
    question: "JavaScript 的垃圾回收机制是怎样的？V8 引擎采用了哪些 GC 算法？",
    answer: 'JS 的垃圾回收（GC）是自动的，开发者不用手动释放内存——但这不意味着不用关心。\n\n主流引擎（V8）用的是分代回收。内存分两代：新生代（New Space）存生命周期短的对象，老生代（Old Space）存生命周期长的对象。\n\n新生代用 Scavenge 算法（Cheney 算法）——把内存空间分成两半，用一半空一半，存活的对象从使用区复制到空闲区，然后清空使用区，两区交换。这个过程很快但只适合小空间。\n\n老生代用 Mark-Sweep & Mark-Compact。Mark 阶段标记所有可达对象（从根对象出发——全局对象、执行栈、闭包引用等），Sweep 阶段清除未标记的对象。Compact 是整理内存碎片（把存活对象往一边移）。老生代空间大，GC 会比较慢——这也是"GC 停顿"的来源。\n\n理解 GC 对写代码的实际意义：避免意外保留引用——用完的变量赋 null、组件卸载时清理事件和定时器、避免全局缓存无限增大。还有就是别用 delete 操作符删除对象属性（会导致 V8 的隐藏类优化失效），直接赋 undefined 或 null 更好。',
    tags: ["GC", "垃圾回收", "V8", "内存管理"],
  },
  {
    id: "js-25",
    title: "Hoisting（变量提升）全面解析",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "什么是变量提升？函数提升和变量提升的区别是什么？",
    answer: '提升（Hoisting）这个面试题看起来基础，但真讲清楚不容易。\n\n简单说：JS 引擎在代码执行之前先扫描一遍，把所有的变量声明和函数声明"提升"到作用域的顶部。var 提升并初始化为 undefined，let/const 提升但不初始化，函数声明整体提升（声明+定义一起）。\n\n这就是为什么你可以在函数声明之前调用它——整个函数已经提升了。但 var 变量在声明之前访问拿到 undefined 而不是报错。\n\n函数表达式（var fn = function() {}）只有变量声明被提升，函数体不提升——在赋值之前调 fn 会报 undefined is not a function。\n\nlet/const 的"暂时性死区"（TDZ）本质就是提升但不初始化——从块开始到声明行之间的区域访问变量就报 ReferenceError。这个设计是刻意的——让你"正常地"在声明之后才能用变量，避免了 var 的静默 undefined 问题。\n\n面试官追问：class 会不会提升？class 在 JS 里也会提升，但和 let 一样有 TDZ——在声明之前访问 class 名称会报错。所以实际上你无法在 class 声明之前使用它，但严格来说它确实被提升了。',
    codeExample: "// 函数声明提升\nfoo() // 'hello'\nfunction foo() { console.log('hello') }\n\n// 函数表达式不提升\nbar() // TypeError: bar is not a function\nvar bar = function() { console.log('world') }\n\n// 优先级示例\nconsole.log(typeof fn) // 'function'\nvar fn = 1\nfunction fn() {}",
    tags: ["Hoisting", "变量提升", "函数提升", "TDZ"],
  }
]
