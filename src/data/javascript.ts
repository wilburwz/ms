import type { KnowledgePoint } from '../types'

export const javascriptData: KnowledgePoint[] = [
  {
    id: "js-01",
    title: "闭包的概念与应用",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请解释 JavaScript 闭包（Closure）的原理，并举出实际应用场景。",
    answer: "闭包：函数能够访问其外部作用域中的变量，即使外部函数已经执行完毕。\n\n原理：JavaScript 采用词法作用域，函数在定义时就确定了作用域链，执行时会创建闭包保留对外部变量的引用。\n\n应用场景：\n1. 数据私有化 / 模块模式：创建私有变量\n2. 函数工厂：生成带预设参数的函数\n3. 事件回调：保持对局部状态的访问\n4. 防抖/节流：保存 timer 变量\n5. React Hooks：useState、useEffect 本质都是闭包\n\n注意事项：\n- 可能导致内存泄漏（变量无法被 GC 回收）\n- 在循环中需要小心（经典 setTimeout 问题）",
    codeExample: "// 数据私有化\nfunction createCounter() {\n  let count = 0  // 私有变量\n  return {\n    increment: () => ++count,\n    decrement: () => --count,\n    getCount: () => count\n  }\n}\nconst counter = createCounter()\ncounter.increment() // 1\n\n// 函数工厂\nfunction multiply(factor: number) {\n  return (x: number) => x * factor\n}\nconst double = multiply(2)\ndouble(5) // 10",
    tags: ["闭包", "作用域", "词法环境"],
  },
  {
    id: "js-02",
    title: "原型链与继承",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请解释 JavaScript 的原型链（Prototype Chain）机制，以及 ES6 class 继承的原理。",
    answer: "原型链：每个 JS 对象都有一个内部 [[Prototype]] 链接（通过 __proto__ 访问），指向其原型对象。当访问对象属性时，如果在自身找不到，会沿着原型链向上查找直到 null。\n\n继承方式演进：\n1. 原型链继承：Child.prototype = new Parent()（共享引用问题）\n2. 构造函数继承：Parent.call(this)（无法继承原型方法）\n3. 组合继承：以上两者结合（调用了两次父构造函数）\n4. 寄生组合继承：Object.create() + call（最优方案）\n5. ES6 class：语法糖，背后仍是原型链\n\nclass 本质：\n- class 声明的方法在原型上定义\n- extends + super() = 寄生组合继承的语法糖\n- static 方法直接挂构造函数上",
    codeExample: "// ES6 Class 继承\nclass Animal {\n  constructor(name: string) {\n    this.name = name\n  }\n  speak() {\n    console.log(`${this.name} makes a sound`)\n  }\n}\n\nclass Dog extends Animal {\n  constructor(name: string, breed: string) {\n    super(name)\n    this.breed = breed\n  }\n  speak() {\n    console.log(`${this.name} barks`)\n  }\n}",
    tags: ["原型链", "继承", "class", "prototype"],
  },
  {
    id: "js-03",
    title: "事件循环 (Event Loop) 与宏任务/微任务",
    category: "javascript",
    subCategory: "异步编程",
    difficulty: 3,
    question: "请详细描述 JavaScript 的事件循环机制，包括宏任务和微任务的执行顺序。",
    answer: "事件循环 (Event Loop) 是 JS 的异步执行模型。\n\n执行流程：\n1. 执行同步代码（调用栈）\n2. 调用栈清空后，清空微任务队列（microtask queue）\n3. 从宏任务队列（macrotask queue）取出一个任务执行\n4. 重复 2-3\n\n宏任务 (MacroTask)：\n- setTimeout, setInterval, setImmediate(Node)\n- I/O, UI rendering\n- postMessage, MessageChannel\n\n微任务 (MicroTask)：\n- Promise.then/catch/finally\n- MutationObserver\n- queueMicrotask()\n- process.nextTick (Node, 优先级最高)\n\n关键：每执行一个宏任务后，必须清空所有微任务，然后才到下一个宏任务。",
    codeExample: "console.log('1')  // 同步\n\nsetTimeout(() => console.log('2'), 0)  // 宏任务\n\nPromise.resolve().then(() => console.log('3'))  // 微任务\n\nconsole.log('4')  // 同步\n\n// 输出顺序：1 → 4 → 3 → 2",
    tags: ["事件循环", "Event Loop", "宏任务", "微任务", "Promise"],
  },
  {
    id: "js-04",
    title: "Promise 实现原理",
    category: "javascript",
    subCategory: "异步编程",
    difficulty: 3,
    question: "Promise 的三种状态是什么？能否手写一个简易 Promise？",
    answer: "Promise 三种状态：\n- pending：初始状态，可转为 fulfilled 或 rejected\n- fulfilled：操作成功，不可逆\n- rejected：操作失败，不可逆\n\n核心机制：\n- then 返回新 Promise，实现链式调用\n- 值穿透：then 中非函数的参数会被忽略\n- 发布订阅模式：同一 Promise 可多次 then\n\n关键实现点：\n1. 状态机（pending → fulfilled/rejected，不可逆）\n2. then 收集回调（用数组存储），异步执行时依次调用\n3. resolve/reject 用 setTimeout 包裹确保异步\n4. then 中返回值处理：普通值、thenable 对象递归解析\n\n静态方法：\n- Promise.resolve / reject\n- Promise.all：全部成功才成功，一个失败即失败\n- Promise.race：第一个完成的决定结果\n- Promise.allSettled：全部完成后返回结果（含成功/失败）\n- Promise.any：第一个成功的，全失败才 reject",
    codeExample: "class MyPromise {\n  state = 'pending'\n  value: any = undefined\n  callbacks: Array<{ onFulfilled: Function, onRejected: Function }> = []\n\n  constructor(executor: Function) {\n    const resolve = (value: any) => {\n      if (this.state !== 'pending') return\n      this.state = 'fulfilled'\n      this.value = value\n      this.callbacks.forEach(cb => cb.onFulfilled(value))\n    }\n    const reject = (reason: any) => {\n      if (this.state !== 'pending') return\n      this.state = 'rejected'\n      this.value = reason\n      this.callbacks.forEach(cb => cb.onRejected(reason))\n    }\n    try { executor(resolve, reject) } catch(e) { reject(e) }\n  }\n\n  then(onFulfilled?: any, onRejected?: any) {\n    return new MyPromise((resolve: Function, reject: Function) => {\n      if (this.state === 'fulfilled') resolve(onFulfilled?.(this.value))\n      else if (this.state === 'rejected') reject(onRejected?.(this.value))\n      else this.callbacks.push({\n        onFulfilled: (v: any) => resolve(onFulfilled?.(v)),\n        onRejected: (r: any) => reject(onRejected?.(r))\n      })\n    })\n  }\n}",
    tags: ["Promise", "异步", "手写", "微任务"],
    relatedIds: ["js-03", "js-05"]
  },
  {
    id: "js-05",
    title: "async/await 原理与错误处理",
    category: "javascript",
    subCategory: "异步编程",
    difficulty: 2,
    question: "async/await 是如何实现的？它与 Promise 的关系是什么？",
    answer: "async/await 是 Promise + Generator 的语法糖。\n\n- async 函数始终返回 Promise\n- await 会暂停函数执行，等待 Promise resolve\n- 本质是 Generator 函数的自动执行器\n\n错误处理：\n- try/catch 包裹 await 调用\n- .catch() 链式处理\n- Promise.allSettled() 批量处理多个异步且不中断\n\n注意事项：\n- await 会阻塞当前 async 函数内后续代码\n- 多个无依赖的 await 应用 Promise.all 并行\n- forEach 不支持 await，应用 for...of",
    codeExample: "// async/await 错误处理\nasync function fetchData() {\n  try {\n    const res = await fetch('/api/data')\n    const data = await res.json()\n    return data\n  } catch (err) {\n    console.error('请求失败:', err)\n    return null\n  }\n}\n\n// 并行请求\nconst [user, posts] = await Promise.all([\n  fetchUser(),\n  fetchPosts()\n])",
    tags: ["async", "await", "Generator", "Promise"],
    relatedIds: ["js-04"]
  },
  {
    id: "js-06",
    title: "this 指向的四种绑定规则",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请详细说明 JavaScript 中 this 的四种绑定规则及其优先级。",
    answer: "四种绑定规则（优先级从高到低）：\n\n1. new 绑定：new 调用构造函数，this 指向新创建的对象\n2. 显式绑定：call / apply / bind 强制指定 this\n3. 隐式绑定：对象方法调用，this 指向调用者（obj.fn() → this = obj）\n4. 默认绑定：独立函数调用，非严格模式指向 window/global，严格模式为 undefined\n\n箭头函数：没有自己的 this，继承外层作用域的 this（定义时确定，不可改变）。\n\n常见陷阱：\n- 回调函数中 this 丢失\n- 事件处理函数中 this 指向\n- setTimeout 中 this 指向 window",
    codeExample: "// new 绑定\nfunction Person(name) { this.name = name }\n\n// 显式绑定\nfn.call(obj, arg1, arg2)\nfn.apply(obj, [arg1, arg2])\nconst bound = fn.bind(obj)\n\n// 隐式绑定 vs 丢失\nconst obj = {\n  name: 'obj',\n  fn() { console.log(this.name) }\n}\nobj.fn()  // 'obj'\nconst f = obj.fn\nf()  // undefined (strict)",
    tags: ["this", "bind", "call", "apply", "箭头函数"],
  },
  {
    id: "js-07",
    title: "防抖 (Debounce) 与节流 (Throttle)",
    category: "javascript",
    subCategory: "性能优化",
    difficulty: 2,
    question: "请解释防抖和节流的区别，并手写实现。",
    answer: "防抖 (Debounce)：事件触发后等待 n 秒执行，如果 n 秒内再次触发则重新计时。\n- 场景：搜索框输入、窗口 resize、表单验证\n\n节流 (Throttle)：n 秒内只执行一次，无论触发多少次。\n- 场景：滚动事件、鼠标移动、按钮点击防重复\n\n对比：\n| | Debounce | Throttle |\n|---|----------|----------|\n| 执行时机 | 停止触发后 | 固定频率 |\n| 特点 | 把多次合并成一次 | 稀释执行频率 |",
    codeExample: "// 防抖\nfunction debounce(fn: Function, delay: number) {\n  let timer: number\n  return function(...args: any[]) {\n    clearTimeout(timer)\n    timer = setTimeout(() => fn.apply(this, args), delay)\n  }\n}\n\n// 节流（时间戳版）\nfunction throttle(fn: Function, interval: number) {\n  let last = 0\n  return function(...args: any[]) {\n    const now = Date.now()\n    if (now - last >= interval) {\n      last = now\n      fn.apply(this, args)\n    }\n  }\n}",
    tags: ["防抖", "节流", "debounce", "throttle"],
  },
  {
    id: "js-08",
    title: "深拷贝与浅拷贝",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "请解释深拷贝和浅拷贝的区别，并实现一个深拷贝函数。",
    answer: "浅拷贝：只复制第一层属性，嵌套对象共享引用。\n- Object.assign(), 展开运算符 (...), Array.slice()\n\n深拷贝：递归复制所有层级，完全独立。\n\n深拷贝实现要点：\n- 基础类型直接返回\n- 处理 Array, Object, Date, RegExp, Map, Set\n- 用 WeakMap 解决循环引用\n- 区分普通对象和特殊对象\n\n其他方案：\n- JSON.parse(JSON.stringify())：简单但丢失函数/undefined/Date\n- structuredClone()：原生 API，支持更多类型\n- Lodash _.cloneDeep()",
    codeExample: "function deepClone(obj: any, map = new WeakMap()): any {\n  if (obj === null || typeof obj !== 'object') return obj\n  if (obj instanceof Date) return new Date(obj)\n  if (obj instanceof RegExp) return new RegExp(obj)\n  if (map.has(obj)) return map.get(obj)  // 循环引用\n\n  const clone: any = Array.isArray(obj) ? [] : {}\n  map.set(obj, clone)\n\n  for (const key of Object.keys(obj)) {\n    clone[key] = deepClone(obj[key], map)\n  }\n  return clone\n}\n\n// 原生方案\nconst cloned = structuredClone(original)",
    tags: ["深拷贝", "浅拷贝", "clone"],
  },
  {
    id: "js-09",
    title: "let/const/var 的区别与暂时性死区",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 1,
    question: "请详细说明 var、let、const 的区别，以及什么是暂时性死区（TDZ）？",
    answer: "var：\n- 函数作用域\n- 变量提升（hoisting），初始化为 undefined\n- 可重复声明\n- 全局声明的变量会挂载到 window\n\nlet：\n- 块级作用域\n- 存在提升但不会被初始化（TDZ）\n- 不可重复声明\n- 不会挂载到 window\n\nconst：\n- 与 let 相同，但声明时必须初始化\n- 不可重新赋值（但引用类型的属性可修改）\n- 通常用于常量、函数引用\n\n暂时性死区 (TDZ)：\n- 从块开始到变量声明语句之间的区域\n- 在此期间访问变量会抛出 ReferenceError\n- 存在的意义：让错误更早暴露，避免使用未声明的变量",
    codeExample: "// TDZ 示例\nconsole.log(a) // undefined (var hoisted)\nvar a = 1\n\nconsole.log(b) // ReferenceError: Cannot access 'b' before initialization\nlet b = 2\n\n// const 引用类型\nconst obj = { x: 1 }\nobj.x = 2  // OK\nobj = {}   // TypeError",
    tags: ["var", "let", "const", "TDZ", "暂时性死区", "块级作用域"],
  },
  {
    id: "js-10",
    title: "ES6 模块化 (ESM) vs CommonJS",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 2,
    question: "ES Module 和 CommonJS 有什么区别？它们的加载机制有何不同？",
    answer: "CommonJS (CJS)：\n- 同步加载：require 是同步的，适合服务端\n- 值拷贝：导出的是值的拷贝，模块内变化不影响外部\n- 动态加载：可在条件中 require\n- 运行时加载：整个模块作为对象加载\n- this 指向当前模块\n\nES Module (ESM)：\n- 异步加载：支持浏览器环境\n- 值的引用（live binding）：导出的是值的引用，模块内变化会反映到外部\n- 静态结构：import 必须写在顶部，不能在条件中使用（import() 动态导入除外）\n- 编译时加载：只加载需要的部分，支持 tree-shaking\n- this 为 undefined\n\nNode.js 兼容：\n- .mjs 或 package.json \"type\": \"module\" 使用 ESM\n- import() 动态导入可在 CJS 中使用",
    codeExample: "// CommonJS\nconst { foo } = require('./module')\nmodule.exports = { bar }\n\n// ES Module\nimport { foo } from './module.js'\nimport('./module.js').then(m => m.foo) // 动态导入\nexport const bar = 123\nexport default function() {}",
    tags: ["ESM", "CommonJS", "模块化", "import", "require", "Tree Shaking"],
  },
  {
    id: "js-11",
    title: "Map/Set/WeakMap/WeakSet 区别与应用",
    category: "javascript",
    subCategory: "数据结构",
    difficulty: 2,
    question: "请对比 Map、Set、WeakMap、WeakSet 的特性、区别和应用场景。",
    answer: "Map：\n- 键值对集合，键可以是任意类型（对象、函数等）\n- 保留插入顺序\n- 可迭代（forEach, for...of）\n- 有 size 属性\n\nSet：\n- 值的集合，值唯一（自动去重）\n- 保留插入顺序，可迭代\n\nWeakMap：\n- 键必须是对象，弱引用（不影响 GC）\n- 不可迭代（无 size, forEach, keys）\n- 场景：存储私有数据、DOM 节点关联数据、缓存\n\nWeakSet：\n- 值必须是对象，弱引用\n- 不可迭代\n- 场景：标记对象是否被处理过\n\nvs Object：Map 键可任意类型、有顺序、性能更好；Object 适用于简单字符串键。",
    codeExample: "// Map 应用 - 计数\nconst map = new Map<string, number>()\narr.forEach(x => map.set(x, (map.get(x) || 0) + 1))\n\n// Set 应用 - 去重\nconst unique = [...new Set(arr)]\n\n// WeakMap - 私有数据\nconst privates = new WeakMap()\nclass Foo {\n  constructor() { privates.set(this, { secret: 42 }) }\n  getSecret() { return privates.get(this).secret }\n}",
    tags: ["Map", "Set", "WeakMap", "WeakSet", "数据结构"],
  },
  {
    id: "js-12",
    title: "跨域解决方案详解",
    category: "javascript",
    subCategory: "网络请求",
    difficulty: 2,
    question: "什么是跨域？有哪些常见的跨域解决方案？各自的优缺点是什么？",
    answer: "跨域（CORS）：浏览器的同源策略限制不同源（协议+域名+端口）之间的 HTTP 请求。\n\n解决方案：\n\n1. CORS（服务端设置响应头）：\n   - Access-Control-Allow-Origin\n   - 支持预检请求（OPTIONS）\n   - 最佳方案，需服务端配合\n\n2. JSONP（JSON with Padding）：\n   - 利用 script 标签不受同源限制\n   - 只支持 GET，不安全（XSS 风险）\n   - 老旧方案，不推荐\n\n3. 代理服务器（Proxy）：\n   - 开发环境：Vite/Webpack devServer proxy\n   - 生产环境：Nginx 反向代理\n\n4. WebSocket：不受同源策略限制\n\n5. postMessage：跨窗口/iframe 通信\n\n6. document.domain / window.name：老旧方案",
    codeExample: "// Vite 代理配置\n// vite.config.ts\nexport default {\n  server: {\n    proxy: {\n      '/api': {\n        target: 'https://backend.example.com',\n        changeOrigin: true,\n        rewrite: (path) => path.replace(/^\\/api/, '')\n      }\n    }\n  }\n}",
    tags: ["跨域", "CORS", "JSONP", "代理", "同源策略"],
  },
  {
    id: "js-13",
    title: "前端存储方案对比",
    category: "javascript",
    subCategory: "浏览器 API",
    difficulty: 1,
    question: "请对比 Cookie、localStorage、sessionStorage、IndexedDB 的特性。",
    answer: "Cookie：\n- 大小：~4KB\n- 随每个 HTTP 请求自动发送\n- 可设置过期时间、HttpOnly、Secure、SameSite\n- 同源共享\n\nlocalStorage：\n- 大小：~5-10MB\n- 持久存储，除非手动清除\n- 不随请求发送\n- 同源共享，同步 API\n\nsessionStorage：\n- 与 localStorage 类似\n- 标签页关闭即清除\n- 不同标签页之间不共享\n\nIndexedDB：\n- 大小：远大于其他方案（通常数百 MB）\n- 异步 API，支持事务\n- 可存储结构化数据（含文件、Blob）\n- 支持索引、查询\n\n选择指南：\n- 需发送到服务端 → Cookie\n- 简单键值持久化 → localStorage\n- 仅当前会话 → sessionStorage\n- 大量结构数据 → IndexedDB",
    codeExample: "// localStorage\nlocalStorage.setItem('theme', 'dark')\nconst theme = localStorage.getItem('theme')\n\n// IndexedDB (简化)\nconst db = await openDB('myDB', 1, {\n  upgrade(db) {\n    db.createObjectStore('items', { keyPath: 'id' })\n  }\n})\nawait db.put('items', { id: 1, name: 'test' })\nconst item = await db.get('items', 1)",
    tags: ["Cookie", "localStorage", "sessionStorage", "IndexedDB", "存储"],
  },
  {
    id: "js-14",
    title: "箭头函数 vs 普通函数",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 1,
    question: "箭头函数和普通函数有什么区别？什么场景不适合用箭头函数？",
    answer: "箭头函数的特点：\n- 没有自己的 this（继承外层作用域）\n- 没有 arguments 对象\n- 不能用作构造函数（不可 new）\n- 没有 prototype 属性\n- 不能用作 Generator 函数\n\n不适合使用箭头函数的场景：\n- 对象方法（需要 this 指向对象）\n- 构造函数\n- 需要动态 this 的事件处理（如 Vue 方法）\n- 需要 arguments 对象\n\n适合的场景：\n- 回调函数/数组方法（.map, .filter）\n- 需要保持外层 this 的场景\n- 简洁的纯函数",
    codeExample: "// ❌ 对象方法不要用箭头函数\nconst obj = {\n  name: 'obj',\n  fn: () => console.log(this.name) // this 指向外层（window/undefined）\n}\n\n// ✅ 普通函数\nconst obj2 = {\n  name: 'obj',\n  fn() { console.log(this.name) } // this 指向 obj2\n}\n\n// ✅ 回调场景\n[1, 2, 3].map(x => x * 2)\nsetTimeout(() => this.doSomething(), 1000)",
    tags: ["箭头函数", "this", "ES6"],
  },
  {
    id: "js-15",
    title: "JavaScript 数据类型与检测方法",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "JavaScript 有哪些数据类型？如何准确检测数据类型？",
    answer: "基本类型（7种）：string, number, bigint, boolean, undefined, symbol, null\n引用类型：Object（含 Array, Function, Date, RegExp, Map, Set 等）\n\n检测方法对比：\n\ntypeof：\n- 可检测：string, number, boolean, undefined, symbol, bigint, function\n- 局限：typeof null === 'object'，typeof [] === 'object'\n\ninstanceof：\n- 检测原型链，arr instanceof Array\n- 局限：跨 iframe 失效，基本类型无法检测\n\nObject.prototype.toString.call()：\n- 最全面的方法，返回 '[object Type]'\n- 可检测所有内置类型\n\nArray.isArray()：\n- ES5 引入，专门检测数组\n- 跨 iframe 可用",
    codeExample: "// 通用类型检测\nfunction getType(val: unknown): string {\n  return Object.prototype.toString.call(val).slice(8, -1)\n}\n\ngetType([])     // 'Array'\ngetType(null)   // 'Null'\ngetType(new Map()) // 'Map'\n\n// NaN 检测\nNumber.isNaN(val)  // 比 isNaN 更严格\n\n// 判断是否纯对象\nfunction isPlainObject(val: unknown): boolean {\n  return Object.prototype.toString.call(val) === '[object Object]'\n}",
    tags: ["数据类型", "typeof", "instanceof", "类型检测"],
  },
  {
    id: "js-16",
    title: "new 操作符的实现原理",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "new 操作符做了什么？如何手写实现一个 new？",
    answer: "new 的过程（四步）：\n\n1. 创建一个空对象\n2. 将该对象的 __proto__ 指向构造函数的 prototype\n3. 将 this 绑定到新对象，执行构造函数\n4. 如果构造函数返回对象，则返回该对象；否则返回新对象\n\n注意：\n- 构造函数返回基本类型会被忽略\n- 返回引用类型会替代 new 创建的对象\n- 箭头函数不能用作构造函数",
    codeExample: "function myNew(Constructor: Function, ...args: any[]) {\n  // 1. 创建对象，绑定原型\n  const obj = Object.create(Constructor.prototype)\n  // 2. 执行构造函数\n  const result = Constructor.apply(obj, args)\n  // 3. 返回结果\n  return result instanceof Object ? result : obj\n}\n\n// 使用\nfunction Person(name: string) {\n  this.name = name\n}\nconst p = myNew(Person, 'Alice')\nconsole.log(p.name)      // 'Alice'\nconsole.log(p instanceof Person) // true",
    tags: ["new", "构造函数", "手写", "原型"],
  },
  {
    id: "js-17",
    title: "call / apply / bind 实现原理",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 2,
    question: "call、apply、bind 的区别？如何手写实现它们？",
    answer: "区别：\n- call：逐个传参 fn.call(ctx, a, b, c)\n- apply：数组传参 fn.apply(ctx, [a, b, c])\n- bind：返回新函数，可预设参数 fn.bind(ctx, a)(b)\n\n手写实现思路：\n1. 将函数设为目标对象的临时属性\n2. 执行该函数\n3. 删除临时属性\n4. 返回结果（bind 返回新函数）\n\nbind 额外特性：\n- 可柯里化预设参数\n- 返回的函数可用作构造函数（new 时 this 指向实例）",
    codeExample: "// call\nFunction.prototype.myCall = function(ctx: any, ...args: any[]) {\n  ctx = ctx || window\n  const fn = Symbol('fn')\n  ctx[fn] = this\n  const result = ctx[fn](...args)\n  delete ctx[fn]\n  return result\n}\n\n// bind\nFunction.prototype.myBind = function(ctx: any, ...args1: any[]) {\n  const fn = this\n  return function F(...args2: any[]) {\n    return fn.apply(\n      this instanceof F ? this : ctx,\n      [...args1, ...args2]\n    )\n  }\n}",
    tags: ["call", "apply", "bind", "手写", "this"],
  },
  {
    id: "js-18",
    title: "数组常用方法与链式调用",
    category: "javascript",
    subCategory: "数组操作",
    difficulty: 1,
    question: "请列举 JavaScript 数组的常用高阶方法，并说明哪些会改变原数组。",
    answer: "不改变原数组（返回新数组）：\n- map：映射转换\n- filter：过滤筛选\n- reduce：累计归并\n- slice：截取片段\n- concat：合并\n- flat / flatMap：扁平化\n\n改变原数组：\n- push / pop：尾部添加/删除\n- unshift / shift：头部添加/删除\n- splice：增删改\n- sort / reverse：排序/反转\n- fill：填充\n\n查找方法（不改变原数组）：\n- find / findIndex：查找符合条件的第一个\n- includes / indexOf：是否包含\n- some / every：部分/全部满足条件",
    codeExample: "// 链式调用实现数据转换\nconst result = users\n  .filter(u => u.age >= 18)\n  .map(u => ({\n    name: u.name.toUpperCase(),\n    year: new Date().getFullYear() - u.age\n  }))\n  .sort((a, b) => b.year - a.year)\n  .slice(0, 10)\n\n// reduce 实现 flat\nfunction flat(arr: any[], depth = 1) {\n  return depth > 0\n    ? arr.reduce((acc, val) =>\n        acc.concat(Array.isArray(val) ? flat(val, depth - 1) : val), [])\n    : arr.slice()\n}",
    tags: ["数组", "map", "filter", "reduce", "高阶函数"],
  },
  {
    id: "js-19",
    title: "ES2020+ 新特性汇总",
    category: "javascript",
    subCategory: "ES6+",
    difficulty: 1,
    question: "ES2020 及之后版本有哪些重要的新特性？",
    answer: "ES2020：\n- Optional Chaining (?.)：安全访问深层属性\n- Nullish Coalescing (??)：空值合并操作符\n- BigInt：大整数类型\n- Promise.allSettled()\n- globalThis：统一全局对象引用\n- import() 动态导入\n\nES2021：\n- String.prototype.replaceAll()\n- Promise.any()\n- 逻辑赋值运算符 (&&=, ||=, ??=)\n- 数字分隔符 (1_000_000)\n\nES2022：\n- 类字段声明（无需 constructor）\n- 私有字段/方法 (#)\n- static 块\n- Array.prototype.at()：支持负索引\n- Top-level await\n- Object.hasOwn()（替代 hasOwnProperty）\n\nES2023+：\n- findLast / findLastIndex\n- toSorted / toReversed / toSpliced（不改变原数组）\n- Hashbang 语法支持",
    codeExample: "// Optional Chaining + Nullish Coalescing\nconst name = user?.profile?.name ?? 'Anonymous'\n\n// 类私有字段\nclass Counter {\n  #count = 0  // 私有字段\n  increment() { this.#count++ }\n  get value() { return this.#count }\n}\n\n// Array.at()\nconst arr = [1, 2, 3]\narr.at(-1)  // 3 (最后一个)\n\n// Top-level await (module)\nconst data = await fetch('/api').then(r => r.json())",
    tags: ["ES2020", "ES2021", "Optional Chaining", "Nullish Coalescing", "新特性"],
  },
  {
    id: "js-20",
    title: "内存泄漏常见场景与排查",
    category: "javascript",
    subCategory: "性能优化",
    difficulty: 2,
    question: "前端常见的内存泄漏场景有哪些？如何排查和避免？",
    answer: "常见场景：\n1. 意外的全局变量（未用 let/const/var 声明）\n2. 被遗忘的定时器/回调（setInterval 未清除）\n3. 闭包引用大对象\n4. DOM 引用未释放（移除 DOM 但 JS 仍持有引用）\n5. 事件监听未移除\n6. Map/Set 强引用导致对象无法 GC\n\n排查工具：\n- Chrome DevTools → Memory 面板\n- 堆快照（Heap Snapshot）：对比前后变化\n- 分配时间线（Allocation Timeline）\n- Performance Monitor\n\n避免措施：\n- 组件卸载时清除定时器/事件\n- 使用 WeakMap/WeakSet 存储对象引用\n- useEffect/Vue watch 返回值清理\n- 避免创建不必要的闭包\n- 使用严格模式避免意外全局变量",
    codeExample: "// ❌ 容易泄漏的代码\nuseEffect(() => {\n  const timer = setInterval(() => {\n    fetch('/api/heartbeat')\n  }, 10000)\n  // 忘记清除定时器！\n}, [])\n\n// ✅ 正确做法\nuseEffect(() => {\n  const timer = setInterval(() => {\n    fetch('/api/heartbeat')\n  }, 10000)\n  return () => clearInterval(timer) // 清理函数\n}, [])",
    tags: ["内存泄漏", "GC", "调试", "Chrome DevTools"],
  },
  {
    id: "js-21",
    title: "for...of / for...in / forEach 区别",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "for...of、for...in 和 forEach 有什么区别？各自的使用场景？",
    answer: "for...in：\n- 遍历对象可枚举属性（包括原型链）\n- 遍历的是键名（key）\n- 可用于对象和数组\n- 不保证遍历顺序\n- 需要 hasOwnProperty 过滤原型属性\n\nfor...of：\n- 遍历可迭代对象（Array, Map, Set, String, NodeList 等）\n- 遍历的是值（value）\n- 支持 break、continue、return\n- 支持 await（配合 async）\n- 不能直接遍历普通对象\n\nforEach：\n- 数组方法，接收回调\n- 不能 break/continue（除非抛出异常）\n- 不能配合 await（回调中用 async 无效）\n- 会遍历所有元素（不会被空元素跳过）\n\n总结：\n- 遍历对象属性 → for...in\n- 遍历可迭代对象值 → for...of\n- 简单遍历数组且有副作用 → forEach\n- 需要异步 → for...of + await",
    codeExample: "// for...in - 对象\nfor (const key in obj) {\n  if (Object.hasOwn(obj, key)) {\n    console.log(key, obj[key])\n  }\n}\n\n// for...of - 数组/可迭代对象\nfor (const [index, value] of arr.entries()) {\n  console.log(index, value)\n}\n\n// for...of + await\nfor (const item of items) {\n  await processItem(item)  // 串行处理\n}",
    tags: ["for...of", "for...in", "forEach", "迭代"],
  },
  {
    id: "js-22",
    title: "严格模式 (Strict Mode) 详解",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "严格模式 (\"use strict\") 有哪些限制和行为变化？",
    answer: "严格模式作用：让 JS 引擎以更严格的方式解析代码，减少静默错误。\n\n变化：\n1. 变量必须先声明后使用（禁止意外全局变量）\n2. 函数中的 this 不再指向 window，而是 undefined\n3. 禁止删除变量/函数（delete 只能删属性）\n4. 禁止函数参数重名\n5. 禁止八进制字面量\n6. eval/arguments 不能作为变量名\n7. eval 中声明的变量不会泄漏到外部作用域\n8. 禁止 with 语句\n9. arguments.callee 不可用\n\n启用方式：\n- 文件顶部 'use strict'\n- 函数内部 'use strict'（仅该函数严格）\n- ES6 class 和 module 默认严格模式",
    codeExample: "'use strict'\n\n// 变量必须先声明\nx = 1  // ReferenceError: x is not defined\n\n// this 为 undefined\nfunction fn() {\n  console.log(this) // undefined (非严格模式为 window)\n}\n\n// 禁止删除变量\nlet a = 1\ndelete a // SyntaxError",
    tags: ["严格模式", "use strict", "this"],
  },
  {
    id: "js-23",
    title: "函数柯里化 (Currying) 与应用",
    category: "javascript",
    subCategory: "函数式编程",
    difficulty: 2,
    question: "什么是函数柯里化？它有什么实际应用场景？如何实现？",
    answer: "柯里化 (Currying)：将一个接收多个参数的函数，转换为一系列接收单个参数的函数。\n\nfn(a, b, c) → fn(a)(b)(c)\n\n应用场景：\n1. 参数复用：预设部分参数\n2. 延迟执行：收集参数到足够数量再执行\n3. 函数组合（compose / pipe）\n4. bind 本质就是柯里化\n\n与偏函数（Partial Application）的区别：\n- 柯里化：每次只接收一个参数\n- 偏函数：一次性预设多个参数",
    codeExample: "// 通用柯里化函数\nfunction curry(fn: Function) {\n  return function curried(...args: any[]) {\n    if (args.length >= fn.length) {\n      return fn.apply(this, args)\n    }\n    return (...args2: any[]) => curried.apply(this, [...args, ...args2])\n  }\n}\n\n// 使用示例\nconst add = (a: number, b: number, c: number) => a + b + c\nconst curriedAdd = curry(add)\ncurriedAdd(1)(2)(3)  // 6\ncurriedAdd(1, 2)(3)  // 6\n\n// 实际应用 - 日志函数\nconst log = curry((level: string, date: Date, msg: string) =>\n  `[${level}] ${date.toISOString()} - ${msg}`\n)\nconst errorLog = log('ERROR')(new Date())\nerrorLog('Something went wrong')",
    tags: ["柯里化", "curry", "函数式编程", "偏函数"],
  },
  {
    id: "js-24",
    title: "垃圾回收机制 (GC) 详解",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 3,
    question: "JavaScript 的垃圾回收机制是怎样的？V8 引擎采用了哪些 GC 算法？",
    answer: "GC 核心概念：自动管理内存，释放不再使用的对象占用的内存。\n\n判断可达性：从根对象（全局对象、栈中的变量）出发，无法到达的对象即为垃圾。\n\nV8 主要算法：\n\n1. 分代回收：\n   - 新生代（Young Generation）：存活时间短的对象\n     - Scavenge 算法（复制算法）：From → To\n     - 存活的对象被提升到老生代\n   - 老生代（Old Generation）：存活时间长的对象\n     - 标记-清除（Mark-Sweep）\n     - 标记-整理（Mark-Compact）：解决碎片\n\n2. Orinoco 优化（并行、并发、增量 GC）：\n   - 并行 GC：多线程同时执行 GC\n   - 增量标记：将标记过程拆分，与 JS 交替执行\n   - 并发 GC：GC 线程与 JS 线程完全并发\n\n3. 弱引用：WeakMap/WeakSet 的键不影响 GC",
    tags: ["GC", "垃圾回收", "V8", "内存管理"],
  },
  {
    id: "js-25",
    title: "Hoisting（变量提升）全面解析",
    category: "javascript",
    subCategory: "核心概念",
    difficulty: 1,
    question: "什么是变量提升？函数提升和变量提升的区别是什么？",
    answer: "变量提升 (Hoisting)：JS 引擎在代码执行前，会将变量声明和函数声明提升到作用域的顶部。\n\nvar 的提升：\n- 声明提升，初始化为 undefined\n- 可在声明前访问（值为 undefined）\n\nlet/const 的提升：\n- 声明提升，但不初始化（TDZ）\n- 声明前访问报 ReferenceError\n\n函数提升：\n- 函数声明：整体提升，可在声明前调用\n- 函数表达式：只有变量提升（遵循变量规则）\n\n提升优先级：\n- 函数声明 > 变量声明\n- 同名函数声明，后者覆盖前者",
    codeExample: "// 函数声明提升\nfoo() // 'hello'\nfunction foo() { console.log('hello') }\n\n// 函数表达式不提升\nbar() // TypeError: bar is not a function\nvar bar = function() { console.log('world') }\n\n// 优先级示例\nconsole.log(typeof fn) // 'function'\nvar fn = 1\nfunction fn() {}",
    tags: ["Hoisting", "变量提升", "函数提升", "TDZ"],
  }
]
