import type { KnowledgePoint } from '../types'

export const typescriptData: KnowledgePoint[] = [
  {
    id: "ts-01",
    title: "TypeScript 的核心优势与类型系统",
    category: "typescript",
    difficulty: 1,
    question: "TypeScript 相比 JavaScript 有哪些核心优势？它的类型系统有什么特点？",
    answer: "核心优势：\n1. 静态类型检查：编译时发现类型错误，减少运行时 bug\n2. 更好的 IDE 支持：智能提示、自动补全、重构\n3. 代码可读性与可维护性：类型即文档\n4. 大型项目必备：接口定义清晰\n\n类型系统特点：\n- 结构化类型系统（鸭子类型），不是名义类型\n- 类型推断：自动推导变量类型\n- 类型守卫：typeof, instanceof, in, 自定义守卫\n- 联合类型 (|) 与交叉类型 (&)\n- 泛型编程\n- 条件类型与映射类型",
    codeExample: "// 类型推断\nlet x = 3  // x: number\nconst arr = [1, 'a']  // arr: (number | string)[]\n\n// 联合类型\ntype Status = 'loading' | 'success' | 'error'\n\n// 类型守卫\nfunction isString(val: unknown): val is string {\n  return typeof val === 'string'\n}",
    tags: ["TypeScript", "类型系统"],
  },
  {
    id: "ts-02",
    title: "泛型 (Generics) 的使用场景",
    category: "typescript",
    difficulty: 2,
    question: "什么是泛型？常见的泛型使用场景有哪些？",
    answer: "泛型：在定义时不指定具体类型，使用时再确定，实现类型参数化。\n\n常见场景：\n1. 泛型函数：入参与返回值类型关联\n2. 泛型接口/类型别名：容器、数据结构\n3. 泛型约束 (extends)：限制类型参数范围\n4. 泛型工具类型：Partial, Pick, Record 等\n5. 泛型类：通用数据结构\n\n常见泛型工具：\n- Partial<T>：所有属性可选\n- Required<T>：所有属性必填\n- Pick<T, K>：选取部分属性\n- Omit<T, K>：排除部分属性\n- Record<K, V>：构建 key-value 对象\n- ReturnType<T>：提取函数返回类型",
    codeExample: "// 泛型函数\nfunction identity<T>(arg: T): T {\n  return arg\n}\n\n// 泛型约束\nfunction getProperty<T, K extends keyof T>(obj: T, key: K) {\n  return obj[key]\n}\n\n// 泛型接口\ninterface ApiResponse<T> {\n  code: number\n  data: T\n  message: string\n}\n\ntype UserResponse = ApiResponse<{ id: number; name: string }>",
    tags: ["TypeScript", "泛型", "Generics"],
  },
  {
    id: "ts-03",
    title: "interface 与 type 的区别",
    category: "typescript",
    difficulty: 1,
    question: "TypeScript 中 interface 和 type 有什么区别？什么场景用哪个？",
    answer: "相同点：\n- 都能描述对象形状\n- 都能被 extends / implements\n- 都支持泛型\n\n区别：\n1. 扩展方式：interface 用 extends，type 用 &\n2. 声明合并：interface 同名会自动合并（适合库扩展），type 不行\n3. 类型范围：type 可以定义联合类型、基本类型别名、元组等，interface 不行\n4. 计算：type 支持条件类型、映射类型，interface 不行\n\n选择建议：\n- 定义对象结构 → interface（声明合并、更直观）\n- 定义联合类型、工具类型、映射类型 → type\n- React Props/State → interface\n- 项目统一风格即可",
    codeExample: "// interface - 声明合并\ninterface Window {\n  myCustomProp: string\n}\nwindow.myCustomProp // OK\n\n// type - 更灵活\ntype Status = 'active' | 'inactive'  // 联合类型\ntype Nullable<T> = T | null          // 工具类型\ntype Point = [number, number]        // 元组",
    tags: ["TypeScript", "interface", "type", "类型定义"],
  },
  {
    id: "ts-04",
    title: "类型推断 (Type Inference) 详解",
    category: "typescript",
    difficulty: 2,
    question: "TypeScript 的类型推断规则是怎样的？什么情况下需要显式标注类型？",
    answer: "推断场景：\n1. 变量初始化：let x = 3 → number\n2. 函数返回值：自动推断\n3. 解构赋值：从源类型推断\n4. 默认参数：从默认值推断\n5. 对象字面量：推断属性类型\n\n需要显式标注的情况：\n1. 函数参数（不会自动推断）\n2. 复杂的联合类型\n3. any 需要收窄时\n4. 函数重载\n5. 外部接口/公共 API（提升可读性）\n\n最佳实践：\n- 能推断就不写（减少冗余）\n- 函数参数必须写\n- 公共 API 返回值建议写\n- 复杂类型用 type alias 提取",
    codeExample: "// 自动推断\nconst nums = [1, 2, 3]  // number[]\nconst obj = { x: 1, y: 'a' }  // { x: number; y: string }\n\n// 需要显式标注\nfunction add(a: number, b: number): number {\n  return a + b\n}\n\n// satisfies 精确推断\nconst config = {\n  port: 3000,\n  host: 'localhost'\n} satisfies Record<string, string | number>\n// config.port 仍是 number，不是 string | number",
    tags: ["TypeScript", "类型推断", "satisfies"],
  },
  {
    id: "ts-05",
    title: "条件类型与 infer 关键字",
    category: "typescript",
    difficulty: 3,
    question: "TypeScript 条件类型怎么用？infer 关键字的作用是什么？",
    answer: "条件类型：根据条件选择类型，类似三元运算符。\nT extends U ? X : Y\n\ninfer：在条件类型中推断类型变量。\n- 只能在 extends 子句中使用\n- 可推断函数参数/返回值类型\n- 可推断 Promise 内部类型\n- 可推断数组元素类型\n\n常见内置条件类型：\n- Extract<T, U>：从 T 中提取可赋值给 U 的类型\n- Exclude<T, U>：从 T 中排除可赋值给 U 的类型\n- NonNullable<T>：排除 null/undefined\n- ReturnType<T>：获取函数返回类型\n- Parameters<T>：获取函数参数类型元组",
    codeExample: "// 提取 Promise 内部类型\ntype Unpacked<T> = T extends Promise<infer U> ? U : T\ntype Result = Unpacked<Promise<string>>  // string\n\n// 提取函数返回值\ntype GetReturnType<T> = T extends (...args: any[]) => infer R ? R : never\ntype FnReturn = GetReturnType<() => number>  // number\n\n// 提取数组第一个元素\ntype Head<T extends any[]> = T extends [infer F, ...any[]] ? F : never\ntype First = Head<[1, 2, 3]>  // 1",
    tags: ["TypeScript", "条件类型", "infer", "高级类型"],
  },
  {
    id: "ts-06",
    title: "装饰器 (Decorators) 详解",
    category: "typescript",
    difficulty: 2,
    question: "TypeScript 装饰器有哪几种？各自的使用场景？",
    answer: "装饰器类型：\n1. 类装饰器：修改/替换类定义\n2. 方法装饰器：修改/替换方法\n3. 属性装饰器：监听属性定义\n4. 参数装饰器：监听参数声明\n\n使用场景：\n- 依赖注入（Angular, NestJS）\n- 路由注册（@Get, @Post）\n- 权限校验（@RequireAuth）\n- 日志/性能监控（@Log, @Timer）\n- 数据验证（@IsString, @IsEmail）\n\n现状：\n- Stage 3 装饰器已通过（与旧版不兼容）\n- TypeScript 5.0+ 支持新装饰器\n- 需启用 experimentalDecorators（旧版）",
    codeExample: "// 类装饰器\nfunction Loggable<T extends { new (...args: any[]): {} }>(constructor: T) {\n  return class extends constructor {\n    created = new Date().toISOString()\n  }\n}\n\n@Loggable\nclass UserService {\n  // 自动添加 created 属性\n}\n\n// 方法装饰器\nfunction debounce(delay: number) {\n  return function(target: any, key: string, descriptor: PropertyDescriptor) {\n    const original = descriptor.value\n    let timer: any\n    descriptor.value = function(...args: any[]) {\n      clearTimeout(timer)\n      timer = setTimeout(() => original.apply(this, args), delay)\n    }\n  }\n}",
    tags: ["TypeScript", "装饰器", "Decorators"],
  },
  {
    id: "ts-07",
    title: "枚举类型 (Enum) 与替代方案",
    category: "typescript",
    difficulty: 1,
    question: "TypeScript 的 enum 有什么问题？有哪些更好的替代方案？",
    answer: "数字枚举的问题：\n- 反向映射导致类型不安全\n- 编译产物较多（生成 IIFE）\n- tree-shaking 不友好\n\n字符串枚举：\n- 无反向映射\n- 编译为对象映射\n- 仍然不如 const enum 轻量\n\nconst enum：\n- 编译时内联，零运行时开销\n- isolatedModules 模式下不可用\n\n替代方案：\n1. as const 对象（最推荐）\n2. 联合字面量类型\n3. 满足映射需求时用 Record",
    codeExample: "// ❌ 数字枚举\nenum Direction { Up, Down, Left, Right }\n// 编译为双向映射对象\n\n// ✅ 推荐：as const\nconst Direction = {\n  Up: 'UP',\n  Down: 'DOWN',\n  Left: 'LEFT',\n  Right: 'RIGHT',\n} as const\ntype Direction = typeof Direction[keyof typeof Direction]\n// Direction = 'UP' | 'DOWN' | 'LEFT' | 'RIGHT'\n\n// ✅ 联合字面量\ntype Status = 'active' | 'inactive' | 'pending'",
    tags: ["TypeScript", "enum", "as const", "枚举"],
  },
  {
    id: "ts-08",
    title: "TypeScript 严格模式与常见配置",
    category: "typescript",
    difficulty: 1,
    question: "TypeScript tsconfig.json 中 strict 相关配置有哪些？",
    answer: "strict: true 开启所有严格检查，包含：\n\n1. strictNullChecks：null/undefined 不能赋值给其他类型\n2. noImplicitAny：禁止隐式 any\n3. strictFunctionTypes：函数参数逆变检查\n4. strictBindCallApply：bind/call/apply 类型检查\n5. strictPropertyInitialization：类属性必须初始化\n6. noImplicitThis：禁止 this 隐式为 any\n7. alwaysStrict：输出 'use strict'\n\n其他推荐配置：\n- noUnusedLocals：未使用的局部变量报错\n- noUnusedParameters：未使用的参数报错\n- noFallthroughCasesInSwitch：switch 穿透检查\n- noUncheckedIndexedAccess：索引签名可能为 undefined\n- exactOptionalPropertyTypes：可选属性精确类型",
    codeExample: "// strictNullChecks\nconst el = document.getElementById('app')\n// el: HTMLElement | null\nel.innerHTML = '' // ❌ Object is possibly null\nel?.innerHTML = '' // ✅\n\n// noUncheckedIndexedAccess\nconst arr = [1, 2, 3]\nconst val = arr[10]  // number | undefined\n\n// strictPropertyInitialization\nclass User {\n  name: string  // ❌ 必须初始化\n  age!: number   // ✅ ! 断言\n  constructor() { this.name = 'unknown' }\n}",
    tags: ["TypeScript", "strict", "tsconfig", "配置"],
  },
  {
    id: "ts-09",
    title: "映射类型与模板字面量类型",
    category: "typescript",
    difficulty: 3,
    question: "TypeScript 映射类型和模板字面量类型怎么使用？",
    answer: "映射类型：基于旧类型创建新类型，遍历属性。\n- [K in keyof T]：遍历所有键\n- as：键重映射（TS 4.1+）\n- 修饰符：+? / -? / +readonly / -readonly\n\n模板字面量类型：基于模板字符串生成类型。\n- 支持联合类型展开\n- 与映射类型结合可自动生成 getter/setter\n\n应用场景：\n- 将所有属性变为可选/只读\n- 自动生成事件名\n- 从路由配置生成类型安全的路径\n- 数据库表字段映射",
    codeExample: "// 映射类型：可选化\ntype MyPartial<T> = { [K in keyof T]?: T[K] }\n\n// 键重映射\ntype Getters<T> = {\n  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K]\n}\ninterface User { name: string; age: number }\ntype UserGetters = Getters<User>\n// { getName: () => string; getAge: () => number }\n\n// 模板字面量\ntype EventName = `on${Capitalize<'click' | 'focus'>}`\n// 'onClick' | ' onFocus'",
    tags: ["TypeScript", "映射类型", "模板字面量", "高级类型"],
  },
  {
    id: "ts-10",
    title: "TypeScript 声明文件 (.d.ts)",
    category: "typescript",
    difficulty: 2,
    question: "如何为 JavaScript 库编写类型声明文件？",
    answer: "声明文件作用：为没有类型的 JS 库提供类型信息。\n\n查找声明：\n- DefinitelyTyped (@types/xxx)\n- 库自带 (package.json types/typings 字段)\n- 本地 .d.ts 文件\n\n编写声明文件：\n1. 全局声明：declare function/class/var\n2. 模块声明：declare module 'xxx'\n3. 三斜线指令：/// <reference types=\"...\" />\n4. 命名空间：declare namespace\n\n常用策略：\n- 简单场景：declare module 'lib-name'\n- 复杂场景：编写完整接口定义\n- 混合：扩展现有类型（declare module 扩展）\n\n发布：\n- 发布到 @types 组织（DefinitelyTyped）\n- 或随包自带 types",
    codeExample: "// global.d.ts - 全局类型\ndeclare function $(selector: string): HTMLElement\ndeclare namespace $ {\n  function ajax(settings: AjaxSettings): Promise<any>\n}\n\n// shims.d.ts - 模块声明\ndeclare module 'some-lib' {\n  export function doSomething(x: string): number\n  export interface Config { debug?: boolean }\n}\n\n// 扩展 Window\ninterface Window {\n  myApp: { version: string }\n}",
    tags: ["TypeScript", "声明文件", ".d.ts", "DefinitelyTyped"],
  },
  {
    id: "ts-11",
    title: "TypeScript 5.0+ 新特性",
    category: "typescript",
    difficulty: 1,
    question: "TypeScript 5.x 有哪些重要的新特性？",
    answer: "TypeScript 5.0：\n- 装饰器（Stage 3 标准，与旧版不兼容）\n- const 类型参数：<const T>\n- 枚举增强：所有枚举都是联合枚举\n- module: bundler 模式\n- satisfies 运算符（4.9 引入）\n\nTypeScript 5.1：\n- JSX 增强支持\n- 不再要求 JSX 元素有构造签名\n\nTypeScript 5.2：\n- using 声明（显式资源管理）\n- 装饰器元数据支持\n\nTypeScript 5.3：\n- import type 优化\n- switch true 类型收窄\n\nTypeScript 5.4+：\n- NoInfer 工具类型\n- 闭包中变量类型收窄\n- Object.groupBy / Map.groupBy 类型支持",
    codeExample: "// const 类型参数\nfunction createRoute<const T extends readonly string[]>(paths: T) {\n  return paths\n}\nconst route = createRoute(['/home', '/about'])\n// route: readonly [\"/home\", \"/about\"] (不是 string[])\n\n// satisfies\nconst palette = {\n  red: [255, 0, 0],\n  green: '#00ff00',\n} satisfies Record<string, string | number[]>\npalette.red[0]  // number (而非 string | number)\n\n// using (显式资源管理)\nusing file = openFile('data.txt')\n// 块结束时自动调用 file[Symbol.dispose]()",
    tags: ["TypeScript", "新特性", "TS5", "satisfies"],
  },
  {
    id: "ts-12",
    title: "TypeScript 中的 any/unknown/never",
    category: "typescript",
    difficulty: 2,
    question: "any、unknown、never 三种类型的区别和各自的使用场景？",
    answer: "any：\n- 放弃类型检查，任何操作都合法\n- 类型安全的天敌\n- 场景：快速原型、迁移 JS 代码\n\nunknown：\n- 类型安全的 any，必须收窄后才能使用\n- 所有值都可赋给 unknown\n- unknown 值只能赋给 any/unknown\n- 场景：不确定类型时的安全选择\n\nnever：\n- 不可能存在的类型\n- 函数抛出异常或无限循环时返回 never\n- 联合类型收窄到空时变为 never\n- 场景：穷尽检查 (exhaustive check)\n\nvoid：\n- 函数没有返回值\n- 与 never 区别：void 可以返回 undefined，never 不会返回",
    codeExample: "// unknown - 必须收窄\nfunction process(val: unknown) {\n  if (typeof val === 'string') {\n    val.toUpperCase()  // ✅ 收窄后可用\n  }\n  val.toUpperCase()  // ❌ Object is of type 'unknown'\n}\n\n// never - 穷尽检查\ntype Shape = 'circle' | 'square'\nfunction area(shape: Shape) {\n  switch (shape) {\n    case 'circle': return Math.PI\n    case 'square': return 1\n    default:\n      const _exhaustive: never = shape  // 如果有遗漏会报错\n  }\n}",
    tags: ["TypeScript", "any", "unknown", "never", "类型安全"],
  }
]
