import type { KnowledgePoint } from '../types'

export const reactData: KnowledgePoint[] = [
  {
    id: "react-01",
    title: "React Hooks 使用规则与原理",
    category: "react",
    difficulty: 2,
    question: "React Hooks 的使用规则是什么？为什么不能在条件语句中使用 Hooks？",
    answer: 'React Hooks 最核心的规则就两条：只在函数组件或自定义 Hook 顶层调用、不要在条件/循环里用。违反这条规则会导致状态混乱——因为 React 是靠调用顺序来关联 Hook 和对应的状态的。你在条件里用 Hook，调用顺序变了，React 就会把状态 A 的数据给到状态 B。\n\n原理上，React 内部用链表来存每个组件的 Hooks 状态。每次渲染时按顺序遍历这个链表——第一个 useState 对链上第一个节点、第二个对第二个节点，以此类推。\n\nuseState 返回 [state, setState]，setState 可以传值也可以传函数（prev => prev + 1）。注意 setState 是异步的——React 18 的 automatic batching 会把多个 setState 合并成一次渲染。\n\n我实际踩过的坑：useEffect 里忘了清定时器、依赖数组漏了依赖导致 stale closure、useCallback 包了所有函数反而降低了性能（缓存本身也有开销）。',
    codeExample: "// 错误：条件中使用 Hook\nfunction Component({ flag }: { flag: boolean }) {\n  if (flag) {\n    const [state, setState] = useState(0)  // 破坏顺序！\n  }\n  const [other, setOther] = useState('')\n}\n\n// 正确：条件逻辑放在 Hook 内部\nfunction Component({ flag }: { flag: boolean }) {\n  const [state, setState] = useState(0)\n  const [other, setOther] = useState('')\n  if (flag) { /* 使用 state */ }\n}",
    tags: ["React", "Hooks", "规则"],
  },
  {
    id: "react-02",
    title: "useEffect 与 useLayoutEffect 的区别",
    category: "react",
    difficulty: 2,
    question: "useEffect 和 useLayoutEffect 有什么区别？各自在什么时候使用？",
    answer: 'useEffect 和 useLayoutEffect 最大的区别是执行时机。\n\nuseEffect 在浏览器完成布局和绘制之后异步执行——用户已经看到渲染结果了，你才执行副作用。所以它不阻塞浏览器渲染，适合大多数副作用：数据请求、订阅、设置定时器。\n\nuseLayoutEffect 在浏览器布局之后、绘制之前同步执行——浏览器已经算好了元素的尺寸和位置，但还没画到屏幕上。你可以在这个时机读 DOM 尺寸并做同步修改，浏览器会把你的修改和原本的渲染合并成一帧画出来——用户不会看到闪烁。适合需要"先量再改"的场景：测量 DOM 尺寸后调整布局、根据内容高度定位弹出框、避免滚动位置跳动。\n\n一个实际案例：tooltip 需要出现在正确的屏幕位置——先用 useLayoutEffect 读元素位置算出 tooltip 应该在哪，然后用 useState 更新位置。如果改用 useEffect，用户会先看到 tooltip 在默认位置闪一下再跳到正确位置。\n\n面试官追问：大多数场景能不能直接用 useLayoutEffect？不建议。因为它是同步执行的，会阻塞浏览器绘制——只在确实需要在绘制前完成 DOM 操作的时候用它，其他全部 useEffect。',
    codeExample: "// useEffect：数据请求（不阻塞渲染）\nuseEffect(() => {\n  fetchData().then(setData)\n}, [])\n\n// useLayoutEffect：测量布局（避免闪烁）\nuseLayoutEffect(() => {\n  const { height } = ref.current.getBoundingClientRect()\n  setHeight(height)\n}, [])",
    tags: ["React", "useEffect", "useLayoutEffect", "Hooks"],
  },
  {
    id: "react-03",
    title: "React Fiber 架构",
    category: "react",
    difficulty: 3,
    question: "请解释 React Fiber 架构的设计思想和工作原理。",
    answer: 'React Fiber 这个话题简历面试里已经详细讲过了，这里补充一些容易被追问的细节。\n\nFiber 的核心是让渲染变得"可中断"。每个 Fiber 节点用链表连接——child（第一个子节点）、sibling（兄弟节点）、return（父节点）。链表的好处是遍历到一半可以停，下次接着走。递归做不到这一点。\n\n时间切片是 Scheduler 的核心：每 5ms 让出主线程给浏览器。React 用了 MessageChannel 实现（不是 requestIdleCallback，因为 Safari 不支持而且精度不够）。每个时间片到期后检查有没有更高优先级任务，有就中断当前渲染。\n\nLane 模型是优先级系统——31 种优先级车道。用户输入 lane=1（最高），普通 setState lane=4，useTransition lane=16（最低）。\n\n和 Vue 3 的区别：React 是运行时"不知道哪些变了所以全量 diff 再优化"，Vue 是编译时"知道哪些会变所以精准更新"。两种哲学没有对错。',
    codeExample: "// Fiber Node 结构（简化）\ninterface FiberNode {\n  type: string | Function  // 组件类型\n  stateNode: any           // DOM 节点\n  return: FiberNode | null // 父节点\n  child: FiberNode | null  // 第一个子节点\n  sibling: FiberNode | null// 下一个兄弟节点\n  alternate: FiberNode | null // 对应另一棵树\n  flags: number            // 副作用标记\n  lanes: number            // 优先级\n}",
    tags: ["React", "Fiber", "架构"],
  },
  {
    id: "react-04",
    title: "React 性能优化策略",
    category: "react",
    difficulty: 2,
    question: "React 中有哪些常用的性能优化手段？",
    answer: 'React 性能优化的方法论：先从三个维度排查，再对症下药。\n\n渲染优化排在首位。用 React DevTools Profiler 录一段操作，看哪些组件渲染了、渲染了多久。纯展示组件但渲染了且 props 没变？上 React.memo。但注意 memo 默认浅比较——如果每次传的都是新创建的对象（style={{}} 或 onClick={() => {}}），memo 等于没包。配合 useCallback 和 useMemo 才有效。\n\n计算密集型丢给 useMemo（缓存高开销结果）或 Web Worker（不阻塞主线程）。虚拟列表处理长列表——用@tanstack/react-virtual，只渲染可视区的几十条。\n\n网络层面：代码分割（React.lazy + Suspense）、图片懒加载、关键 CSS 内联、Bundle 体积监控。\n\n优化量化：React DevTools Profiler 的 render duration 对比前后，Chrome Performance 面板看 Long Task（>50ms）数量变化。\n\n面试官追问 useMemo 和 useCallback 能无止境用吗？不行，缓存本身有开销。只在真正需要的地方用——传给 memo 子组件的 props、高计算开销的值。',
    codeExample: "// React.memo + useCallback 组合\nconst ExpensiveChild = React.memo(({ onClick }: { onClick: () => void }) => {\n  return <button onClick={onClick}>Click</button>\n})\n\nfunction Parent() {\n  const [count, setCount] = useState(0)\n  const handleClick = useCallback(() => {\n    setCount(c => c + 1)\n  }, [])\n  return <ExpensiveChild onClick={handleClick} />\n}",
    tags: ["React", "性能优化", "memo", "useMemo"],
  },
  {
    id: "react-05",
    title: "状态管理：Redux vs Zustand",
    category: "react",
    difficulty: 2,
    question: "请对比 Redux Toolkit 和 Zustand 的差异与使用场景。",
    answer: 'Redux 和 Zustand 我都用过。Redux 太重了——action type、action creator、reducer、middleware，一个简单的布尔值切换要写三处代码。Zustand 是这个问题的解药。\n\nZustand 的核心优势：API 极简——create((set) => ({ count: 0, inc: () => set(s => ({ count: s.count + 1 })) }))。不需要 Provider 包裹，不需要 action type，不需要 reducer。组件里 const count = useStore(s => s.count)，selector 默认浅比较。\n\nRedux 什么时候还用？大型团队需要强约束和规范、中间件生态（redux-saga）、时间旅行调试——这些场景 Redux 仍有优势。但 Zustand + immer 已经覆盖了大部分需求。\n\n选型建议：新项目优先 Zustand。服务器状态（API 数据）一律用 TanStack Query，不要放 Redux 或 Zustand 里。',
    codeExample: "// Zustand 示例\nimport { create } from 'zustand'\n\nconst useBearStore = create<BearState>((set) => ({\n  bears: 0,\n  increase: () => set((state) => ({ bears: state.bears + 1 }))\n}))\n\nfunction Counter() {\n  const bears = useBearStore((s) => s.bears)\n  const increase = useBearStore((s) => s.increase)\n  return <button onClick={increase}>{bears}</button>\n}",
    tags: ["React", "Redux", "Zustand", "状态管理"],
  },
  {
    id: "react-06",
    title: "React 事件机制与合成事件",
    category: "react",
    difficulty: 2,
    question: "React 的合成事件 (SyntheticEvent) 和原生事件有什么区别？",
    answer: 'React 的合成事件（SyntheticEvent）是对原生事件的跨浏览器包装，补平了不同浏览器的事件 API 差异。\n\n17 版之前 React 把所有事件都委托到 document 上，17 之后改成了 root 节点。这意味着 React 应用的根节点容器捕捉所有事件，而不是 document——让 React 应用和非 React 应用在同一个页面里更好共存。\n\n合成事件有个特点：事件对象在回调执行完后会被回收重用——如果你在异步操作里访问 event，会发现它的属性都是 null。需要异步访问的话直接赋值给变量就行。\n\nReact 事件和原生事件混用要注意执行顺序——尽量不要在同一个项目里混用。面试官追问：合成事件的 e.stopPropagation() 阻止的是 React 合成事件树里的冒泡，不阻止原生事件。需同时阻止调 e.nativeEvent.stopImmediatePropagation()。',
    tags: ["React", "合成事件", "事件机制"],
  },
  {
    id: "react-07",
    title: "useRef 与 useState 的区别",
    category: "react",
    difficulty: 1,
    question: "useRef 和 useState 有什么区别？什么场景用 useRef？",
    answer: 'useRef 和 useState 都会在组件多次渲染间保持数据，但触发机制完全不同。\n\nuseState 的值变了，组件重新渲染。适合影响 UI 的数据：表单输入、开关状态、列表数据。\n\nuseRef 的值变了，组件不重新渲染。适合不需要驱动 UI 的数据：存储 timer ID、保存 DOM 引用、缓存上一次的值（比较前后变化）。ref 的值存在 .current 里，你可以随便改，React 完全不管。\n\n常见用法：用 ref 存"上一次的 props"做对比（在 useEffect 里 prevRef.current = currentProps）、用 ref 做"标志位"判断组件是否已挂载、存 timer ID 以便清理。\n\n面试官追问：为什么不都用 useState？因为不必要的渲染影响性能。比如一个 timer ID——它变不变都不需要在 UI 上体现，用 useState 会导致每次改 timer 都触发一次无意义的渲染。这就是 useRef 的价值。',
    codeExample: "// 访问 DOM\nconst inputRef = useRef<HTMLInputElement>(null)\ninputRef.current?.focus()\n\n// 保存前一次渲染的值\nfunction usePrevious<T>(value: T) {\n  const ref = useRef<T>(value)\n  useEffect(() => { ref.current = value })\n  return ref.current\n}\n\n// 存储 timer\nconst timerRef = useRef<number>()\ntimerRef.current = setInterval(() => {}, 1000)\nclearInterval(timerRef.current)",
    tags: ["React", "useRef", "useState", "Hooks"],
  },
  {
    id: "react-08",
    title: "React 组件设计模式",
    category: "react",
    difficulty: 2,
    question: "常见的 React 组件设计模式有哪些？",
    answer: 'React 组件设计模式的核心原则：组合优于继承。\n\n组合模式——用 children prop 和 slot 模式。一个 Card 组件，标题走 prop，正文走 children。灵活度比继承高得多。\n\n容器组件与展示组件：数据获取和逻辑在容器组件里，UI 渲染在展示组件里。展示组件只接收 props，测试和复用都很方便。不过 Hooks 出来以后这种模式减少了——自定义 Hooks 可以替代容器组件。\n\nRender Props：组件的 prop 传一个函数，函数返回 JSX。子组件调用这个函数并把内部数据传给父组件。Hooks 出现后 Render Props 也减少了。\n\n高阶组件（HOC）：接收组件返回新组件。嵌套多了变成"包装地狱"，TypeScript 类型推断也痛苦。\n\n现在主推 Custom Hooks——逻辑复用最灵活的方式。我的原则：能用 Custom Hook 就别用 HOC，能用组合就别用继承。',
    codeExample: "// 自定义 Hook 提取逻辑\nfunction useWindowSize() {\n  const [size, setSize] = useState({ w: 0, h: 0 })\n  useEffect(() => {\n    const handler = () => setSize({ w: innerWidth, h: innerHeight })\n    handler()\n    window.addEventListener('resize', handler)\n    return () => window.removeEventListener('resize', handler)\n  }, [])\n  return size\n}\n\n// 组合模式\nfunction Layout({ sidebar, content }) {\n  return (\n    <div className=\"flex\">\n      <aside>{sidebar}</aside>\n      <main>{content}</main>\n    </div>\n  )\n}",
    tags: ["React", "设计模式", "HOC", "组合", "自定义Hook"],
  },
  {
    id: "react-09",
    title: "React 18 新特性",
    category: "react",
    difficulty: 2,
    question: "React 18 引入了哪些重要新特性？",
    answer: 'React 18 最大的改变是并发特性（Concurrent Features）。\n\nAutomatic Batching 影响最广——以前只在浏览器事件里多次 setState 会批量处理，setTimeout、Promise 里的不会。React 18 以后在任何地方多次 setState 都自动合并成一次渲染。\n\nuseTransition 让你把更新分成"紧急"和"不紧急"——用户打字是紧急的，搜索结果渲染可以不那么急。用 startTransition 包裹非紧急更新，React 在空闲时才处理。\n\nuseDeferredValue 是 useTransition 的变体——不是包更新，而是让某个值"延迟更新"。适合搜索结果列表——保持输入框流畅，列表可以慢半拍。\n\nSuspense 在 18 里正式可以和 SSR 配合做流式传输——哪些组件的数据还在加载就显示 fallback，其他的先渲染，首屏时间大幅缩短。\n\nuseId 生成唯一 ID（服务端和客户端一致），适合表单关联。Server Components 是 18 的试验特性，正式在 Next.js 13 落地。',
    codeExample: "// useTransition\nconst [isPending, startTransition] = useTransition()\n\nfunction handleChange(e) {\n  // 紧急更新：输入框\n  setInputValue(e.target.value)\n  // 非紧急更新：搜索结果\n  startTransition(() => {\n    setSearchQuery(e.target.value)\n  })\n}\n\n// useDeferredValue\nconst deferredQuery = useDeferredValue(query)\n<SearchResults query={deferredQuery} />",
    tags: ["React18", "Concurrent", "useTransition", "批处理"],
  },
  {
    id: "react-10",
    title: "Next.js 核心概念",
    category: "react",
    difficulty: 2,
    question: "Next.js 的核心特性有哪些？App Router 带来了什么改变？",
    answer: 'Next.js 现在已经是 React 生态的标配框架。\n\nApp Router（13+）基于文件系统嵌套路由。app/ 文件夹下每个 page.tsx 就是路由页面，layout.tsx 是共享布局（嵌套的），loading.tsx 自动当 Suspense fallback。\n\nSSR/SSG/ISR/CSR 灵活切换是 Next.js 最大的卖点。静态页面用 ISR（设 revalidate 定时刷新），动态页面用 SSR，纯客户端交互用 CSR。\n\nServer Components（13+）：默认所有组件服务端渲染，只有加 \'use client\' 的才打包到客户端。服务端组件可以直接访问数据库，代码不会暴露到客户端。\n\n中间件（Middleware）在请求到达页面之前做鉴权、重定向、A/B testing。\n\n学习曲线确实比纯 React 陡峭不少——服务端/客户端边界判断、缓存策略、部署环境兼容性都是坑。但 SEO 和首屏性能提升对商业项目是刚需。',
    codeExample: "// app/page.tsx (Server Component)\nexport default async function Page() {\n  const data = await fetch('https://api.example.com/data')\n  const posts = await data.json()\n  return <PostList posts={posts} />\n}\n\n// app/components/Counter.tsx\n'use client'\nimport { useState } from 'react'\nexport default function Counter() {\n  const [count, setCount] = useState(0)\n  return <button onClick={() => setCount(c => c + 1)}>{count}</button>\n}",
    tags: ["Next.js", "App Router", "RSC", "SSR"],
  },
  {
    id: "react-11",
    title: "React 闭包陷阱 (Stale Closure)",
    category: "react",
    difficulty: 2,
    question: "React 中的闭包陷阱是什么？如何解决？",
    answer: '闭包陷阱（Stale Closure）是 React Hooks 里最常见的"感觉没 bug 但就是不对"的问题。\n\n本质：useEffect 或 useCallback 里引用的外部变量，其实是"当时渲染时"的快照，不是最新值。每次渲染都创建新的函数作用域和新的闭包。\n\n经典场景：setInterval 里读 count——useEffect 只在挂载执行一次，那个闭包里的 count 就是初始值 0，之后重渲染产生的 count 在另一个闭包里。所以你看到 count 永远是 0。\n\n解决方案：用 useRef 存最新值（ref.current = count，每次渲染更新），在 setInterval 里读 ref.current。或者用函数式更新 setCount(c => c + 1)。或者把 count 加到依赖数组——但定时器会频繁重建。\n\n面试官追问：Vue 3 为什么没这个问题？因为 Vue 的响应式是自动追踪——ref 被包装成对象，你访问 .value 总是拿到最新值，不是某个闭包的快照。这是 React 和 Vue 在响应式设计思路上的根本差异。',
    codeExample: "// ❌ 闭包陷阱\nconst [count, setCount] = useState(0)\nuseEffect(() => {\n  const id = setInterval(() => {\n    setCount(count + 1)  // 永远是 0 + 1 = 1\n  }, 1000)\n  return () => clearInterval(id)\n}, [])\n\n// ✅ 函数式更新\nuseEffect(() => {\n  const id = setInterval(() => {\n    setCount(c => c + 1)  // 始终用最新值\n  }, 1000)\n  return () => clearInterval(id)\n}, [])\n\n// ✅ useRef 保存最新值\nconst countRef = useRef(count)\ncountRef.current = count",
    tags: ["React", "闭包陷阱", "useState", "useEffect"],
  },
  {
    id: "react-12",
    title: "React 服务端组件 (RSC) 原理",
    category: "react",
    difficulty: 3,
    question: "React Server Components 的工作原理是什么？",
    answer: 'React Server Components (RSC) 简历深度面试已详细讲过了，这里补充几个细节。\n\nRSC Payload 不是 HTML、不是 JSON，是 React 专有序列化格式——包含组件树结构和 props 数据，但不包含组件代码。客户端 React reconciler 收到后重建组件树，服务端组件代码永远不会出现在客户端 bundle 里。\n\n服务端组件不能直接用 useState、useEffect、onClick。需要交互的地方抽出一个客户端组件（\'use client\'），通过 children 或 props 传给服务端组件。\n\n"use client" 的边界规则：标记的不止自己，它 import 的子组件也全变客户端——除非这些子组件同时被另一个服务端组件 import。\n\n和 SSR 的区别：SSR 是把组件渲染成 HTML 发给浏览器，组件代码还是会打包到客户端做 hydration。RSC 是组件代码和渲染都在服务端完成，只发结果。两者可以结合——服务端渲染 RSC 成 HTML 再发给浏览器。',
    tags: ["React", "RSC", "Server Components", "服务端渲染"],
  },
{
    id: "react-13",
    title: "React setState 执行机制",
    category: "react",
    difficulty: 2,
    question: "React 中 setState 是同步还是异步的？React 18 有什么变化？",
    answer: 'setState 的异步特性不是因为 setTimeout，而是 React 的批处理机制。\n\nReact 18 Automatic Batching 后，所有 setState 都自动批处理——你在同一个事件处理函数里调三次 setCount，React 只触发一次重渲染。在当前执行上下文结束后统一处理——避免了中间状态的多次渲染。\n\nsetState 传函数 vs 传值的区别：传值用的是调用时的快照值；传函数拿的是 React 保证的最新值。连续多次 setCount(count + 1) 三次加的都是初始值，最终只加了 1。改成 setCount(prev => prev + 1) 就对了。\n\n和 Vue 的响应式对比：Vue 也是批处理的——同一事件循环改 10 次 ref，只触发一次组件更新。但 Vue 不需要你区分"传值还是传函数"——ref.value = newValue 永远拿到最新值。React 这种显式的函数式更新是一个常踩的坑。',
    tags: ["React", "setState", "批量更新", "React18"],
  },
  {
    id: "react-14",
    title: "React 生命周期详解",
    category: "react",
    difficulty: 2,
    question: "React 类组件和函数组件的生命周期分别有哪些？",
    answer: 'React 生命周期分三个阶段：挂载、更新、卸载。函数组件里对应的是 Hooks 用法。\n\n挂载阶段：组件创建并插入 DOM。useState 设初始值，useEffect(..., []) 模拟 componentDidMount——空依赖数组只在挂载执行一次。适合首次数据请求和事件绑定。\n\n更新阶段：props 或 state 变化触发渲染。useEffect(..., [dep]) 依赖变化就重新执行。useLayoutEffect 在 DOM 更新后、浏览器绘制前同步执行——适合需要量 DOM 尺寸的场景。\n\n卸载阶段：组件从 DOM 移除。useEffect 的 cleanup 函数模拟 componentWillUnmount——清定时器、取消请求、解绑事件、关连接。\n\n面试官追问：函数组件有 constructor 吗？没有。函数组件的函数体就是 render + constructor 的合体，每次渲染整个函数重新跑。这也是 Hooks 不能放条件语句里的原因——React 靠调用顺序关联状态。',
    codeExample: "// 函数组件生命周期等价\nuseEffect(() => {\n  // componentDidMount\n  fetchData()\n  return () => { /* componentWillUnmount */ }\n}, [])\n\nuseEffect(() => {\n  // componentDidUpdate (only count changed)\n  console.log('count changed:', count)\n}, [count])\n\nuseEffect(() => {\n  // render 后每次都执行\n})",
    tags: ["React", "生命周期", "useEffect", "类组件"],
  },
  {
    id: "react-15",
    title: "React refs 详解与应用",
    category: "react",
    difficulty: 1,
    question: "React refs 有几种使用方式？各自的应用场景？",
    answer: 'refs 在 React 有四种用法。\n\nDOM 访问最基础：const inputRef = useRef(null)，<input ref={inputRef} />，挂载后 inputRef.current 就是 DOM 节点。适合获取焦点、滚动定位、测量尺寸。\n\n存储可变值：ref 的值改了不触发重渲染——存 timer ID、上一次的值、是否已挂载的标志位。和 useState 的核心区别就是"改这个值要不要重渲染"。\n\n转发 refs（forwardRef）：子组件把外界传进来的 ref 绑定到自己的内部 DOM 元素。适合通用组件封装——比如一个 Input 组件，外部需要调 focus()。\n\n命令式句柄（useImperativeHandle）：配合 forwardRef，暴露自定义的方法给父组件——不直接暴露 DOM ref，而是暴露一个"代理对象"。比如子组件对外暴露 scrollTo(id) 方法，父组件调它但不知道内部实现。',
    codeExample: "// forwardRef + useImperativeHandle\nconst Input = forwardRef((props, ref) => {\n  const inputRef = useRef<HTMLInputElement>(null)\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current?.focus(),\n    clear: () => { if (inputRef.current) inputRef.current.value = '' }\n  }))\n  return <input ref={inputRef} />\n})\n\n// 父组件使用\nconst parentRef = useRef<{ focus: () => void }>(null)\nparentRef.current?.focus()",
    tags: ["React", "refs", "useRef", "forwardRef"],
  },
  {
    id: "react-16",
    title: "React 类组件 vs 函数组件",
    category: "react",
    difficulty: 1,
    question: "React 类组件和函数组件有什么区别？为什么推荐函数组件？",
    answer: '函数组件和类组件的核心区别在于"每次渲染是否创建新实例"。\n\n类组件的核心是"一个实例的多个生命周期"——this 是同一个，mounted 时和 updated 时是同一个 this 对象。\n\n函数组件的核心是"每次渲染都是一次全新的调用"——没有 this 累积，每次渲染产生新的闭包。这导致了闭包陷阱：你 effect 里引用的 props 是那次渲染的快照。\n\n函数组件的优势：更简洁、逻辑复用更灵活（自定义 Hooks）、更容易 Tree-shaking、TypeScript 推断更好。新代码全是函数组件。\n\n但理解类组件仍有意义：很多老项目有大量类组件需要维护、错误边界（Error Boundary）目前还只能用类组件的 componentDidCatch、一些面试官通过对比来考察深度理解。',
    tags: ["React", "类组件", "函数组件", "Hooks"],
  }
]
