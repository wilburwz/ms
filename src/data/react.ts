import type { KnowledgePoint } from '../types'

export const reactData: KnowledgePoint[] = [
  {
    id: "react-01",
    title: "React Hooks 使用规则与原理",
    category: "react",
    difficulty: 2,
    question: "React Hooks 的使用规则是什么？为什么不能在条件语句中使用 Hooks？",
    answer: "使用规则：\n1. 只能在函数组件顶层调用，不能在循环/条件/嵌套函数中\n2. 只能在 React 函数组件或自定义 Hook 中调用\n\n原因：React 依赖 Hooks 的调用顺序来关联内部状态。每次渲染时，Hooks 的调用顺序必须一致，React 才能正确地将状态与对应的 Hook 关联。\n\n内部原理：\n- 每个组件维护一个 Hooks 链表\n- 渲染时按顺序遍历链表\n- 如果在条件语句中使用，链表顺序会改变，导致状态错乱\n\n自定义 Hook：\n- 以 use 开头命名\n- 可组合内置 Hooks 提取复用逻辑",
    codeExample: "// 错误：条件中使用 Hook\nfunction Component({ flag }: { flag: boolean }) {\n  if (flag) {\n    const [state, setState] = useState(0)  // 破坏顺序！\n  }\n  const [other, setOther] = useState('')\n}\n\n// 正确：条件逻辑放在 Hook 内部\nfunction Component({ flag }: { flag: boolean }) {\n  const [state, setState] = useState(0)\n  const [other, setOther] = useState('')\n  if (flag) { /* 使用 state */ }\n}",
    tags: ["React", "Hooks", "规则"],
  },
  {
    id: "react-02",
    title: "useEffect 与 useLayoutEffect 的区别",
    category: "react",
    difficulty: 2,
    question: "useEffect 和 useLayoutEffect 有什么区别？各自在什么时候使用？",
    answer: "执行时机：\n\n- useEffect：在浏览器绘制之后异步执行，不阻塞渲染\n- useLayoutEffect：在 DOM 更新后、浏览器绘制之前同步执行\n\n流程对比：\n1. React 更新 DOM\n2. 执行 useLayoutEffect（同步，阻塞绘制）\n3. 浏览器绘制\n4. 执行 useEffect（异步）\n\n选择：\n- 大多数情况用 useEffect\n- 需要同步读取/修改 DOM 布局（避免闪烁）用 useLayoutEffect\n- 服务端渲染时 useLayoutEffect 会报警告",
    codeExample: "// useEffect：数据请求（不阻塞渲染）\nuseEffect(() => {\n  fetchData().then(setData)\n}, [])\n\n// useLayoutEffect：测量布局（避免闪烁）\nuseLayoutEffect(() => {\n  const { height } = ref.current.getBoundingClientRect()\n  setHeight(height)\n}, [])",
    tags: ["React", "useEffect", "useLayoutEffect", "Hooks"],
  },
  {
    id: "react-03",
    title: "React Fiber 架构",
    category: "react",
    difficulty: 3,
    question: "请解释 React Fiber 架构的设计思想和工作原理。",
    answer: "Fiber 是 React 16 引入的全新协调引擎。\n\n解决的问题：\n- React 15 的 Stack Reconciler 递归遍历，无法中断\n- 大量更新时主线程被长时间占用，导致掉帧\n\nFiber 核心思想：\n1. 可中断：将渲染工作拆分为小单元 (Fiber Node)\n2. 可恢复：保存进度，中断后可继续\n3. 可优先：不同更新有不同优先级\n\n双缓存机制：\n- current 树：当前屏幕显示的 Fiber 树\n- workInProgress 树：正在构建的新 Fiber 树\n- 交替使用，完成时切换\n\n调度流程：\n1. 触发更新 → 创建更新任务\n2. 调度器 (Scheduler) 按优先级排序\n3. 协调器 (Reconciler) 构建 workInProgress 树\n4. 渲染器 (Renderer) 提交变更到 DOM",
    codeExample: "// Fiber Node 结构（简化）\ninterface FiberNode {\n  type: string | Function  // 组件类型\n  stateNode: any           // DOM 节点\n  return: FiberNode | null // 父节点\n  child: FiberNode | null  // 第一个子节点\n  sibling: FiberNode | null// 下一个兄弟节点\n  alternate: FiberNode | null // 对应另一棵树\n  flags: number            // 副作用标记\n  lanes: number            // 优先级\n}",
    tags: ["React", "Fiber", "架构"],
  },
  {
    id: "react-04",
    title: "React 性能优化策略",
    category: "react",
    difficulty: 2,
    question: "React 中有哪些常用的性能优化手段？",
    answer: "组件级别：\n- React.memo：浅比较 props，避免不必要渲染\n- useMemo：缓存计算结果\n- useCallback：缓存函数引用\n- 合理拆分组件，缩小重渲染范围\n\n状态管理：\n- 状态下沉：将状态放到最接近使用它的组件\n- 内容提升：将不依赖变化的子组件作为 children 传入\n\n渲染优化：\n- 虚拟列表（react-window, react-virtuoso）\n- 懒加载（React.lazy + Suspense）\n- 代码分割（动态 import）\n- key 的正确使用\n\n工具：\n- React DevTools Profiler\n- why-did-you-render\n- Chrome Performance",
    codeExample: "// React.memo + useCallback 组合\nconst ExpensiveChild = React.memo(({ onClick }: { onClick: () => void }) => {\n  return <button onClick={onClick}>Click</button>\n})\n\nfunction Parent() {\n  const [count, setCount] = useState(0)\n  const handleClick = useCallback(() => {\n    setCount(c => c + 1)\n  }, [])\n  return <ExpensiveChild onClick={handleClick} />\n}",
    tags: ["React", "性能优化", "memo", "useMemo"],
  },
  {
    id: "react-05",
    title: "状态管理：Redux vs Zustand",
    category: "react",
    difficulty: 2,
    question: "请对比 Redux Toolkit 和 Zustand 的差异与使用场景。",
    answer: "Redux Toolkit：\n- 成熟生态，中间件丰富\n- 单一 store，action→reducer→store 单向数据流\n- 适合大型复杂应用\n- DevTools 强大\n\nZustand：\n- 极简 API，几乎零样板代码\n- 多 store 设计\n- 无需 Provider 包裹\n- TypeScript 友好，Bundle 极小 (~1KB)\n\n选择建议：\n- 团队已有 Redux 经验 → Redux Toolkit\n- 新项目、追求简洁 → Zustand",
    codeExample: "// Zustand 示例\nimport { create } from 'zustand'\n\nconst useBearStore = create<BearState>((set) => ({\n  bears: 0,\n  increase: () => set((state) => ({ bears: state.bears + 1 }))\n}))\n\nfunction Counter() {\n  const bears = useBearStore((s) => s.bears)\n  const increase = useBearStore((s) => s.increase)\n  return <button onClick={increase}>{bears}</button>\n}",
    tags: ["React", "Redux", "Zustand", "状态管理"],
  },
  {
    id: "react-06",
    title: "React 事件机制与合成事件",
    category: "react",
    difficulty: 2,
    question: "React 的合成事件 (SyntheticEvent) 和原生事件有什么区别？",
    answer: "合成事件：\n- React 自己的事件系统，跨浏览器兼容\n- 事件委托到根节点（React 17+）或 document（React 16）\n- 事件池复用（React 16，17 已移除）\n\n与原生事件区别：\n- 命名：驼峰式 (onClick) vs 全小写 (onclick)\n- this 指向：需要 bind 或箭头函数\n- 事件对象：SyntheticEvent 包装了原生事件\n- 执行时机：React 事件晚于原生事件\n\n执行顺序：\n1. 原生事件（捕获 → 目标 → 冒泡）\n2. React 合成事件（捕获 → 目标 → 冒泡）\n\n注意事项：\n- e.stopPropagation() 只阻止 React 事件冒泡\n- 需 e.nativeEvent.stopImmediatePropagation() 阻止原生\n- React 18 中事件委托到 root 而非 document",
    tags: ["React", "合成事件", "事件机制"],
  },
  {
    id: "react-07",
    title: "useRef 与 useState 的区别",
    category: "react",
    difficulty: 1,
    question: "useRef 和 useState 有什么区别？什么场景用 useRef？",
    answer: "区别：\n- useState 更新会触发组件重渲染，useRef 不会\n- useRef 可跨渲染周期保存可变值\n- useRef 返回 { current } 对象，直接修改 .current\n\nuseRef 场景：\n1. 访问 DOM 元素\n2. 保存上一次的值\n3. 存储不需要触发渲染的可变数据（如 timer ID）\n4. 保存子组件实例引用\n\n注意：\n- 修改 ref.current 不会触发重渲染\n- 不要在渲染过程中读写 ref（可能导致不一致）\n- 函数组件不能用 string ref",
    codeExample: "// 访问 DOM\nconst inputRef = useRef<HTMLInputElement>(null)\ninputRef.current?.focus()\n\n// 保存前一次渲染的值\nfunction usePrevious<T>(value: T) {\n  const ref = useRef<T>(value)\n  useEffect(() => { ref.current = value })\n  return ref.current\n}\n\n// 存储 timer\nconst timerRef = useRef<number>()\ntimerRef.current = setInterval(() => {}, 1000)\nclearInterval(timerRef.current)",
    tags: ["React", "useRef", "useState", "Hooks"],
  },
  {
    id: "react-08",
    title: "React 组件设计模式",
    category: "react",
    difficulty: 2,
    question: "常见的 React 组件设计模式有哪些？",
    answer: "1. 容器/展示模式 (Container/Presentational)：\n   - 容器组件：处理逻辑和数据\n   - 展示组件：纯 UI，通过 props 接收数据\n\n2. 组合模式 (Composition)：\n   - 使用 children / slots 组合\n   - 避免深层 prop 传递\n\n3. Render Props 模式：\n   - 通过函数 prop 动态决定渲染内容\n   - 共享逻辑的方式之一\n\n4. HOC (高阶组件)：\n   - 函数接收组件返回增强组件\n   - 如 withRouter, connect\n   - 现已被 Hooks 取代大部分场景\n\n5. 自定义 Hooks：\n   - 提取有状态逻辑的最佳方式\n   - 比 HOC 和 Render Props 更清晰\n\n6. Provider 模式：\n   - Context + Provider 跨层级传递数据",
    codeExample: "// 自定义 Hook 提取逻辑\nfunction useWindowSize() {\n  const [size, setSize] = useState({ w: 0, h: 0 })\n  useEffect(() => {\n    const handler = () => setSize({ w: innerWidth, h: innerHeight })\n    handler()\n    window.addEventListener('resize', handler)\n    return () => window.removeEventListener('resize', handler)\n  }, [])\n  return size\n}\n\n// 组合模式\nfunction Layout({ sidebar, content }) {\n  return (\n    <div className=\"flex\">\n      <aside>{sidebar}</aside>\n      <main>{content}</main>\n    </div>\n  )\n}",
    tags: ["React", "设计模式", "HOC", "组合", "自定义Hook"],
  },
  {
    id: "react-09",
    title: "React 18 新特性",
    category: "react",
    difficulty: 2,
    question: "React 18 引入了哪些重要新特性？",
    answer: "1. Concurrent Features（并发特性）：\n   - 可中断渲染，优先响应用户交互\n   - useTransition / startTransition：标记低优先级更新\n   - useDeferredValue：延迟更新非关键 UI\n\n2. Automatic Batching（自动批处理）：\n   - 所有状态更新自动批处理（不再限于 React 事件）\n   - setTimeout、Promise、原生事件中的更新也会批处理\n\n3. Suspense 增强：\n   - 支持服务端渲染 (SSR)\n   - Selective Hydration：按优先级逐步激活\n\n4. 新 API：\n   - createRoot：新的入口点\n   - flushSync：强制同步刷新\n   - useId：生成唯一 ID（SSR 安全）\n   - useSyncExternalStore：订阅外部数据源\n   - useInsertionEffect：CSS-in-JS 注入",
    codeExample: "// useTransition\nconst [isPending, startTransition] = useTransition()\n\nfunction handleChange(e) {\n  // 紧急更新：输入框\n  setInputValue(e.target.value)\n  // 非紧急更新：搜索结果\n  startTransition(() => {\n    setSearchQuery(e.target.value)\n  })\n}\n\n// useDeferredValue\nconst deferredQuery = useDeferredValue(query)\n<SearchResults query={deferredQuery} />",
    tags: ["React18", "Concurrent", "useTransition", "批处理"],
  },
  {
    id: "react-10",
    title: "Next.js 核心概念",
    category: "react",
    difficulty: 2,
    question: "Next.js 的核心特性有哪些？App Router 带来了什么改变？",
    answer: "核心特性：\n- 文件系统路由（基于文件夹结构）\n- SSR / SSG / ISR 灵活切换\n- API Routes（全栈能力）\n- 内置优化（图片、字体、脚本）\n- TypeScript 支持开箱即用\n\nApp Router (Next.js 13+)：\n- app/ 目录取代 pages/\n- React Server Components (RSC) 默认\n- 嵌套布局 (Layout) 持久化\n- Server Actions：服务端直接处理表单\n- Streaming SSR + Suspense\n\nServer vs Client Components：\n- Server：默认，无交互，无 Hooks，减少 JS 体积\n- Client：'use client'，可交互，有 Hooks\n\n数据获取：\n- Server Component 中直接 async/await\n- 缓存策略：no-store, revalidate 等",
    codeExample: "// app/page.tsx (Server Component)\nexport default async function Page() {\n  const data = await fetch('https://api.example.com/data')\n  const posts = await data.json()\n  return <PostList posts={posts} />\n}\n\n// app/components/Counter.tsx\n'use client'\nimport { useState } from 'react'\nexport default function Counter() {\n  const [count, setCount] = useState(0)\n  return <button onClick={() => setCount(c => c + 1)}>{count}</button>\n}",
    tags: ["Next.js", "App Router", "RSC", "SSR"],
  },
  {
    id: "react-11",
    title: "React 闭包陷阱 (Stale Closure)",
    category: "react",
    difficulty: 2,
    question: "React 中的闭包陷阱是什么？如何解决？",
    answer: "问题：函数组件中，闭包捕获的是创建时的 state 值，而不是最新值。\n\n常见场景：\n- useEffect/useCallback 依赖数组不完整\n- 定时器中读取到旧的 state\n- 事件处理函数引用旧状态\n\n解决方案：\n1. 补全依赖数组\n2. 使用函数式更新 setState(prev => prev + 1)\n3. 使用 useRef 保存最新值\n4. ESLint react-hooks/exhaustive-deps 规则\n5. 使用 useReducer 替代多个 useState",
    codeExample: "// ❌ 闭包陷阱\nconst [count, setCount] = useState(0)\nuseEffect(() => {\n  const id = setInterval(() => {\n    setCount(count + 1)  // 永远是 0 + 1 = 1\n  }, 1000)\n  return () => clearInterval(id)\n}, [])\n\n// ✅ 函数式更新\nuseEffect(() => {\n  const id = setInterval(() => {\n    setCount(c => c + 1)  // 始终用最新值\n  }, 1000)\n  return () => clearInterval(id)\n}, [])\n\n// ✅ useRef 保存最新值\nconst countRef = useRef(count)\ncountRef.current = count",
    tags: ["React", "闭包陷阱", "useState", "useEffect"],
  },
  {
    id: "react-12",
    title: "React 服务端组件 (RSC) 原理",
    category: "react",
    difficulty: 3,
    question: "React Server Components 的工作原理是什么？",
    answer: "核心思想：在服务端执行的 React 组件，结果序列化后发送到客户端。\n\n工作流程：\n1. 服务端渲染 RSC 为特殊格式（类似 JSON）\n2. 客户端接收并重建 React 树\n3. Client Components 正常在客户端执行\n4. RSC 可以直接访问数据库/文件系统\n\n限制：\n- 不能使用 useState, useEffect 等 Hooks\n- 不能使用浏览器 API\n- 不能有交互事件处理\n- 只能导入 Client Components 或其他 RSC\n\n优势：\n- 零客户端 JS 体积\n- 可直接访问后端资源\n- 自动代码分割\n- 减少 TTFB 和 FCP\n\n序列化格式：\n- 模块引用、元素、props 等\n- 支持流式传输 (Streaming)",
    tags: ["React", "RSC", "Server Components", "服务端渲染"],
  },
{
    id: "react-13",
    title: "React setState 执行机制",
    category: "react",
    difficulty: 2,
    question: "React 中 setState 是同步还是异步的？React 18 有什么变化？",
    answer: "setState 在不同场景下的表现：\n\nReact 合成事件/生命周期中：异步（批量更新）\n- 多次 setState 合并为一次更新\n- 获取最新状态用 setState(prev => newState)\n\nsetTimeout/原生事件/Promise 中：同步\n- 不受 React 批量更新机制控制\n\nReact 18 自动批处理（Automatic Batching）：\n- 所有更新都会批量处理（不再区别环境）\n- 包括 setTimeout、Promise、原生事件\n- 退出批量：ReactDOM.flushSync()\n\nuseState vs this.setState：\n- useState 不会自动合并对象\n- 需要手动展开：setState(prev => ({ ...prev, key: val }))",
    tags: ["React", "setState", "批量更新", "React18"],
  },
  {
    id: "react-14",
    title: "React 生命周期详解",
    category: "react",
    difficulty: 2,
    question: "React 类组件和函数组件的生命周期分别有哪些？",
    answer: "类组件生命周期：\n- 挂载：constructor → render → componentDidMount\n- 更新：shouldComponentUpdate → render → componentDidUpdate\n- 卸载：componentWillUnmount\n- 错误：componentDidCatch / getDerivedStateFromError\n\n函数组件（Hooks 模拟）：\n- 挂载：useEffect(() => {}, [])\n- 更新：useEffect(() => {})\n- 卸载：useEffect(() => { return cleanup }, [])\n- Mount + Update：useLayoutEffect\n- 每次渲染：无依赖的 useEffect\n\n废弃的生命周期（React 17+）：\n- componentWillMount\n- componentWillReceiveProps\n- componentWillUpdate\n\n注意：\n- Hooks 没有直接的 didMount/didUpdate/didUnmount\n- 需要靠依赖数组和 cleanup 函数模拟",
    codeExample: "// 函数组件生命周期等价\nuseEffect(() => {\n  // componentDidMount\n  fetchData()\n  return () => { /* componentWillUnmount */ }\n}, [])\n\nuseEffect(() => {\n  // componentDidUpdate (only count changed)\n  console.log('count changed:', count)\n}, [count])\n\nuseEffect(() => {\n  // render 后每次都执行\n})",
    tags: ["React", "生命周期", "useEffect", "类组件"],
  },
  {
    id: "react-15",
    title: "React refs 详解与应用",
    category: "react",
    difficulty: 1,
    question: "React refs 有几种使用方式？各自的应用场景？",
    answer: "refs 的三种创建方式：\n\n1. createRef（类组件）：this.myRef = React.createRef()\n\n2. useRef（函数组件）：const myRef = useRef(null)\n\n3. 回调 ref：ref={el => this.input = el}\n\n应用场景：\n- 访问 DOM 元素：inputRef.current.focus()\n- 保存不触发渲染的可变值（timer ID, 上次值）\n- 转发 ref 到子组件：React.forwardRef\n- 获取子组件实例：useImperativeHandle\n\n注意：\n- ref 变化不触发重渲染\n- 不要在 render 中读取/写入 ref\n- 函数组件没有实例（需 useImperativeHandle 暴露）",
    codeExample: "// forwardRef + useImperativeHandle\nconst Input = forwardRef((props, ref) => {\n  const inputRef = useRef<HTMLInputElement>(null)\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current?.focus(),\n    clear: () => { if (inputRef.current) inputRef.current.value = '' }\n  }))\n  return <input ref={inputRef} />\n})\n\n// 父组件使用\nconst parentRef = useRef<{ focus: () => void }>(null)\nparentRef.current?.focus()",
    tags: ["React", "refs", "useRef", "forwardRef"],
  },
  {
    id: "react-16",
    title: "React 类组件 vs 函数组件",
    category: "react",
    difficulty: 1,
    question: "React 类组件和函数组件有什么区别？为什么推荐函数组件？",
    answer: "类组件：\n- 有实例，可访问 this\n- 有生命周期方法\n- 代码量多，需要 bind 或箭头函数\n- 热重载不友好\n- 难以复用逻辑（HOC/Render Props）\n\n函数组件（推荐）：\n- 无实例，无 this\n- Hooks 模拟生命周期\n- 代码简洁\n- 逻辑复用简单（自定义 Hook）\n- 更好的 Tree Shaking\n- 更容易优化（React.memo）\n- 未来 React 特性更倾向函数组件\n\n为什么推荐：\n1. Hooks 解决了逻辑复用问题\n2. 代码更简洁易读\n3. TypeScript 类型推导更好\n4. React 18 并发特性对齐函数组件\n5. 官方推荐，社区趋势",
    tags: ["React", "类组件", "函数组件", "Hooks"],
  }
]
